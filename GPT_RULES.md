# GPT-Pub 工作约束

GPT-Pub 是一个完全独立的公开项目仓库。它独立制定规则、分配项目编号、维护开发主线、运行 CI 和发布版本，不由任何其他仓库管理，也不作为其他仓库的公开镜像。

## 1. 规则层级

规则按以下顺序生效：

1. 根目录 `GPT_RULES.md`；
2. 分类目录 `README.md`；
3. 项目 `README.md`；
4. 项目 `阶段记录.md`。

下一级可以增加更严格的约束，但不能削弱上一级规则。

## 2. 公共边界

只有已经明确确认可以公开的内容才能进入本仓库。禁止提交：

- 专利创意、未公开论文、实验记录和内部研究判断；
- 外部私有仓库的完整分支、完整 Git 历史或未经筛选的导出；
- Token、密码、密钥、Cookie、私有下载地址和内部访问凭据；
- 个人信息、真实用户文件、未脱敏日志、本机绝对路径和私人截图；
- 可重新推导出私有内容的索引、附件、缓存或构建产物。

敏感配置只进入 GitHub Secrets 或本地环境，不进入仓库。

## 3. 独立分类与编号

GPT-Pub 使用自己的分类和永久项目编号。当前结构为：

```text
projects/
└── 1-桌面软件/
    ├── 101-VideoUpright/
    └── 102-AtlasDesk/
```

编号规则：

- `1-桌面软件`：第一类项目；
- `P101–P199`：桌面软件项目；
- `P101`：VideoUpright；
- `P102`：AtlasDesk；
- 后续只有在出现真实新类别时，才建立新的分类和百位编号段。

GPT-Pub 的分类、编号和路径与任何外部仓库没有继承或对应关系。外部旧编号只在迁移记录中作为历史事实保留。

正式项目路径为：

```text
projects/<分类>/NNN-简短项目名/
```

## 4. 固定读取顺序

每次接入必须：

1. 阅读 `GPT_RULES.md`；
2. 阅读 `目录.md`；
3. 确认项目分类、编号和允许修改的路径；
4. 阅读分类 `README.md`；
5. 阅读项目 `README.md`、`阶段记录.md` 和迁移或发布资料；
6. 查看本仓库中的相关提交、分支、PR、标签和 CI；
7. 用一句话明确本次写入范围后再开始。

不得只凭聊天记忆工作，也不得重复建立近似项目。迁移完成后，本仓库文件是项目当前状态的唯一事实源。

## 5. 读写与项目隔离

- 默认可读取整个公开仓库；
- 默认只能修改用户指定的项目目录；
- 不得顺手修改其他项目、根规则、分类规则、`shared/` 或无关工作流；
- 新项目、迁移或项目状态实质变化时，才同步更新 `目录.md` 和分类入口；
- 原始资料不覆盖，修订版另存并说明原因。

本仓库是 monorepo，任何分支都包含整个仓库。因此每条项目分支相对最新 `main` 的有效差异只能位于：

```text
对应项目目录
对应项目的活动 CI
本项目确有必要的目录与状态记录
```

P101 分支不得修改 P102，P102 分支不得修改 P101。项目分支之间不得互相合并，只能通过 `main` 接收其他项目已经稳定的变化。

## 6. 长期分支模型

每个项目保留两条长期分支：

| 分支 | 作用 | 是否允许日常开发 |
|---|---|---|
| `p101-exp` / `p102-exp` | 当前项目的连续开发与实验集成 | 是 |
| `p101-stable` / `p102-stable` | 已通过完整验收、等待进入 `main` 的稳定候选 | 否 |
| `main` | 全仓已经稳定发布的共同主线 | 否 |

### 6.1 `exp`

- 大多数日常修改直接在对应 `exp` 分支连续推进；
- 每次开始新阶段前，先把最新 `main` 同步进 `exp`；
- 普通串行迭代可以直接形成清晰小提交；
- 高风险、并行或容易推翻的尝试，临时建立 `p101-简短任务名` 或 `p102-简短任务名`，完成后通过 PR 回到对应 `exp`；
- `exp` 可以失败和回退，但不得越过项目路径边界。

### 6.2 `stable`

- `stable` 只接收对应 `exp` 的稳定候选，不做日常开发；
- 禁止直接向 `stable` 写入功能提交；
- 提升前必须冻结当前候选，把最新 `main` 同步到 `exp`，并确认相对 `main` 只修改本项目；
- 使用 PR 执行 `exp → stable`，运行完整 Windows/Linux、原生自检、打包和必要的视觉验收；
- `stable` 不是版本档案，正式历史由 `main` 上的不可移动标签保存。

### 6.3 `main`

- `main` 只接收 `pNNN-stable`；
- 禁止 `exp`、临时任务分支或外部 fork 直接合并到 `main`；
- 使用 PR 执行 `stable → main`，再次确认范围、CI、版本和交付物；
- 一个 PR 只提升一个项目；
- 合并后在 `main` 建立项目版本标签和 Release。

### 6.4 合并后的同步

项目进入 `main` 后：

- 当前 `stable` 没有新增提交时，快进到最新 `main`；
- 当前 `exp` 没有继续开发时，同样快进到最新 `main`；
- `exp` 已经继续开发时，将最新 `main` 合并进 `exp`，不得强制重写其成果；
- 其他项目的 `exp` 在下一次开发或提升前同步最新 `main`。

推荐流程：

```text
main 同步到项目 exp
→ 日常开发主要推进 exp
→ PR：exp → stable
→ 完整验收
→ PR：stable → main
→ 在 main 打标签和发布
→ main 回流 stable / exp
```

## 7. 分支命名、PR 与标签

长期分支：

```text
p101-exp
p101-stable
p102-exp
p102-stable
```

临时分支：

```text
p101-简短任务名
p102-简短任务名
repo-简短任务名
```

PR 标题：

```text
P101｜任务名称
P102｜任务名称
仓库｜任务名称
```

正式版本标签只从 `main` 建立：

```text
p101-vX.Y.Z
p102-vX.Y.Z
```

外部仓库的旧标签只在迁移记录中说明，不直接导入本仓库活动标签体系。

PR 正文至少说明目标、源分支、目标分支、修改范围、公开安全检查、验证、结果、风险和生成文件。发现路径越界、验证失败、候选混入其他项目内容或存在隐私风险时必须停止。

## 8. 跨仓迁移硬约束

外部仓库只是迁移来源，不是 GPT-Pub 的上级或管理仓库。每个项目必须使用独立迁移 PR，禁止把任何外部仓库的 `main`、工作分支或完整 bundle 直接推入本仓库。

迁移必须完成：

1. 固定源仓库、源提交和允许路径；
2. 只过滤目标项目的有效历史；
3. 将旧路径重写到 GPT-Pub 的独立正式路径；
4. 扫描当前文件和全部保留历史中的敏感信息；
5. 核对文件数量、字节数和 SHA-256；
6. 检查提交信息、作者信息、附件、测试素材和历史工作流是否适合公开；
7. 在 GPT-Pub 中重新建立活动 CI，不直接激活旧仓库工作流；
8. 在目标环境完成验证；
9. 在项目迁移记录中说明源提交、旧路径和旧编号；
10. 迁移成功后只在 GPT-Pub 继续开发，不建立跨仓双主线。

首次迁移成功并进入 `main` 后，再从该 `main` 基线建立本项目的 `stable` 与 `exp` 长期分支。

## 9. CI 与 Actions

- `exp` 默认运行快速测试、静态检查和基础构建；
- `exp → stable` 运行完整项目 CI、原生自检、正式打包和必要视觉验收；
- `stable → main` 运行合并准入检查并核对正式产物；
- 工作流必须使用项目路径过滤，避免无关项目触发；
- 同一分支的新提交应取消旧运行；
- 纯文档修改不运行重量 CI；
- 一次性迁移、截图和发布工作流完成后删除；
- 公共 PR 默认权限保持只读，不使用 `pull_request_target` 执行不可信代码；
- Artifact 只保存必要交付，正式版本进入 GitHub Release。

## 10. 代码、产物与发布

代码交付必须给出运行环境、命令、测试和结果。生成目录、缓存、日志、测试媒体和可重新生成的中间产物不进入 Git。

正式发布至少核对：

- `main` 上的源提交；
- 版本号和标签；
- 目标平台；
- 自动测试和原生自检；
- EXE、ZIP 等交付物 SHA-256；
- 版本说明；
- 已知限制和签名状态。

## 11. 极简原则

新增目录、规则、自动化或公共框架前，先确认它解决已经真实出现的问题。没有明确用途的内容暂不添加。
