# GPT-Pub 工作约束

> 约束层级：A｜仓库级  
> 适用范围：整个 GPT-Pub  
> 下位规则：分类 `开发约束.md`、项目 `开发约束.md`

GPT-Pub 是独立的公开项目仓库。它独立维护项目编号、开发主线、CI、标签和 Release，不受其他仓库管理，也不作为其他仓库的自动镜像。

## A-01｜规则层级与权威性

规则按以下顺序生效：

```text
A｜/GPT_RULES.md
→ B｜分类/开发约束.md（如存在）
→ C｜项目/开发约束.md（如存在）
```

下位规则只能细化、补充或收紧上位规则，不得放宽或覆盖上位规则。

`README.md`、`目录.md`、`工作记录.md`、`阶段记录.md`、`设计与演进.md` 和各类建议书属于入口、状态、证据或参考文件，不具有覆盖 A/B/C 约束的效力。

确需突破上位规则时，必须在对应上位约束中修改原规则或登记限定范围的例外。例外至少写明对应规则、适用范围、原因、风险和失效条件。下位文件不得用“本项目特殊”自行生效。

## A-02｜公开与隐私边界

只有已经明确确认可以公开的内容才能进入本仓库。禁止提交：

- 专利创意、未公开论文、实验记录和内部研究判断；
- 外部私有仓库未经筛选的完整分支、完整 Git 历史、bundle 或导出包；
- Token、密码、密钥、Cookie、私有下载地址和内部访问凭据；
- 个人信息、真实用户文件、未脱敏日志、本机绝对路径和私人截图；
- 可重新推导出私有内容的索引、附件、缓存、测试数据或构建产物。

敏感配置只允许进入 GitHub Secrets 或本地环境。无法确认公开安全时必须停止，不得以“稍后再清理”为由先提交。

## A-03｜事实源与固定读取顺序

事实源固定为：

```text
实现事实 → 源码
验证事实 → 测试与 CI
历史事实 → Git、PR、标签与 Release
当前规则 → A/B/C 开发约束
当前状态 → README、工作记录与阶段记录
```

每次接入依次读取：

1. `/GPT_RULES.md`；
2. `/目录.md`，确认分类、项目编号和正式路径；
3. 分类 `开发约束.md`（如存在）；
4. 项目 `开发约束.md`（如存在）；
5. 项目 `README.md`、`阶段记录.md`、`工作记录.md`；
6. 与当前任务相关的设计、迁移和发布记录；
7. 相关分支、提交、PR、标签、CI 和 Release。

不得只凭聊天记忆继续工作。开始写入前必须用一句话明确允许修改的范围。

## A-04｜分类、编号与项目入口

GPT-Pub 使用自己的分类和永久项目编号。当前项目名称、编号、状态和正式路径只以 `/目录.md` 为准，不在本文件重复维护项目名单。

正式项目路径为：

```text
projects/<分类>/NNN-简短项目名/
```

后续只有出现真实新类别时才建立新的分类和编号段。外部仓库的旧编号只作为迁移历史保留，不自动继承为 GPT-Pub 编号。

## A-05｜写入范围与项目隔离

- 默认可读取整个公开仓库；
- 默认只能修改用户当前指定的项目或仓库级任务范围；
- 不得顺手修改其他项目、其他项目 CI、根规则、分类规则或无关公共文件；
- 一个项目的分支不得夹带另一个项目的修改；
- 根目录或分类级规则变更使用独立 `repo-简短任务名` 分支和独立 PR，不与产品版本提升混合；
- 新项目、迁移、改名或状态实质变化时，才同步更新 `/目录.md` 和分类入口。

项目分支相对最新 `main` 的有效差异只能位于：

```text
对应项目目录
对应项目的活动 CI
本项目确有必要的共享入口或状态行
```

外部原始材料、冻结快照和迁移证据不得覆盖；仓库内由 Git 管理的代码和文档正常原位修订，历史由 Git 保存，不另造“最终版、修订版、最终修订版”。

## A-06｜Git 与引用安全

长期分支原则上只允许：

```text
main
pNNN-exp
pNNN-stable
```

除上述分支外，其余项目分支和 `repo-*` 分支一律视为临时分支，不得长期保留。不得为了单独触发 CI、生成 Artifact、发布、记录更新或普通小修复而建立长期 `*-ci`、`*-release`、`*-record`、`*-fix` 等旁支。

- `main`、长期项目分支和正式标签禁止强推、重写历史或移动引用；
- 禁止直接向 `main` 写入，必须通过范围明确的 PR；
- 一个 PR 只处理一个项目或一个仓库级任务；
- 正式标签只能建立在 `main`，建立后不得移动或复用；
- 长期分支不得删除；
- 临时分支创建时必须有明确目标、基线、回流目标和终止条件；普通串行工作应直接使用对应 `exp`；
- 临时分支的 PR 合并后，在确认该分支准确 head 已被目标分支包含、没有合并后的新增独有提交时，应立即删除；允许使用经过审计的仓库级自动化删除这个精确 head；
- 临时分支未合并但明确废弃时，必须先审计其独有提交和实际文件差异。只有用户明确授权放弃这些独有内容，并在 PR 或清理记录中固定分支名、准确 head SHA、差异内容和废弃原因后，才允许删除；不得以“看起来没用”为依据直接删除；
- 删除分支不得使用宽泛通配符或未经逐条判定的批量规则。自动清理只能作用于事件给出的精确已合并 head，或当前任务已经审计并明确列出的精确对象；
- 发现来源、目标、差异范围或引用关系不清楚时，先比较分支和提交，不得靠猜测执行合并、重置或删除。

凡任务没有可适用的 B/C 级细则，尤其是 `repo-*` 仓库级任务，仍须遵守最低写入安全：不得用临时文件、探针、`noop`、空提交或默认分支写入探测权限；所有写操作必须明确目标非 `main` 分支、允许路径，并使用来自同一分支最新读取结果的文件 SHA；分支、参数或 SHA 来源不明，或任何写入失败时必须立即停止，不得省略分支重试、回退默认分支或先直写再删除；首次写入后及 PR 前必须比较工作分支与最新 `main`，确认来源、ahead/behind 和实际差异。

## A-07｜跨仓迁移

外部仓库只是迁移来源，不是 GPT-Pub 的上级仓库。每个项目必须使用独立迁移分支和 PR，禁止直接推入外部仓库的 `main`、工作分支或完整 bundle。

迁移至少完成：

1. 固定源仓库、源提交和允许路径；
2. 只提取目标项目的允许内容或经确认可公开的有效历史；
3. 将旧路径映射到 GPT-Pub 正式路径；
4. 扫描当前文件及保留历史中的敏感信息；
5. 核对文件数量、字节数、清单和 SHA-256；
6. 检查提交信息、作者信息、附件、素材和旧工作流是否适合公开；
7. 在 GPT-Pub 重新建立 CI，不直接激活旧仓库工作流；
8. 在目标环境完成验证；
9. 记录源提交、旧路径、旧编号和公开边界；
10. 迁移完成后只保留一个活动维护主线。

## A-08｜完成、验证与停止条件

文档写了“完成”不等于真实完成。任何合并、发布或迁移至少需要：

```text
实现已经存在
+ 完成标准已经满足
+ 有可追溯验证证据
```

出现以下任一情况必须停止，不得合并、打标签或发布：

- 修改范围越界；
- 分支来源或目标不明确；
- 必需 CI 失败、仍在运行或被意外跳过；
- 版本、提交、产物或哈希不一致；
- 产物缺失、不完整或混入私人数据；
- 仍存在未解释的安全、迁移或兼容风险。

## A-09｜极简原则

新增目录、规则、自动化、长期分支或公共框架前，必须确认它解决了已经真实出现的问题。能由现有文件、Git、CI 或一个清晰规则解决的，不再建立第二套体系。

分支治理同样遵守单一机制原则。能够由仓库级统一 branch hygiene 处理的临时分支收口，不再为每个项目长期维护重复的分支清理工作流。

## A-10｜公开科学计算与跨仓边界

GPT-Pub 可以保存公开安全的计算代码、测试、公开算例、原始数值结果和自生成图件，但不得保存其在私有项目中的真实动机、专利布局、内部判断或反向映射。

- 科学计算项目使用 GPT-Pub 自己的分类和编号，不为外部仓库预留号码，也不要求一一对应；
- 一个公开计算项目可以被多个外部项目复用，一个外部项目也可以引用多个公开计算项目；
- 本仓库不得出现私有仓库项目号、私有路径、私有项目名称、内部任务说明或“服务于某项未公开专利”等反向线索；
- 通用计算代码只接收明确的输入与 `OUTPUT_ROOT`，不得把某个本机盘符、私有仓库路径或外部项目编号写死在模型代码中；
- 本仓库的 Actions 只能生成公开输出，不得读取私有仓库、持有私有仓库写入凭据或把结果写入私有仓库；
- 一次运行只能有一个输出根目录，代码、配置、数据、图件、日志和清单不得跨公开与私有位置混写；
- 普通测试和计算 CI 只读仓库并上传 Artifact，不得自动提交结果、更新分支或创建未经审查的 Release；
- 分支名、提交信息、PR、工作流输入、日志、Artifact 名称、文件名、CSV 表头、SVG 注释和图片元数据同样属于公开内容，必须经过公开安全检查；
- 小型关键数据和最终图件可以进入 Git；大型中间结果优先使用限期 Artifact，确需长期冻结时使用 Release 资产与清单、SHA-256；
- 公开项目的正式路径一旦被外部文档引用，应保持稳定；标题可以演进，目录编号和路径不得随意改名。

任何输入、参数、几何、图件或数据能够直接揭示未公开技术核心时，即使没有解释文字，也不得进入 GPT-Pub。