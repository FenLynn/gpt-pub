# GPT-Pub 工作约束

本仓库只维护已经确认可以公开的软件、工具和公共代码。目标是让不同对话能够安全接续，同时严格隔离私有仓库、专利、未公开科研和个人资料。

## 1. 规则层级

规则按以下顺序生效：

1. 根目录 `GPT_RULES.md`；
2. 分类目录 `README.md`；
3. 项目 `README.md`；
4. 项目 `阶段记录.md`。

下一级可以增加更严格的约束，但不能削弱上一级规则。

## 2. 公共边界

只有已经明确确认可以公开的内容才能进入本仓库。禁止提交：

- 专利创意、未公开论文、实验记录和内部研究判断；
- 私有仓库的完整分支、完整 Git 历史或未经筛选的导出；
- Token、密码、密钥、Cookie、私有下载地址和内部访问凭据；
- 个人信息、真实用户文件、未脱敏日志、本机绝对路径和私人截图；
- 可重新推导出私有内容的索引、附件、缓存或构建产物。

敏感配置只进入 GitHub Secrets 或本地环境，不进入仓库。

## 3. 项目结构与编号

公开项目按主要交付目标分类。当前使用：

```text
projects/
└── 2-桌面软件/
```

桌面软件使用 `P201–P299`，正式路径为：

```text
projects/2-桌面软件/NNN-简短项目名/
```

历史项目迁移后，旧提交、旧标签、旧 PR 和旧报告中的编号不改写；迁移后的新分支、新标签、新 CI 和新文档使用当前编号。

## 4. 固定读取顺序

每次接入必须：

1. 阅读 `GPT_RULES.md`；
2. 阅读 `目录.md`；
3. 确认项目编号和允许修改的路径；
4. 阅读分类 `README.md`；
5. 阅读项目 `README.md`、`阶段记录.md` 和迁移或发布资料；
6. 查看相关提交、分支、PR、标签和 CI；
7. 用一句话明确本次写入范围后再开始。

不得只凭聊天记忆工作，也不得重复建立近似项目。

## 5. 读写隔离

- 默认可读取整个公开仓库；
- 默认只能修改用户指定的项目目录；
- 不得顺手修改其他项目、根规则、分类规则、`shared/` 或无关工作流；
- 新项目、迁移、项目状态实质变化时，才同步更新 `目录.md` 和分类入口；
- 原始资料不覆盖，修订版另存并说明原因。

## 6. 跨仓迁移硬约束

每个项目必须使用独立迁移 PR。禁止把 `gpt-hub`、`agent-foundry` 或其他私有仓库的 `main`、工作分支或完整 bundle 直接推入本仓库。

迁移必须同时完成：

1. 固定源仓库、源提交和允许路径；
2. 只过滤目标项目的有效历史；
3. 将旧路径重写到本仓库正式路径；
4. 扫描当前文件和全部保留历史中的敏感信息；
5. 核对文件数量、字节数和 SHA-256；
6. 检查提交信息、作者信息、附件、测试素材和历史工作流是否适合公开；
7. 重新建立本仓库活动 CI，不直接激活旧仓库工作流；
8. 在 Windows/Linux 等目标环境完成验证；
9. 记录旧编号、旧路径、源提交和历史映射；
10. 迁移成功后，私有仓库只保留链接与历史证据，不再双线维护。

发现任何无法确认是否可公开的内容，立即停止，不得猜测放行。

## 7. 分支与 PR

不得直接修改 `main`。

```text
p201-简短任务名      # P201
p202-简短任务名      # P202
repo-简短任务名      # 仓库级规则与架构
```

一个分支只服务一个主要项目和一个清晰任务。PR 默认先 Draft，标题格式为：

```text
P201｜任务名称
P202｜任务名称
仓库｜任务名称
```

PR 正文至少说明目标、修改范围、公开安全检查、验证、结果、风险和生成文件。任务范围清楚、验证通过、无隐私风险且不影响未授权项目时，可以转为正式并合并；否则停止报告。

## 8. CI 与 Actions

- 工作流必须使用项目路径过滤，避免无关项目触发；
- 纯文档修改不运行重量 CI；
- 普通代码 PR运行必要的自动检查；
- Windows 原生、视觉和正式打包按项目需要保留；
- 同一分支的新提交应取消旧运行；
- 一次性迁移、截图和发布工作流完成后删除；
- Artifact 只保存必要交付，正式版本进入 GitHub Release；
- 公共 PR 的默认权限保持只读，不使用 `pull_request_target` 执行不可信代码。

## 9. 代码、产物与发布

代码交付必须给出运行环境、命令、测试和结果。生成目录、缓存、日志、测试媒体和可重新生成的中间产物不进入 Git。

正式发布至少核对：

- 源提交；
- 版本号；
- 目标平台；
- 自动测试和原生自检；
- EXE/ZIP 等交付物 SHA-256；
- 版本说明；
- 已知限制和签名状态。

## 10. 极简原则

新增目录、规则、自动化或公共框架前，先确认它解决已经真实出现的问题。没有明确用途的内容暂不添加。
