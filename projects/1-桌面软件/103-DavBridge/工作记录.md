# DavBridge 工作记录

## 当前阶段

当前正式版本：**v0.4.0**。

当前 `p103-exp` 候选：**v0.4.11**。

最后完成完整 P103 CI 的代码 head：

```text
3c9de7086438f5e8bff03d5ec71a5ac89befccae
```

P103 CI run：`34704232146`，结果 `success`。

候选 Artifact：`DavBridge-v0.4.11-win-x64`，Artifact ID `10301252775`。

EXE：

```text
2426461 bytes
SHA256 54414f66e694a612f964dfad0e327864b06c96e28395affa90eab83d9aae527c
```

Artifact ZIP SHA256：

```text
7713397e02e7cfd558873b0bd7be622811640fd113b492ddf66a9eb8c260fa16
```

## v0.4.11 本轮完成

- 顶部路线改为长线段、等距箭头间隙、短线段，箭头不再贴近坚果云。
- InfiniCLOUD 使用深金色名称，坚果云使用深棕色名称。
- 镜像覆盖和流量预算图标重新光学居中。
- 当前任务对象名称降权为普通文字。
- 真实状态和任务进度移到中央。
- 右侧按钮只显示动作，不再把忙碌状态写进按钮。
- 点击暂停、继续、重试时中央状态立即进入对应过渡态，再由真实 snapshot 更新。
- 当前任务状态图标和左下角状态使用同一套语义。

## 验证

准确代码 head `3c9de708...` 的完整 CI 全绿：

```text
scope          success
frontend       success
core-smoke     success
windows-build  success
report-status  success
```

1100×825 总览预览使用运行态 mock 验证了“状态 + 63% 进度 + 暂停动作”布局；转移页同步复核通过。

本轮没有修改 `DavBridge.Core`。

## 当前断点

下一步等待用户 Windows 实机验收 v0.4.11。未经明确验收，不提升 `p103-stable` 或 `main`，不创建正式 Release。
