# DavBridge 工作记录

## 当前阶段

P103 已推进到 **v0.1.7 双安全门真实通过后的短时长期迁移验证阶段**。真实连接诊断、迁移就绪扫描、首个真实 `.zip + .prop` Group 的 PUT / Depth:0 / GET / SHA-256 闭环，以及 GoodSync 既有完整 Group 的 NO-WRITE 无 PUT 接管均已真实通过。长期迁移目前仍保持 Paused。

当前优先断点变更为：**短时开放长期迁移，验证真实非空目标上的连续处理、已有副本接管与缺失文件上传混合路径，并验证暂停、托盘后台和重启恢复。**

当前候选提升 PR：`#381`，`p103-exp → p103-stable`，保持 Draft。

## 当前工作项

| 工作项 | 状态 | 完成标准 | 证据或尚缺内容 |
|---|---|---|---|
| 项目骨架与交接体系 | 已完成 | P103 目录、A/B/C 约束、HANDOFF、长期分支和 CI 入口齐全 | GitHub 远端已固化 |
| GitHub 源码恢复布局 | 已完成 | 完整源码、solution、源码地图、本地产物隔离 | `代码/DavBridge.sln` 为统一入口 |
| InfiniCLOUD 认证 | 真实验证通过 | 正常访问源 `/zotero` | 实测目录响应 6,930 个可见条目 |
| 坚果云认证与目标目录 | 真实验证通过 | 正常访问 WebDAV 根目录与 `/zotero` | 根目录 8 项，`/zotero` 本次达到 750 项 |
| 迁移就绪扫描 | 真实验证通过 | 源端统计、单文件上限、zip/prop 配对均正常 | 6,929 文件、3,465 组、9.97 GB、最大 89.8 MB、超限 0、未配对 0 |
| 真实 PUT 强校验链 | 真实验证通过 | PUT 后准确资源确认、重新 GET、SHA-256 一致 | 首个真实 Group 两成员均 StrongVerified |
| GoodSync 既有目标接管 | **真实验证通过** | 已存在完整目标与源完全一致时无 PUT 接管 | 实机组总量约 13.0 KB，两个成员均 StrongVerified，上传增量严格 0 B，目标校验 GET 约 13.0 KB |
| NO-WRITE 保护 | **真实验证通过** | 既有副本验证时 `PutFileAsync` 代码级禁止，任何缺失/不一致只停止不覆盖 | 实机验证全过程未发生 PUT |
| 第二安全门持久化 | **真实验证通过** | 只有上传增量 0 B 且两个成员 StrongVerified 时写入 `ExistingReplicaValidationPassed=true` | 实机结果满足通过条件 |
| 上传与下载配额 | 自动验证通过，已进入真实计量 | 上传 1 GB、下载 3 GB、普通预留 50 MB、人工校准 | 首组上传 132 B、首组验证下载 132 B；既有副本验证上传 0 B、下载约 13.0 KB |
| 重置日期语义 | v0.1.6 起已实现并实机确认 | 只显示日期，不显示未经用户提供的具体时间 | 实机显示 `2026-09-07，当日 09:00 后探测上传` |
| 09:00 新周期探测 | 已实现，待真实重置日验证 | 09:00 前不清账，09:00 后真实 Group 强校验成功才确认新周期 | 自动 smoke + Windows build |
| 密码显示 | 已实机 | 与真实长度一致的 `*`，眼睛切换 | 正常 |
| 标题栏版本号 | 已实机 | `DavBridge vX.Y.Z` 从程序集读取 | 正常 |
| 首组验证安全门 | 真实验证通过 | 单组完成后立即停止，不自动启动整库 | 已验证 |
| 整库双安全门 | **真实验证通过** | 首个真实 PUT Group 与 NO-WRITE 既有副本接管均通过前，“开始 / 继续”不得启用整库 | 两个安全门均已有实机证据 |
| WinForms 托盘与后台 | 部分实机 | 关闭缩托盘、登录自启动、后台续传、暂停持久化、睡眠恢复 | 下一阶段短时验证 |
| 750 项分页 | 部分真实验证 | 已证明单次列举达到 750；真实分页协议仍待查 | 不影响准确文件 URL 校验 |

## 双安全门真实证据

### 安全门一：真实目标缺失上传

```text
1 zip + 1 prop
组总量：132 B
目标既有：0 / 2
实际上传：132 B
目标强校验下载：132 B
zip：StrongVerified
prop：StrongVerified
结束后：Paused
```

### 安全门二：GoodSync 既有完整副本 NO-WRITE 接管

为避免公开真实 Zotero storage key，不记录组 basename。

```text
1 zip + 1 prop
组总量：约 13.0 KB
验证前目标已有：2 / 2
验证模式：NO-WRITE
PutFileAsync：代码级禁止
实际上传增量：0 B
坚果云目标校验下载：约 13.0 KB
zip：StrongVerified
prop：StrongVerified
结果：通过
结束后：Paused
```

这证明当前真实服务上两种关键路径均已打通：

```text
目标缺失
→ InfiniCLOUD GET + SHA-256
→ 坚果云 PUT
→ 准确资源 PROPFIND Depth:0
→ 坚果云重新 GET + SHA-256
→ StrongVerified

目标已存在且由 GoodSync 预先同步
→ InfiniCLOUD GET + SHA-256
→ 坚果云 GET + SHA-256
→ 字节数与 SHA-256 完全一致
→ 不发生 PUT
→ StrongVerified
```

## 下一准确断点

```text
保持 v0.1.7
→ 两个安全门均已真实通过
→ 下一步短时点击“开始 / 继续”启用长期迁移
→ 先只运行一小段，不追求本周期跑满
→ 观察已有 GoodSync 文件是否继续 0 B 上传接管
→ 观察缺失文件是否正常 PUT + 强校验
→ 手动点击“暂停”确认能够在当前 Group 边界安全停下
→ 退出程序并重新启动，确认状态与进度恢复
→ 再验证关闭窗口缩托盘、后台续传和开机自启动
```

真实重置日到来时，再实测 09:00 探测策略。此前 `#381` 继续保持 Draft，不打正式标签，不创建 Release。
