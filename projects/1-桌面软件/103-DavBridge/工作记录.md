# DavBridge 工作记录

## 当前阶段

P103 已推进到 **v0.1.5 首个真实 Group 强校验通过**。真实连接诊断、迁移就绪扫描和首个真实 `.zip + .prop` Group 的 PUT / Depth:0 / GET / SHA-256 闭环均已通过。GitHub 远端源码恢复布局已经固化，当前长期迁移仍保持 Paused。

当前准确断点是：**本次首组目标端原先 0/2，因此真实缺失目标上传路径已经验证，但 GoodSync 既有目标的无 PUT 接管路径仍需单独做一次小组实测。该路径通过后，再开放整库长期迁移和后台无感运行验证。**

当前候选提升 PR：`#381`，`p103-exp → p103-stable`，保持 Draft，尚未合并。

## 当前工作项

| 工作项 | 状态 | 完成标准 | 证据或尚缺内容 |
|---|---|---|---|
| 项目骨架与交接体系 | 已完成 | P103 目录、A/B/C 约束入口、HANDOFF、长期分支和 CI 入口齐全 | 已建立 P103 文档、`p103-exp`、`p103-stable` 与 P103 CI |
| GitHub 源码恢复布局 | **已完成** | 远端包含完整源码、统一 solution、源码地图和本地产物隔离规则 | `代码/DavBridge.sln`、`代码/README.md`、`代码/.gitignore` 已建立 |
| InfiniCLOUD 认证 | **真实验证通过** | Connection ID/User ID + Apps Password 正常访问源 `/zotero` | 实测成功，目录响应 6,930 个可见条目 |
| 坚果云认证与目标目录 | **真实验证通过** | 注册邮箱 + 第三方应用密码访问根目录与 `/zotero` | 根目录可见 8 项，`/zotero` 本次响应达到 750 项 |
| 750 项限制实测 | 部分真实验证 | 证明单次列举可达到 750，并继续查明真实分页机制 | 已观察真实 750 项边界；751 项后的分页协议仍未知 |
| 迁移就绪扫描 | **真实验证通过** | 统计源端对象、逻辑组、总量、最大文件、超限和配对异常 | 6,929 个普通文件、3,465 组、9.97 GB、最大 89.8 MB、超限 0、未配对 0 |
| 强校验与假成功防护 | **真实 PUT 路径验证通过** | PUT 后准确资源确认、重新 GET、SHA-256 一致才成功 | 首个真实 Group 两成员均 StrongVerified |
| 真实 `PROPFIND Depth:0` | **验证通过** | PUT 后对准确目标文件 URL 独立确认 | 首个真实 Group 已覆盖 |
| Zotero Group 调度 | 自动验证通过，首组实测通过 | 同 basename 的 zip/prop 成组，只完成整组才通过 | 首个真实完整 Group 已通过 |
| GoodSync 既有目标接管 | 自动验证通过，**真实待验证** | 目标已有且与源完全一致时只 GET + SHA-256 接管，不 PUT | 真实目标至少 750 个可见对象，但首组目标为 0/2，尚未覆盖该路径 |
| 上传与下载配额 | 自动验证通过，已进入真实计量 | 1 GB 上传、3 GB 下载、普通预留 50 MB、人工校准、24 h 冲刺 | 首组实际计入上传 132 B、目标校验下载 132 B |
| 密码设置体验 | 已实机 | 已保存密码按真实长度显示 `*`，眼睛切换明文 | v0.1.5 实机运行正常 |
| 标题栏版本号 | 已实机 | 标题显示 `DavBridge vX.Y.Z` 且从程序集版本读取 | v0.1.5 标题栏实机正常 |
| 首组验证安全门 | **真实验证通过** | 自动选择一组、展示计划、只处理一组、完成后停止 | 首个真实 Group 通过后仍保持 Paused |
| 整库迁移准入锁 | 已解锁条件验证 | 完整 Group 两成员 StrongVerified 后才允许进入长期迁移确认 | 当前已有首组满足条件，但暂不实际开放整库运行，先补 GoodSync 接管实测 |
| 状态持久化 | 自动验证通过 | 原子保存、`.bak` 回退、异常退出后继续 | smoke 已覆盖 |
| WinForms 托盘与后台 | 部分实机验证 | 关闭缩托盘、登录自启动、后台继续、暂停持久化、睡眠唤醒 | 窗口与托盘路径可用，长时间后台和睡眠唤醒仍待验证 |
| 大清单基线 | 自动验证通过 | 6000 Group / 12000 对象逻辑分组 | smoke 通过 |
| 仓库临时分支清理 | 待处理 | 清理无独有提交的临时分支 | 不得参与开发、PR 或发布 |

## 2026-08-12 真实证据

### 连接与就绪扫描

```text
InfiniCLOUD 源目录响应：6,930 个可见条目
坚果云 WebDAV 根目录：8 个可见对象
坚果云 /zotero：本次响应 750 项

迁移就绪扫描：
源端普通文件对象：6,929
Zotero 逻辑组：3,465
源端总量：9.97 GB
最大文件：89.8 MB
超过单文件上限：0
未配对 zip/prop：0
目标端显示：750+
```

### 首个真实 Group

为避免把真实 Zotero storage key 写入公开仓库，不记录组名。

```text
成员：1 zip + 1 prop
组总量：132 B
目标既有：0 / 2
实际上传：132 B
坚果云强校验下载：132 B
zip：StrongVerified
prop：StrongVerified
异常：0
执行结束后：Paused
```

代码中的 `StrongVerified` 意味着真实执行了：源端 GET + SHA-256、目标 PUT、准确文件 URL 的 `PROPFIND Depth:0`、目标重新 GET + SHA-256、源端传输后再次元数据检查。任何目标缺失、长度不符、哈希不一致或源端中途变化都不会进入该状态。

## 下一准确断点

```text
保持长期迁移 Paused
→ 增加或使用一个“既有副本验证”受控入口
→ 自动寻找目标端已经存在的较小完整 zip + prop Group
→ 验证两个目标成员只做 GET + SHA-256 即可接管
→ 确认本次上传增量为 0
→ 确认两个成员 StrongVerified
→ 再开放长期迁移准入
→ 先短时后台运行，验证托盘、暂停、重启恢复、开机自启动
→ 稳定后才进入每周期长期无感搬运
```

在 GoodSync 既有目标真实接管和后台运行得到实机证据前，不合并 `#381`，不打正式标签，不创建 Release。
