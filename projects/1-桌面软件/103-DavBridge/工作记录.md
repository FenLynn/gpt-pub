# DavBridge 工作记录

## 当前阶段

当前正式 GitHub Release：**v0.4.0**。

当前 stable/main 稳定基线：**v0.4.16**。

```text
main         73aefcf04570180bb9526a43cf805aff1e7673b3
p103-stable  73aefcf04570180bb9526a43cf805aff1e7673b3
```

当前 `p103-exp` 候选：**v0.4.18**。

功能实现 head：

```text
e76e4deda04ad0a5a9839e8a9781c0dbcce004d5
```

## v0.4.18 目标

在不增加复杂图表、不新增高频状态采样的前提下，让用户一眼判断 DavBridge 最近一天是否健康工作。

## 本轮完成

- ProductExperience 增加 OperationalHealthV048；
- 默认统计近 24 小时 warning、网络等待、暂停完成、清单完成次数；
- About 页增加轻量健康条；
- 同时显示当前 StrongVerified 和当前 backlog；
- 如果 40 条活动窗口不足以覆盖完整 24 小时，UI 明确提示活动窗口可能截断；
- 增加 deterministic operational health native self-test；
- Windows CI 强制检查 operationalHealthSummary；
- 不修改 DavBridge.Core 与迁移安全链。

## 数据边界

所有健康统计仅来自既有通用活动与当前聚合计数。不会写入真实 Zotero 文件名、远端路径、URL、凭据或 SHA，也不会反向参与迁移判断。

## 验证

v0.4.18 准确验证代码 head：

```text
2c70a847c870133ae8e7f681929ff029ac35bcd8
```

完整 P103 CI run：`34752362599`。

```text
scope          success
frontend       success
core-smoke     success
windows-build  success
report-status  success
```

Windows candidate：`DavBridge-v0.4.18-win-x64`，Artifact ID `10316611787`。

EXE：`2455133 bytes`，SHA256 `7a5d10b2caa99d30495d8f1754e2f72c4d9b9a9448ce6b5d0a2c588ac031d884`。

Artifact ZIP SHA256：`a5a0f702d484b3ecf78c5bc89c37518f7de39e9cd53589e6b036573936fd3b49`。

About 1100×825 CI 预览已人工检查，健康条保持轻量，没有破坏 v0.4.17 About 布局。

## 当前断点

等待用户 Windows 实机查看 v0.4.18 About 页的近 24 小时运行健康摘要。未经用户授权，不提升 stable/main，不创建 tag 或 Release。
