# DavBridge 工作记录

## 当前阶段

P103 已推进到 **v0.1.4 真实服务验证候选**。核心逻辑、Mock WebDAV、Windows 构建和 Runtime/Data 边界持续通过自动验证；2026-08-12 已首次取得用户真实账户的连接诊断证据：InfiniCLOUD 与坚果云认证均成功，坚果云 `/zotero` 单次目录响应达到 750 项。当前准确断点是：**先完成迁移就绪扫描、流量校准和一个可控 Zotero Group 的真实接管或 PUT/GET/SHA-256 闭环，再开放整库长期后台迁移。**

当前候选提升 PR：`#381`，`p103-exp → p103-stable`，保持 Draft，尚未合并。

## 当前工作项

| 工作项 | 状态 | 完成标准 | 证据或尚缺内容 |
|---|---|---|---|
| 项目骨架与交接体系 | 已完成 | P103 目录、A/B/C 约束入口、HANDOFF、长期分支和 CI 入口齐全 | 已建立 P103 文档、`p103-exp`、`p103-stable` 与 P103 CI |
| 核心 WebDAV 读写层 | 自动验证通过，真实读连接部分验证 | 源端只读，目标端支持 PUT、Depth:0 属性确认和 GET 强校验 | Mock smoke + Windows 编译通过；真实 PUT/Depth:0/GET 强校验仍待验证 |
| InfiniCLOUD 认证兼容 | **真实验证通过** | Connection ID/User ID + Apps Password 正常完成源端 PROPFIND | 2026-08-12 用户 v0.1.3 实测连接成功，源 `/zotero` 本次目录响应包含 6,930 个可见对象 |
| 坚果云认证与目标目录 | **真实验证通过** | 注册邮箱 + 第三方应用密码可访问 WebDAV 根目录与 `/zotero` | 2026-08-12 用户 v0.1.3 实测根目录可见 8 项，`/zotero` 本次响应正好 750 项 |
| 750 项限制实测 | 部分真实验证 | 证明 750 为单次列表边界，并进一步明确真实分页机制 | 已真实观察 `/zotero` 单次响应为 750；尚未取得 751 项以后的分页协议细节，因此不得把 750 当作目标总数 |
| Zotero Group 调度 | 自动验证通过 | 同 basename 的 zip/prop 成组，部分完成后只重试未完成成员 | smoke 覆盖分组与部分组恢复 |
| 强校验与假成功防护 | 自动验证通过 | PUT 成功但目标缺失、长度或哈希异常不得记成功 | smoke 已覆盖；真实云端闭环待验证 |
| GoodSync 既有目标接管 | 自动验证通过，待真实内容验证 | 已存在目标与源端完全一致时直接强校验接管，不重复 PUT；不一致时冲突 | 用户真实目标已确认至少存在 750 个可见对象；尚未完成第一个实际文件 SHA-256 接管 |
| 上传与下载配额 | 自动验证通过 | 上传 1 GB、下载 3 GB、50 MB 普通预留、人工校准、24 h 冲刺、失败保守计量 | 配额模型通过；真实坚果云计费仍待实测 |
| 组级额度预判 | 已修复 | 目标缺失成员才预留上传额度；已有成员先校验；每个实际 PUT 前再次检查上传额度 | v0.1.2 起已进入核心逻辑 |
| 密码设置体验 | v0.1.4 已实现，待实机验证 | 已保存密码以与真实长度一致的 `*` 显示，眼睛按钮切换显示/隐藏，不要求用户每次重填 | v0.1.4 设置页已更新 |
| 状态持久化 | 自动验证通过 | 原子保存、`.bak` 回退、异常退出后继续 | `.bak` 损坏恢复 smoke 通过 |
| WinForms 托盘壳 | 部分实机验证 | 登录自启动、关闭缩托盘、后台自动继续、暂停持久化 | 窗口与连接诊断已在用户 Windows 11 正常运行；长期后台、睡眠唤醒仍待验证 |
| 首次启用非空目标 | 已修复 | 目标目录已有 GoodSync 文件不阻止迁移，一致接管、不一致冲突 | UI 已更新；真实接管闭环待验证 |
| 大清单基线 | 自动验证通过 | 至少覆盖 6000 个 Zotero 附件 / 12000 个 WebDAV 对象的逻辑分组 | 大清单 smoke 通过 |
| 迁移就绪扫描 | **下一步** | 真实统计源端对象、逻辑组、总量、最大文件、>500 MB 和 zip/prop 异常 | 两端连接已经通过，可以开始此步 |
| 正式迁移就绪 | 待验证 | 就绪扫描、流量校准、既有目标接管策略实测、首个真实 zip/prop Group 强校验通过 | 尚未达到 |
| 仓库临时分支清理 | 待处理 | 删除无独有提交且不使用的 `p103-v010-candidate-check`、`p103-stable-safety-anchor`、`p103-do-not-use` | 当前连接器尚未提供删除分支引用接口；三者不得参与开发、PR 或发布 |

## 2026-08-12 真实连接诊断证据

用户 Windows 11 上使用 v0.1.3：

```text
InfiniCLOUD
✓ 连接成功，源目录可访问
  本次目录响应：6,930 个可见对象

坚果云 WebDAV 根目录
✓ 认证成功，根目录可访问
  本次响应：8 个可见对象

坚果云 /zotero
✓ 目标目录可访问
  本次响应：750 个可见对象
```

由此可以正式更新事实边界：

- InfiniCLOUD 真实认证与源目录 PROPFIND 已验证；
- 坚果云真实 Basic Auth、根目录与 `/zotero` PROPFIND 已验证；
- 750 项边界在用户真实目标库中被实际触发；
- 当前 750 只能表述为“本次响应可见 750 项”或“至少 750 项”，不能表述为目标端总文件数；
- 单文件直接 URL 校验与迁移核心仍不依赖完整目录枚举；
- 坚果云真实 PUT、准确文件 Depth:0、重新 GET 和 SHA-256 仍待下一阶段验证。

## 当前自动验证基线

P103 CI 当前要求：

```text
范围门禁
→ .NET 8 核心 restore
→ 核心 smoke
→ Windows x64 restore / publish
→ framework-dependent 单 EXE Runtime 检查
→ 隔离 APPDATA / LOCALAPPDATA 自检
→ SHA-256
→ Windows 候选 Artifact
```

当前 smoke 至少覆盖：

1. zip/prop 逻辑分组；
2. 50 MB 普通预留与 24 h 冲刺；
3. `state.json` 损坏后的 `.bak` 回退；
4. 正常端到端强校验；
5. PUT 返回成功但目标实际不存在；
6. 传输期间源端变化；
7. GoodSync 既有目标内容一致时安全接管；
8. 既有目标内容不一致时冲突且不覆盖；
9. 既有目标校验受下载额度保护；
10. PUT 后崩溃恢复且不重复上传；
11. zip 已完成时只处理剩余 prop；
12. 超过单文件上限安全阻塞；
13. 6000 个 Zotero 附件 / 12000 个对象的大清单分组。

## 下一准确断点

```text
运行最新 v0.1.4 候选
→ 设置页核对等长 * 与眼睛显示/隐藏
→ 迁移就绪扫描
→ 核对源端对象数量、逻辑组、总量、最大文件和 zip/prop 异常
→ 目标端若仍显示 750+，仅作为截断列表提示，不作为总数
→ 校准坚果云当前上传、下载已用量与下一重置时间
→ 在整库长期迁移启用前，完成一个可控的小 Zotero Group 的真实接管或 PUT/Depth:0/GET/SHA-256 闭环
```

在真实首组闭环完成前，不合并 `#381`，不打标签，不创建正式 Release，不把 Mock/CI 结果描述为真实云端已经验证。
