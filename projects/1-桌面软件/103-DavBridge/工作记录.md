# DavBridge 工作记录

## 当前阶段

当前正式版本：**v0.4.0**。

当前 `p103-exp` 候选：**v0.4.10**。

最后完成完整 P103 CI 的代码 head：

```text
9bd983c248b30c6908ccb89786766f4f6321fe1f
```

P103 CI run：`34700404112`，结果 `success`。

候选 Artifact：`DavBridge-v0.4.10-win-x64`，Artifact ID `10299967686`。

EXE：

```text
2422365 bytes
SHA256 19c5529124ec95d052e418e79b282dbd003ff9352331fe05c1741cb834ca33d5
```

Artifact ZIP SHA256：

```text
1a637b19339d826c53e6a2e0cec24ced7c1834001d969fef08b22d4f34bc2e2e
```

## v0.4.10 本轮完成

本轮继续只做 UI 和设置交互收束：

- 首页蓝色路线延伸到目标端，三阶段连接间距统一。
- 镜像覆盖图标重画，覆盖进度使用更明显的蓝色渐变。
- 流量预算紫色上下箭头改为加粗 SVG，标题与进度内容距离缩短。
- 设置入口改为明确齿轮。
- 左下角状态图标全部 SVG 化，暂停为纯双竖线。
- 转移页重构为与首页一致的三条轻量队列行，解释进入 tooltip。
- tooltip 改为 Teleport 到 body 的全局最高层。
- 设置账户字段恢复正常编辑交互；已有迁移记录时只在保存阶段保留端点身份安全门。

## 验证

准确代码 head `9bd983c2...` 的完整 CI 全绿：

```text
scope          success
frontend       success
core-smoke     success
windows-build  success
report-status  success
```

1100×825 总览与转移预览已人工检查。转移页信息密度显著降低，首页阶段节奏统一，路线右端进一步贴近坚果云。

相对 v0.4.9 本轮没有修改 `DavBridge.Core`。

## 当前断点

下一步等待用户 Windows 实机验收 v0.4.10。未经明确验收，不提升 `p103-stable` 或 `main`，不创建正式 Release。
