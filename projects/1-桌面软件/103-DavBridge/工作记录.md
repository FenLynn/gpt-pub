# DavBridge 工作记录

## 当前阶段

P103 已推进到 **v0.1.2 真实服务验证候选**。代码、Mock WebDAV、Windows 构建和 Runtime/Data 边界持续通过自动验证，当前准确断点是：**下一步只做修复后的真实 InfiniCLOUD 认证、源端扫描、坚果云既有 GoodSync 文件接管和首个真实小 Group 闭环，不直接放开整库长期迁移。**

当前候选提升 PR：`#381`，`p103-exp → p103-stable`，保持 Draft，尚未合并。

## 当前工作项

| 工作项 | 状态 | 完成标准 | 证据或尚缺内容 |
|---|---|---|---|
| 项目骨架与交接体系 | 已完成 | P103 目录、A/B/C 约束入口、HANDOFF、长期分支和 CI 入口齐全 | 已建立 P103 文档、`p103-exp`、`p103-stable` 与 P103 CI |
| 核心 WebDAV 读写层 | 自动验证通过 | 源端只读，目标端支持 PUT、Depth:0 属性确认和 GET 强校验 | Mock smoke + Windows 编译通过；真实服务仍待验证 |
| InfiniCLOUD 认证兼容 | 待实机复验 | 使用 Connection ID/User ID + Apps Password 正常完成源端 PROPFIND | v0.1.1 起加入 handler credentials、pre-auth 与更完整 401 诊断；需用户实机复验 |
| Zotero Group 调度 | 自动验证通过 | 同 basename 的 zip/prop 成组，部分完成后只重试未完成成员 | smoke 覆盖分组与部分组恢复 |
| 强校验与假成功防护 | 自动验证通过 | PUT 成功但目标缺失、长度或哈希异常不得记成功 | smoke 已覆盖 |
| GoodSync 既有目标接管 | 自动验证通过，待真实验证 | 已存在目标与源端完全一致时直接强校验接管，不重复 PUT；不一致时冲突 | smoke 已覆盖一致接管、不同内容冲突和下载额度保护 |
| 上传与下载配额 | 自动验证通过 | 上传 1 GB、下载 3 GB、50 MB 普通预留、人工校准、24 h 冲刺、失败保守计量 | 配额模型通过；真实坚果云计费仍待实测 |
| 组级额度预判 | 已修复 | 目标缺失成员才预留上传额度；已有成员先校验；每个实际 PUT 前再次检查上传额度 | v0.1.2 核心逻辑已更新，等待最新 CI 候选 |
| 状态持久化 | 自动验证通过 | 原子保存、`.bak` 回退、异常退出后继续 | `.bak` 损坏恢复 smoke 通过 |
| WinForms 托盘壳 | 待实机验证 | 登录自启动、关闭缩托盘、后台自动继续、暂停持久化 | Windows CI 已编译并通过隔离自检 |
| 首次启用非空目标 | 已修复 | 目标目录已有 GoodSync 文件不再被旧逻辑直接阻止，启用前明确说明安全接管与冲突策略 | v0.1.2 UI 已更新，等待实机验证 |
| 大清单基线 | 自动验证通过 | 至少覆盖 6000 个 Zotero 附件 / 12000 个 WebDAV 对象的逻辑分组 | 大清单 smoke 通过 |
| 真实 InfiniCLOUD / 坚果云 capability probe | 待验证 | 真实连接、源端列举、目标访问、Depth:0、首个小对象 PUT/GET 有实测记录 | 需要用户真实账户实机验证 |
| 正式迁移就绪 | 待验证 | 现有库扫描、无 >500 MB 阻塞、流量校准、既有目标接管策略实测、首个真实 zip/prop Group 强校验通过 | 尚未达到 |
| 750 项后目标完整枚举 | 待验证 | 明确坚果云真实分页机制并完成 >750 项测试 | 官方公开资料不足，必须真实服务验证；不阻塞单文件直接 URL 校验 |
| 仓库临时分支清理 | 待处理 | 删除无独有提交且不使用的 `p103-v010-candidate-check`、`p103-stable-safety-anchor`、`p103-do-not-use` | 当前连接器尚未提供删除分支引用接口；三者不得参与开发、PR 或发布 |

## 当前自动验证基线

P103 CI 当前要求：

```text
范围门禁
→ .NET 8 核心 restore
→ 核心 smoke
→ Windows x64 restore / publish
→ framework-dependent 单 EXE Runtime 检查
→ 隔离 APPDATA / LOCALAPPDATA 自检
→ SHA-256
→ Windows 候选 Artifact
```

当前 smoke 至少覆盖：

1. zip/prop 逻辑分组；
2. 50 MB 普通预留与 24 h 冲刺；
3. `state.json` 损坏后的 `.bak` 回退；
4. 正常端到端强校验；
5. PUT 返回成功但目标实际不存在；
6. 传输期间源端变化；
7. GoodSync 既有目标内容一致时安全接管；
8. 既有目标内容不一致时冲突且不覆盖；
9. 既有目标校验受下载额度保护；
10. PUT 后崩溃恢复且不重复上传；
11. zip 已完成时只处理剩余 prop；
12. 超过单文件上限安全阻塞；
13. 6000 个 Zotero 附件 / 12000 个对象的大清单分组。

## 下一准确断点

取得当前 `p103-exp` 最新绿色 Windows Artifact 后，在用户 Windows 11 上按以下顺序验证：

```text
关闭旧 DavBridge
→ 运行最新 v0.1.2 候选
→ 保留已有加密配置
→ 迁移就绪扫描
→ 确认 InfiniCLOUD 401 已解决
→ 核对源端对象数量、总量、最大文件和 zip/prop 异常
→ 核对坚果云当前可见的 GoodSync 既有文件数量
→ 校准当前坚果云上传、下载已用量与下一重置日期
→ 只启用一个可控的小 Group 做真实接管或 PUT/GET/SHA-256 闭环
```

在真实能力验证完成前，不合并 `#381`，不打标签，不创建正式 Release，不把 Mock/CI 结果描述为真实云端已经验证。
