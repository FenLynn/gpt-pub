# DavBridge 工作记录

## 当前阶段

当前正式版本：**v0.4.0**。

当前 `p103-exp` 候选：**v0.4.15**。

最后完成完整 P103 CI 的代码 head：

```text
92f3a05a8b351aab357250899c7de0ecb82ce760
```

P103 CI run：`34736655980`，结果 `success`。

候选 Artifact：`DavBridge-v0.4.15-win-x64`，Artifact ID `10311605517`。

EXE：`2442845 bytes`，SHA256 `aa72ee8ec14c4fbf6fe8bcd117a3fc041864233af1bbaaec9ccf92b8a843569e`。

Artifact ZIP SHA256：`908b1bef4a812b5329565303672da14b051ad152a823c5df077ad2b102630770`。

## v0.4.15 本轮完成

- 当前任务进度条改为真正的目标端“上传进度”，不再复用下载或强校验下载进度。
- 上传 100% 后仍可继续显示“正在强校验 / 正在安全暂停”等真实状态，100% 不再暗示整个事务完成。
- 安全暂停中央状态、左下角状态和按钮文案去重；按钮固定显示短文案“暂停中”。
- 暂停 / 继续不再额外弹出重复 Toast。
- 转移页去掉“已暂停”重复。
- 最近活动合并同一状态转换链中的重复事件。
- 最近活动滚动条默认隐藏，hover 时轻量出现。
- 流量校准原生模态打开时，后方 Web UI 轻度降权。

## 验证

准确代码 head `92f3a05a8b351aab357250899c7de0ecb82ce760` 的完整 CI 全部通过：

```text
scope          success
frontend       success
core-smoke     success
windows-build  success
report-status  success
```

1100×825 总览和转移预览已复核，新增“上传进度”标签没有破坏当前任务布局。

## 当前断点

等待用户 Windows 实机验收 v0.4.15 的状态语义、最近活动去重和原生模态背景降权。未经明确验收，不提升 `p103-stable` 或 `main`，不创建正式 Release。
