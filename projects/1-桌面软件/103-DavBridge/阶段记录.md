# DavBridge 阶段记录

## 当前基线

P103 DavBridge 当前处于首次开发和真实服务验证阶段，已推进到 **v0.1.7 双安全门与第一轮短时长期迁移均真实通过**。两端 WebDAV 认证、目录访问、迁移就绪扫描、首个真实 Zotero `.zip + .prop` Group 的 PUT / Depth:0 / GET / SHA-256 强校验闭环、GoodSync 既有副本 NO-WRITE 接管，以及第一轮连续迁移与人工暂停均已取得实机证据，但仍不是正式版本。

- 当前开发分支：`p103-exp`
- 稳定候选分支：`p103-stable`
- 候选提升 PR：`#381`，Draft，`p103-exp → p103-stable`
- 正式版本：无
- 正式标签：无
- Release：无

PR 和 Actions 是候选准确 head SHA、CI 与 Artifact 的事实源。

## v0.1.7 当前设计基线

- Windows x64，C# / .NET 8 / WinForms，framework-dependent 单文件发布；
- InfiniCLOUD 正式产品代码只读，坚果云为唯一目标写端；
- `.zip` 与 `.prop` 成组处理；
- PUT 成功后必须针对准确资源执行 `PROPFIND Depth:0`，随后从坚果云重新 GET，并与源端做 SHA-256 强校验；
- GoodSync 或其他客户端已经存在于目标端的文件允许经过 GET 与 SHA-256 后安全接管，完全一致时不重复 PUT；
- 既有目标与源端不同且无法证明是 DavBridge 自己写入的旧版本时进入冲突，不自动覆盖；
- 独立“既有副本验证”采用代码级 `NO-WRITE` 目标包装器，`PutFileAsync` 直接禁止；
- 长期迁移有两个代码级安全门：一个真实上传 Group 强校验通过，一个既有 GoodSync Group 在 NO-WRITE 模式下无 PUT 接管通过；
- 上传 1 GB、下载 3 GB 都进入安全额度模型，普通阶段为其他服务保留 50 MB；
- 流量允许人工校准，DavBridge 在校准点以后保守累计真实 PUT 与目标验证 GET；
- 坚果云重置值只保存日期。重置日当地时间 09:00 以前不清零旧账本；09:00 后以最小仍需真实 PUT 的完整 Group 做新周期探测，完整强校验成功后才确认新周期；
- 登录后可以自动启动并托盘静默运行，暂停状态持久化；
- 已保存密码按真实长度显示为 `*`，眼睛按钮可临时显示明文；
- 坚果云目录列举达到 750 项时只表述为 `750+` 或“本次列举达到上限”；
- 完整源码统一以 `代码/DavBridge.sln` 恢复，开发期源码事实源为远端 `p103-exp`；
- 最终 `Content Complete` 与 `Exact Mirror` 分开判定。

## 已获得的真实服务证据

2026-08-12，用户 Windows 11 已取得：

### 连接与就绪扫描

```text
InfiniCLOUD 源目录响应：6,930 个可见条目
坚果云 WebDAV 根目录：8 个可见对象
坚果云 /zotero：本次响应达到 750 项

源端普通文件对象：6,929
Zotero 逻辑组：3,465
源端总量：9.97 GB
最大文件：89.8 MB
超过目标单文件上限：0
未配对 zip/prop：0
目标端显示：750+
```

### 安全门一：真实目标缺失上传

```text
成员：1 个 .zip + 1 个 .prop
组总量：132 B
验证前目标已有：0 / 2
实际 PUT：132 B
目标强校验 GET：132 B
结果：两个成员均 StrongVerified
异常：0
结束后状态：Paused
```

### 安全门二：GoodSync 既有完整副本 NO-WRITE 接管

为避免公开真实 Zotero storage key，不记录组 basename。

```text
成员：1 个 .zip + 1 个 .prop
组总量：约 13.0 KB
验证前目标已有：2 / 2
实际 PUT：0 B
目标校验 GET：约 13.0 KB
结果：两个成员均 StrongVerified
NO-WRITE：生效
结束后状态：Paused
```

### 第一轮短时长期迁移

用户启用长期迁移后运行一小段并手动暂停。暂停后的实机状态：

```text
状态：Paused
上传估算：约 1.5 MB / 1.00 GB
下载估算：约 13.0 MB / 3.00 GB
安全预留：50.0 MB
已强校验：20
异常：0
记录：20
既有副本：已通过
```

进入长期迁移前已有 4 条强校验记录，因此本轮新增 16 个 StrongVerified 对象，对应至少 8 个完整 Group。上传估算没有明显增长，而下载估算增长到约 13.0 MB，说明该短时阶段主要在验收 GoodSync 既有副本，与设计一致。

## 尚未验证

- 完全退出程序后重新运行，确认 20 条 StrongVerified、配额账本、双安全门和当前断点完整恢复；
- 重启后再次继续，确认不会重复上传已经 StrongVerified 的对象；
- Windows 11 登录自启动、关闭窗口缩托盘、睡眠唤醒和长时间后台运行；
- 真实重置日的 09:00 探测与失败重试行为；
- 坚果云额度真正耗尽时的稳定 HTTP 或响应特征；
- 坚果云超过 750 个目录项后的实际分页协议细节。

## 当前准确断点

```text
双安全门均已真实通过
→ 第一轮短时长期迁移已真实通过
→ 当前 Paused，已强校验 20，异常 0
→ 下一步完全退出 DavBridge
→ 重新运行 v0.1.7，但先不要点击开始
→ 核对 StrongVerified 记录、流量估算、双安全门和断点是否完整恢复
→ 恢复正确后，再继续约 1 分钟并再次暂停
→ 确认不重复上传已完成对象
→ 再验证托盘后台和开机自启动
```

`#381` 继续保持 Draft。在退出与重启恢复、后台运行和开机自启动获得实机证据前，不进入 `p103-stable`，不建立正式标签或 Release。
