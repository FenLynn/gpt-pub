# DavBridge 阶段记录

## 正式稳定阶段

当前正式版本：**DavBridge v0.4.0**。

正式标签：`p103-v0.4.0`。

正式 Release commit：

```text
94aa30fe488235b1a15065d54e6cf3b8c94fef47
```

## 当前实验阶段

当前候选：**v0.4.15**。

分支：`p103-exp`。

最后完整验证代码 head：

```text
92f3a05a8b351aab357250899c7de0ecb82ce760
```

P103 CI run：`34736655980`，结果 `success`。

候选 Artifact：`DavBridge-v0.4.15-win-x64`，Artifact ID `10311605517`。

## 本阶段结果

本轮只收口状态表达和最近活动，不改变迁移安全链。

```text
上传字节进度 → 只显示为“上传进度”
上传完成 → 可继续显示强校验 / 安全暂停等状态
暂停状态 → 中央、左下角、按钮各司其职
活动记录 → 合并同一状态转换的重复事件
原生模态 → Web UI 背景降权
```

## 安全边界

没有修改 DavBridge.Core、WebDAV、StrongVerified、WriteUnknown、quota/Cycle、Reconciliation 或 DELETE。

## 提升规则

未经用户真实机器验收，不得提升到 `p103-stable` 或 `main`。
