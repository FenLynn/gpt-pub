# DavBridge 阶段记录

## 正式稳定阶段

当前正式版本：**DavBridge v0.4.0**。

正式标签：`p103-v0.4.0`。

正式 Release commit：

```text
94aa30fe488235b1a15065d54e6cf3b8c94fef47
```

## 当前实验阶段

当前候选：**v0.4.11**。

分支：`p103-exp`。

最后完成完整 CI 的代码 head：

```text
3c9de7086438f5e8bff03d5ec71a5ac89befccae
```

P103 CI run：`34704232146`，结果 `success`。

候选 Artifact：`DavBridge-v0.4.11-win-x64`，Artifact ID `10301252775`。

EXE SHA256：`54414f66e694a612f964dfad0e327864b06c96e28395affa90eab83d9aae527c`。

Artifact ZIP SHA256：`7713397e02e7cfd558873b0bd7be622811640fd113b492ddf66a9eb8c260fa16`。

## v0.4.11 阶段目标与结果

这一阶段重点修正首页路线语义和当前任务的信息架构：

- 路线箭头左右保留等距空白，并在箭头右侧恢复短线段。
- 两端名称采用各自品牌核心色。
- 覆盖和流量图标重新居中。
- 当前任务对象、状态、进度、动作四种信息重新分层。
- 状态与动作彻底分离，动作按钮不再承担状态显示。
- 点击动作时状态先立即进入过渡态，然后以 C# snapshot 为最终事实。

## 安全边界

v0.4.11 没有修改 `DavBridge.Core`、WebDAV、StrongVerified、SourceChanged、WriteUnknown、HTTP 412、quota/Cycle、Reconciliation、回收站状态机或 DELETE 安全链。

## 自动验证

准确代码 head `3c9de708...` 已通过完整 P103 CI。1100×825 运行态总览和转移页预览均已人工检查。

## 提升规则

未经用户真实机器验收，不得把 v0.4.11 提升到 `p103-stable` 或 `main`。
