# DavBridge 阶段记录

## 正式发布阶段

当前正式 GitHub Release 仍为 **DavBridge v0.4.0**。

正式标签：`p103-v0.4.0`。

正式 Release commit：

```text
94aa30fe488235b1a15065d54e6cf3b8c94fef47
```

## v0.4.16 稳定准入阶段

当前版本：**v0.4.16**。

用户已完成 v0.4.15 实机验收，并授权 v0.4.16 在可靠性封板通过后进入 `p103-stable` 和 `main`。

第一轮完整验证代码 head：

```text
35015c8581cb537ae98813eb5f2efe0458525487
```

P103 CI run：`34743274402`，结果 `success`。

候选 Artifact：`DavBridge-v0.4.16-win-x64`，Artifact ID `10313388007`。

## 可靠性封板结果

Core Smoke 从 14 项扩展为 19 项，新增连续暂停/恢复、源端网络失败恢复、上传后强校验网络失败恢复且不得重复 PUT、零字节对象、quota 精确边界五类回归。

既有 native self-test 继续验证 config/state/reconcile/product-experience 备份恢复、runtime session、background wake 与窗口迁移。

本轮不改变 UI、WebDAV 传输协议、StrongVerified、WriteUnknown、quota/Cycle、Reconciliation 或 DELETE 安全链。

## 提升路径

固定按：

```text
p103-exp
→ PR: p103-exp → p103-stable
→ 当前 PR 准确 head 完整 CI
→ merge
→ PR: p103-stable → main
→ 当前 PR 准确 head 完整 CI
→ merge
→ main 回流 p103-stable / p103-exp
```

stable/main 合并不等于正式 Release。本轮没有 tag/Release 授权。
