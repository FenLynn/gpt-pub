# DavBridge 阶段记录

## 正式发布阶段

当前正式 GitHub Release：**DavBridge v0.4.0**。

## 稳定主线阶段

当前 stable/main 基线：**DavBridge v0.4.16**。

```text
main         73aefcf04570180bb9526a43cf805aff1e7673b3
p103-stable  73aefcf04570180bb9526a43cf805aff1e7673b3
```

## 当前实验阶段

当前候选：**v0.4.19**。

最后完整验证代码 head：

```text
723c77948e4a04cbb533b14d1236f99e9a37c801
```

P103 CI run：`34753217596`，五个 jobs 全部 success。

Windows candidate Artifact ID：`10316692654`。

## 本阶段结果

v0.4.19 将 v0.4.18 基于最近活动列表的健康统计改为持久匿名事件账本。

`product-experience.json` schema 2 只新增匿名时间戳 + 类别事件，不保存真实文件名、路径、URL、凭据、SHA 或核心迁移账本。

旧 schema 1 会一次性安全升级。前 24 小时明确处于健康账本建立期，之后近 24 小时统计不再受最近活动 40 条限制。

native self-test 已验证旧 sidecar 升级、保存、重载、再次升级不重复计数。

## 安全边界

不修改 DavBridge.Core、WebDAV、StrongVerified、quota/Cycle、人工暂停、Reconciliation 或 DELETE。

## 提升规则

v0.4.19 当前只在 `p103-exp`。未经用户明确授权，不提升 stable/main，不创建 tag 或 Release。
