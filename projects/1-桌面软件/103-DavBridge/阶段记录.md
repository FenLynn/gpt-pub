# DavBridge 阶段记录

## 当前基线

P103 DavBridge 当前处于首次开发和真实服务验证阶段，已经形成 **v0.1.4 自动候选**，并完成了两端真实 WebDAV 认证、目录访问和迁移就绪扫描，但尚未完成首个真实 Zotero Group 的端到端强校验，因此仍不是正式版本。

- 当前开发分支：`p103-exp`
- 稳定候选分支：`p103-stable`
- 候选提升 PR：`#381`，Draft，`p103-exp → p103-stable`
- 正式版本：无
- 正式标签：无
- Release：无

PR 和 Actions 是当前候选准确 head SHA、CI 与 Artifact 的事实源。文档不复制一个会随下一次提交立即过期的候选 SHA。

## v0.1.4 当前设计基线

- Windows x64，C# / .NET 8 / WinForms；
- framework-dependent 单文件发布，保持 Runtime 轻量；
- InfiniCLOUD 正式产品代码只读，坚果云为唯一目标写端；
- `.zip` 与 `.prop` 成组处理；
- PUT 成功后必须针对准确资源确认，并从坚果云重新 GET 与源端做 SHA-256 强校验；
- GoodSync 或其他客户端已经存在于目标端的文件允许经过 GET 与 SHA-256 后安全接管，完全一致时不重复 PUT；
- 既有目标与源端不同且无法证明是 DavBridge 自己写入的旧版本时必须进入冲突，不自动覆盖；
- 普通周期预留 50 MB，重置前 24 h 可进入冲刺；
- 上传 1 GB 与下载 3 GB 都进入安全额度模型，人工校准值与 DavBridge 后续保守计量叠加；
- 已有目标文件的接管校验计入下载账本，不消耗上传账本；
- 组级上传预判只计算目标端缺失成员，已有成员先校验；每个实际 PUT 前再次执行上传额度安全检查；
- 登录后可以自动启动和托盘静默运行；
- 首次仅保存连接信息不会自动开始迁移，长期迁移必须经过用户显式启用；
- 首次目标目录允许存在既有 GoodSync 文件；
- 暂停状态持久化，重新登录 Windows 不得擅自恢复用户已暂停的迁移；
- 设置页从 v0.1.4 起直接加载当前 DPAPI 解密后的已保存密码，默认按真实长度显示为 `*`，眼睛按钮可临时显示明文；
- 坚果云目录列举返回达到 750 项时，界面只表述为 `750+` 或“本次列举达到上限”，不把它当作目标总数；
- 完整源码统一以 `代码/DavBridge.sln` 恢复，当前开发期源码事实源为远端 `p103-exp`；
- 最终 `Content Complete` 与 `Exact Mirror` 分开判定。

## 自动验证证据

当前自动候选已覆盖并验证：

- P103 PR 范围门禁；
- 13 项以上核心 smoke，包括正常强校验、PUT 假成功、源端变化、既有目标一致接管、既有目标不一致冲突、下载额度保护、崩溃恢复、部分 Group 恢复、单文件上限和大清单；
- 6000 个 Zotero Attachment Group / 12000 个 WebDAV 对象的大清单分组；
- Windows x64 .NET 8 restore 与 publish；
- framework-dependent 单 EXE Runtime 边界检查；
- 隔离 `%APPDATA%` / `%LOCALAPPDATA%` 的 Windows 自检；
- 候选 EXE SHA-256 生成；
- GitHub Actions Windows Artifact 生成。

## 已获得的真实服务证据

2026-08-12，用户 Windows 11 已取得以下真实验证结果。

### 连接诊断

- InfiniCLOUD 使用 Connection ID/User ID + Apps Password 成功认证；
- InfiniCLOUD 源 `/zotero` 目录真实 PROPFIND 成功，目录响应包含 6,930 个可见条目；
- 坚果云使用注册邮箱 + 第三方应用密码成功认证；
- 坚果云 WebDAV 根目录真实 PROPFIND 成功，本次响应包含 8 个可见对象；
- 坚果云 `/zotero` 真实 PROPFIND 成功，本次响应正好包含 750 个可见对象；
- 这验证了真实账户中确实会触发 750 项单次列举边界，但尚未证明 751 项以后的具体分页协议。

### 迁移就绪扫描

v0.1.4 实测：

```text
源端普通文件对象：6,929
Zotero 逻辑组：3,465
源端总量：9.97 GB
最大文件：89.8 MB
超过目标单文件上限：0
未配对 zip/prop：0
目标端本次可见：750+
```

连接诊断的 6,930 个可见条目与就绪扫描的 6,929 个普通文件对象并不冲突。就绪扫描明确过滤 collection，只把普通文件纳入迁移对象。

由此当前真实库不存在 500 MB 单文件阻塞，也没有发现 `.zip/.prop` 配对异常。目标端完整总数仍未知，单文件迁移继续依靠准确资源路径，不依赖整目录列表完整性。

## 尚未验证

- 真实 GoodSync 既有文件与 InfiniCLOUD 源文件的首次 SHA-256 接管校验；
- 真实坚果云对准确文件 URL 的 Depth:0 行为；
- 首个真实小对象或 Zotero Group 的 PUT、重新 GET 与 SHA-256 闭环；
- 坚果云额度耗尽时的稳定 HTTP 或响应特征；
- 坚果云超过 750 个目录项后的实际分页协议细节；
- 首个真实 Zotero `.zip + .prop` Group 的端到端强校验；
- Windows 11 登录自启动、睡眠唤醒和长时间后台运行。

上述内容在获得真实证据之前不得记录为“已完成”。`#381` 在真实首组闭环前保持 Draft，不进入 `p103-stable`，不建立正式标签或 Release。
