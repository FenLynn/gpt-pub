# DavBridge 阶段记录

## 当前基线

P103 DavBridge 当前处于首次开发和真实服务验证阶段，已推进到 **v0.1.7 GoodSync 既有副本验证候选**。两端真实 WebDAV 认证、目录访问、迁移就绪扫描以及首个真实 Zotero `.zip + .prop` Group 的 PUT / Depth:0 / GET / SHA-256 强校验闭环均已通过，但仍不是正式版本。

- 当前开发分支：`p103-exp`
- 稳定候选分支：`p103-stable`
- 候选提升 PR：`#381`，Draft，`p103-exp → p103-stable`
- 正式版本：无
- 正式标签：无
- Release：无

PR 和 Actions 是候选准确 head SHA、CI 与 Artifact 的事实源。

## v0.1.7 当前设计基线

- Windows x64，C# / .NET 8 / WinForms，framework-dependent 单文件发布；
- InfiniCLOUD 正式产品代码只读，坚果云为唯一目标写端；
- `.zip` 与 `.prop` 成组处理；
- PUT 成功后必须针对准确资源执行 `PROPFIND Depth:0`，随后从坚果云重新 GET，并与源端做 SHA-256 强校验；
- GoodSync 或其他客户端已经存在于目标端的文件允许经过 GET 与 SHA-256 后安全接管，完全一致时不重复 PUT；
- 既有目标与源端不同且无法证明是 DavBridge 自己写入的旧版本时进入冲突，不自动覆盖；
- v0.1.7 新增独立 **既有副本验证** 安全门。该模式自动从坚果云本次可见列表中选择一个 zip 和 prop 都已经存在、尚未验证且较小的完整 Group；
- 既有副本验证套用代码级 `NO-WRITE` 目标包装器，`PutFileAsync` 直接禁止。目标缺失、内容不一致或状态异常时只能停止，不能上传或覆盖；
- 既有副本验证通过条件为：两个成员均 `StrongVerified`，且本次上传增量必须严格等于 `0 B`；
- `MigrationState.ExistingReplicaValidationPassed` 持久化这一真实验证结果；
- 长期迁移现在有两个代码级安全门：至少一个真实上传 Group 完成强校验，以及至少一个既有 GoodSync Group 在 NO-WRITE 模式下完成无 PUT 接管；任一未通过时“开始 / 继续”均拒绝整库迁移；
- 上传 1 GB、下载 3 GB 都进入安全额度模型，普通阶段为其他服务保留 50 MB；
- 流量允许人工校准，DavBridge 在校准点以后保守累计真实 PUT 与目标验证 GET；
- 坚果云重置值在产品语义中只保存“日期”，不保存或显示未经用户提供的具体时刻；
- 到重置日期当天 09:00 以前不清零旧周期账本；09:00 以后使用最小仍需真实 PUT 的完整 Group 做新周期探测；
- 新周期探测必须真实完成 PUT、准确目标确认、目标重新 GET 和 SHA-256，只有完整 Group 强校验通过后才确认新周期；失败则旧账本保持不变并低频重试；
- 09:00 是 DavBridge 的保守探测起点，不宣称为坚果云官方重置时间；
- 登录后可以自动启动并托盘静默运行，暂停状态持久化；
- 已保存密码按真实长度显示为 `*`，眼睛按钮可临时显示明文；
- 坚果云目录列举达到 750 项时只表述为 `750+` 或“本次列举达到上限”；
- 完整源码统一以 `代码/DavBridge.sln` 恢复，开发期源码事实源为远端 `p103-exp`；
- 最终 `Content Complete` 与 `Exact Mirror` 分开判定。

## 自动验证证据

v0.1.7 自动候选要求覆盖：

- P103 PR 范围门禁；
- `DavBridge.sln` 三工程成员完整性；
- 既有核心 smoke，包括正常强校验、PUT 假成功、源端变化、GoodSync 一致接管、目标不一致冲突、下载额度保护、崩溃恢复、部分 Group 恢复、单文件上限和大清单；
- 6000 个 Zotero Attachment Group / 12000 个 WebDAV 对象的大清单；
- reset schedule smoke，固定验证日期归一化、09:00 前禁止探测、09:00 起允许探测、成功后推进下一周期；
- Windows x64 .NET 8 restore 与 publish；
- framework-dependent 单 EXE Runtime 边界检查；
- 隔离 `%APPDATA%` / `%LOCALAPPDATA%` 的 Windows 自检；
- 候选 EXE SHA-256 与 GitHub Actions Artifact。

## 已获得的真实服务证据

2026-08-12，用户 Windows 11 已取得：

```text
InfiniCLOUD 源目录响应：6,930 个可见条目
坚果云 WebDAV 根目录：8 个可见对象
坚果云 /zotero：本次响应达到 750 项

源端普通文件对象：6,929
Zotero 逻辑组：3,465
源端总量：9.97 GB
最大文件：89.8 MB
超过目标单文件上限：0
未配对 zip/prop：0
目标端显示：750+
```

首个真实小型 Group：

```text
成员：1 个 .zip + 1 个 .prop
组总量：132 B
验证前目标已有：0 / 2
实际 PUT：132 B
目标强校验 GET：132 B
结果：两个成员均 StrongVerified
异常：0
结束后状态：Paused
```

真实链路已经覆盖源端 GET + SHA-256、坚果云 PUT、准确目标 `PROPFIND Depth:0`、目标重新 GET + SHA-256、源目标比较以及传输后源端元数据复查。

v0.1.6 实机界面还确认了：旧状态完整继承，标题栏 `DavBridge v0.1.6` 正常，流量重置只显示 `2026-09-07，当日 09:00 后探测上传`，不再出现未经用户提供的具体时刻。

## 尚未验证

- v0.1.7 “既有副本验证”在真实 GoodSync 文件上的 NO-WRITE 接管闭环；
- 该实测必须确认本次上传增量为 `0 B`，两个成员均 StrongVerified；
- 真实重置日的 09:00 探测与失败重试行为；
- 坚果云额度真正耗尽时的稳定 HTTP 或响应特征；
- 坚果云超过 750 个目录项后的实际分页协议细节；
- Windows 11 登录自启动、睡眠唤醒和长时间后台运行；
- 长期整库迁移在真实非空目标上的持续运行行为。

## 当前准确断点

```text
首个真实 PUT Group 已通过
→ 换用 v0.1.7
→ 保持长期迁移 Paused
→ 点击“既有副本验证”
→ 软件只从本次可见的已有目标中选择一个完整小 Group
→ 确认计划窗口明确写出 NO-WRITE 与上传上限 0 B
→ 执行后确认 zip、prop 均 StrongVerified
→ 确认本次上传严格为 0 B
→ 该安全门通过后，再短时开放长期后台迁移
→ 验证托盘、暂停、重启恢复和开机自启动
```

`#381` 继续保持 Draft。在 GoodSync 既有目标真实接管路径和至少一段长期后台运行获得实机证据前，不进入 `p103-stable`，不建立正式标签或 Release。
