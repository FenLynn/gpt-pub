# DavBridge 阶段记录

## 正式发布阶段

当前正式 GitHub Release：**DavBridge v0.4.0**。

正式标签仍为 `p103-v0.4.0`。

v0.4.26 没有正式 tag 或 GitHub Release 授权。

## 稳定主线阶段

当前 stable/main 源码基线：**DavBridge v0.4.26**。

v0.4.26 首次 main 准入提交：

```text
829def0a9776da47fd432a0831fa666993781356
```

最终候选验证 head：

```text
c9da993f465d1e216d30deb5f5cefd517b67b9ce
```

P103 CI run：`34765050320`，五个 jobs 全部 success，Core Smoke 20/20。

## 准入记录

`p103-exp → p103-stable`：PR #432，已合并。

`p103-stable → main`：PR #433，已合并。

两级 PR 的准确 head CI 均通过，范围只包含 P103 与其专属 CI。

## 当前阶段结果

v0.4.26 完成 v0.4 系列收束。当前 UI 结构、surface 层级、About 信息架构、品牌版本号、生产 UI 所有权门、持久化暂停重启测试和运行健康语义均作为稳定基线冻结。

DavBridge.Core、WebDAV、StrongVerified、quota/Cycle、Reconciliation、回收站和 DELETE 安全链未因最终 UI 收束而降低安全门。

## 后续

v0.4 不再主动扩张功能。

真实运行暴露缺陷时做兼容维护。出现明显的新产品主题时先评估 v0.5。

stable/main 准入不等于正式发布，正式 Release 仍需单独人工授权。
