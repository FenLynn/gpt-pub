# DavBridge 阶段记录

## 正式稳定基线

P103 DavBridge 第一阶段核心迁移链已经完成真实环境验证，并以 **v0.1.7** 固化到 `main`。

当前长期分支：

- `main`：v0.1.7 正式稳定回滚基线
- `p103-stable`：v0.1.7 稳定基线
- `p103-exp`：v0.2.x 长周期实机迁移、UI 和低流量安全事务开发线

## 当前实验阶段

当前实机候选：**v0.2.24**

准确代码 head：`e0cd875d851eec531ae9552ef0b1bf59416ec84a`

P103 CI run：`31842182807`

CI：**success**

Artifact：`DavBridge-v0.2.24-win-x64`

ZIP SHA256：`539393e65bdded1bfb8cd6083254fb817212c6d1aa75482dd3b06f9c53fabd75`

EXE SHA256：`6f6aee0a4e84755812ab1bb0eced53ae850d7d23c3af5b3ff919540d24fc18ac`

## 已验证不变的核心链

当前 v0.2.24 继续沿用第一阶段已建立并经回归覆盖的安全语义：

- InfiniCLOUD 源端只读；
- Zotero `.zip + .prop` 成组；
- 条件 PUT；
- 目标准确存在性确认；
- 目标重新 GET；
- SHA256 强校验；
- GoodSync 等既有目标一致时 NO-WRITE 接管；
- 不一致未知目标进入 Conflict；
- WriteUnknown reconciliation；
- 412 协调；
- SourceChanged；
- 上传和下载配额；
- 暂停、断点、托盘和恢复；
- 完成前最终源 Manifest 门。

v0.2.24 没有迁移 `config.json`、`state.json`、`state.json.bak` 或 `secrets.dat`，SchemaVersion 仍为 1。

## v0.2.20 至 v0.2.24 阶段演进

- v0.2.20：额度等待期加入既有副本低流量维护，实机发现坚果云目录单次可见约 750 项，目录 LIST 不适合大库全覆盖。
- v0.2.21：改成从源端未验证组出发，对目标准确路径直接探测，绕过目录窗口。
- v0.2.22：额外既有副本校核改成用户主动启动，取消 24 / 48 组手动扫描上限，严格 NO-WRITE。
- v0.2.23：将正常转移和手动校核拆成两个独立 UI 视图，实机解决同一进度条竞争写入导致的闪烁。
- v0.2.24：全面精修两页 UI，统一低饱和色彩与流程控件，补充校核百分比、结果摘要和不可用原因，同时减少旧绘制叠层。

## v0.2.24 自动验证

最终准确 run `31842182807` 通过：

- scope；
- Core Smoke；
- WaitQuota 手动扫描和 PUT 0 回归；
- Windows x64 单 EXE publish；
- Runtime boundary；
- Windows 隔离 self-test；
- 五组窗口 / DPI 场景；
- 转移页和校核页主动切换布局测试；
- Artifact 与 SHA256。

## 当前待实机验证

v0.2.24 尚未提升到 stable。当前只需要验证：

1. 新配色和整体 UI 是否达到收口标准。
2. 转移页节点流程、进度条、当前周期和按钮是否自然。
3. 校核页节点流程、`x / N · %`、运行点和完成摘要是否正确。
4. 切换活动页不影响后台校核运行。
5. WaitQuota 转移页保持稳定静态显示。
6. 下载记账、StrongVerified 和上传 0 B 语义保持。
7. 窄窗口、高 DPI、托盘、退出和重启没有回归。

## 长期仍待真实环境观察

- Windows 登录自启动长期行为；
- 睡眠与唤醒；
- 真实重置日 09:00 探测；
- 坚果云额度真正耗尽时的稳定响应；
- 750 项以后分页协议细节；
- 最终全库 Content Complete 与 Exact Mirror 验收。

## 准确断点

`main / p103-stable = v0.1.7`，保持不动。

`p103-exp` 当前先实机验证 v0.2.24。通过后再决定清理旧 UI generation，并评估 `p103-exp → p103-stable`，不得提前进入 `main`。
