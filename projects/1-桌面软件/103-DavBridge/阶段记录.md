# DavBridge 阶段记录

## 正式稳定阶段

当前正式版本：**DavBridge v0.4.0**。

正式标签：`p103-v0.4.0`。

正式 Release commit：

```text
94aa30fe488235b1a15065d54e6cf3b8c94fef47
```

## 当前实验阶段

当前候选：**v0.4.14**。

分支：`p103-exp`。

最后完整验证代码 head：

```text
378bb37707a3a5f1d685427edb77294d916acdcc
```

P103 CI run：`34726420112`，结果 `success`。

候选 Artifact：`DavBridge-v0.4.14-win-x64`，Artifact ID `10307014716`。

## 本阶段结果

本阶段只修复重复暂停与主操作按钮交互。

```text
暂停
→ 安全收尾
→ 已暂停 / 继续
→ 点击继续后 UI 命令立即释放
→ 后台 pass 运行
→ 任何 active-run 阶段再次提供暂停
→ 可再次安全暂停
```

按钮槽位始终保留。暂停黄色，继续/重试绿色，不可操作灰色。

## 安全边界

v0.4.13 的 cooperative safe pause 保持不变。本轮没有修改 DavBridge.Core、WebDAV、StrongVerified、WriteUnknown、quota/Cycle、Reconciliation 或 DELETE。

## 提升规则

未经用户真实机器连续暂停验收，不得提升到 `p103-stable` 或 `main`。
