# P106｜MediaIndex

MediaIndex 是面向大规模照片与视频库存的只读内容检索工具。核心目标不是传统“重复文件清理”，而是回答：

> 给定一张图片、一个视频或一批媒体，库存中是否已经存在同源素材；如果存在，准确返回所在路径与匹配置信度。

## 当前阶段

**Phase 0：算法与索引路线验证。**

当前不进入正式产品开发，不做自动删除、移动、重命名或整理用户文件。优先证明：

1. 图片同源识别在压缩、缩放、转格式、水印、裁剪等变化下的召回能力。
2. 强裁剪和局部变化下的精确验证能力。
3. 难负样本下的误报边界。
4. 数十万级库存的索引体积、候选召回与查询延迟。
5. 图片路线稳定后，再进入视频转码与片段检索验证。

## 项目身份

- 预留编号：`P106`
- 预留路径：`projects/1-桌面软件/106-MediaIndex/`
- 日常开发：`p106-exp`
- 稳定候选：`p106-stable`
- 正式主线：`main`
- 固定流转：`main → p106-exp → p106-stable → main`

> 当前按用户要求仅建立 P106 自有目录与两条长期分支，不修改 `/目录.md` 或任何公共文件。因此按照仓库 A-04 规则，P106 在共享目录索引中的正式登记仍待后续统一仓库任务完成。当前编号与路径视为已预留，不得被其他项目占用。

## 当前算法假设

当前仅作为待验证架构，不视为最终结论：

```text
Query
  ↓
Exact hash
  ↓
全局 / 多区域 perceptual hash
  ↓
候选集
  ↓
局部特征 + 几何验证
  ↓
Confirmed / Probable / Similar / Not found
```

AI embedding 暂不作为最终同源判据。是否引入，只由真实难例和规模测试决定。

## 重要边界

- 默认只读现有媒体，不接管目录结构。
- 索引数据库与原媒体分离。
- 不以单一相似度直接删除或覆盖文件。
- 合成测试只能用于路线和阈值探索，不能替代真实照片域验证。
- benchmark 必须记录数据集、参数、代码版本、结果与失败案例。
- 私人照片、视频、真实用户路径、未脱敏日志不得提交到公开仓库。

## 入口

接续开发依次读取：

1. `/GPT_RULES.md`
2. `/目录.md`
3. `projects/1-桌面软件/开发约束.md`
4. 本目录 `开发约束.md`
5. `README.md`
6. `阶段记录.md`
7. `工作记录.md`
8. `docs/algorithm-validation.md`
9. `docs/benchmark-plan.md`
10. `设计与演进.md`
11. 实时核对 `main / p106-stable / p106-exp`

跨对话恢复入口：`HANDOFF.md`。
