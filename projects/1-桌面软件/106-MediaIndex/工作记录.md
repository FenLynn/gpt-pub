# P106｜MediaIndex 工作记录

> 历史事实见 `阶段记录.md`、`docs/algorithm-validation.md` 与 `docs/video-validation.md`。

## 当前工作主题

**Phase 0 的 synthetic / programmatic 验证已经停止扩张，真实域验收工具链已经准备完成。**

## 项目隔离状态

继续严格遵守：

- 只修改 `projects/1-桌面软件/106-MediaIndex/`。
- `p106-exp` 承载全部开发与验证。
- 不修改 `p106-stable`。
- 不修改 `main`。
- 不修改 `/目录.md`。
- 不修改公共 CI。
- 不修改其他项目。

## 图片状态

已完成 R001 至 R016。

A4 真实域 runner 已新增：

- `experiments/a004_real_image_acceptance_runner.py`

当前功能：

- SHA-256 exact hit。
- selected-region pHash candidate retrieval。
- 默认 Top-50。
- SIFT + RANSAC + NCC baseline verifier。
- positive candidate recall / Top-1 / Confirmed recall。
- hard-negative false Confirmed。
- Query latency。
- JPG / PNG / WebP / TIFF / HEIC / HEIF 等常见图片输入。

## 视频状态

已完成 V001 至 V014 的合成 / 程序样例验证。

V011 baseline runner 已强化：

- 支持相对 manifest 路径。
- 支持 expected_source 留空的 unrelated negatives。
- positive Top-1 与 negative exploratory strong-match 分开统计。
- 输出 offset、scale、margin、median Hamming。

## 一键本机验收工具

新增：

```text
experiments/
  install_acceptance_env.bat
  prepare_real_domain_acceptance.bat
  run_real_domain_acceptance.bat
  image_manifest_template.csv
  video_manifest_template.csv
  acceptance_summary.py
```

工作流：

```text
install
→ prepare private workspace
→ place private media
→ fill manifests
→ run
→ only share JSON results
```

真实媒体不需要上传仓库，也不需要提交给 ChatGPT。

## Acceptance GUI

为进一步降低用户操作，新增 Windows Acceptance 壳：

```text
src/MediaIndex.Acceptance/
build/BuildAcceptancePortable.ps1
build_acceptance.bat
start_acceptance.bat
experiments/acceptance_worker.py
docs/acceptance-app.md
```

当前 `v0.0.1-dev` 设计：

- WinForms .NET 8 自包含 host。
- Python baseline 通过 PyInstaller 打包成私有 worker EXE。
- portable 运行时不要求用户安装 Python 或 .NET Runtime。
- GUI 自动生成 manifest。
- Query 支持拖放。
- 每条 Query 必须显式标注，防止漏填被误当 hard negative。
- 视频 source start 是可选项。
- 一键 A4 + V011。
- 一键导出匿名 JSON。

用户统一入口改为 `start_acceptance.bat`。portable 已存在时直接启动，不存在时自动调用 build 并启动。首次 build 仍为全自动。如果本机没有 .NET 8 SDK，build 脚本会下载私有 SDK；用户已有 Python 3 时会自动创建独立 build venv。

## 当前唯一下一步

执行 **A4 + V011 用户真实域验收**。

建议实际准备：

- 100 至 300 张真实库存图片。
- 30 至 50 个图片正样本 Query。
- 20 至 50 个图片 hard negatives。
- 20 至 50 个真实库存视频。
- 视频总时长最好至少 2 至 5 小时。
- 30 个以上真实视频 Query。

如果用户暂时不方便准备全部，可以先用更小的 30 至 50 图 + 10 至 20 视频做 smoke acceptance，然后再扩到正式规模。

真实域通过后，冻结 Phase 0 并开始 MVP。


## 2026-09-16｜v0.0.2 自动验收真实反馈

用户在 Round 1 的 20 图库存上直接运行“自动验收”，无需手工标注。

40 个自动正样本：

- Top-50：100%
- Top-1：85%
- Confirmed：57.5%

6 个 Top-1 错误全部集中于 Crop30 / 非对称 Crop，且都出现在没有 candidate 被高置信确认的低置信路径。

已据此修改图片 rerank：

```text
有 Confirmed
→ verifier 排序

无 Confirmed
→ multi-region pHash fallback
→ verifier 仅 tie-break
```

同时修正 GUI 摘要，下一版显示：

```text
Top1
Top50
Confirmed
负样本是否实际测试
```

自动 smoke 也加入 synthetic unrelated negatives，避免“负样本 0 条却显示误确认 0”的误导。

版本推进到 v0.0.3-dev。

## 2026-09-16｜从 Acceptance 转入正式 MediaIndex Product Preview

用户要求不要继续让其充当测试员，而是把 hard negative 与正式产品入口一次推进。

本轮完成：

### Hard negative

新增：

```text
experiments/ci_hard_negative_suite.py
```

固定 known hard-negative 类型：

- same scene / different camera with parallax
- same poster template / different center content
- repeated texture / phase change
- low-texture look-alike

该套件接入 P106 Windows CI。若出现 high-confidence false same-source，则构建失败。

### Persistent MediaIndex core

新增：

```text
experiments/mediaindex_core.py
experiments/ci_mediaindex_core.py
```

当前实现：

- SQLite metadata
- compact region hash matrix
- image ID matrix
- incremental reuse
- lazy SHA-256
- persistent query
- offline-source candidate support
- deep verifier
- template fallback

### 正式主程序

新增：

```text
src/MediaIndex.App/
```

主界面包含：

- 图库索引
- 图片查询
- 自动验收

用户主流程变为：

```text
选择图库
→ 建立/更新索引
→ 拖入图片
→ 查询库存
→ 返回路径
```

Acceptance 从主流程退到诊断/回归工具。

### 当前 Preview 版本

```text
MediaIndex v0.1.0
```

该版本仍属于 p106-exp Product Preview，不是 stable/main 正式 Release。

## 2026-09-16｜MediaIndex v0.1.0 Windows 主程序成功

最终成功 run：`35071827474`。

主程序 artifact：`MediaIndex-v0.1.0-win-x64`。

主 EXE：`235793460` bytes。

SHA-256：

```text
bb6ddb309ad329dba009964ed4989cc33e7cfef9d3094ef6d2b9d435e79768ae
```

本轮 hard-negative gate 曾真实抓到同模板不同主体的误 Confirmed。最终通过新增 block NCC 与低尾局部一致性约束修复，没有通过降低测试难度规避。

当前立即进入下一工程任务：persistent local-feature Lane B。

## 2026-09-16｜MediaIndex v0.2.0 完整通过

成功 CI run：`35099480358`。

核心结果：

- persistent Lane B 已进入正式主索引。
- 12 个强相关背景、小局部裁剪 stress 全部 end-to-end Top1 正确。
- local Top20 12/12。
- 2 个 pHash Top20 完全漏掉的真源由 Lane B 找回。
- hard negative 继续 0 false Confirmed。
- hard negative 继续 0 false Probable geometry。
- 主 EXE 与 embedded worker 均通过 self-test。

最终主程序：

```text
MediaIndex v0.2.0
EXE bytes: 235809844
SHA-256: 9af2c3ca43344427c8e751ae1e0a49895ae50c50cdeeb4d40dfbf20ea121f64e
```

重要修复：template fallback 不再分别独立 resize source/query，而是用统一 common scale 保持相对尺寸关系。


## 2026-09-16｜R017 persistent Lane B scale

完成 v0.2.0 参数下 10k / 100k / 500k synthetic inverted-index scale。

500k 结果：

```text
postings: 31.497M
raw postings: 120.15 MiB
raw local-word truth payload: ~61.0 MiB
mmap median query: 1.30 ms
mmap P95: 3.42 ms
median postings touched: 14,479
```

1 个 target-specific word 存活时 Top20/Top50 100/100。

0 个 target-specific word 时 Top20 11/100、Top50 37/100，说明 candidate budget 不能替代缺失的信息证据。


## 2026-09-16｜R018 base + delta overlay

验证 500k base 下不必为少量文件变化全量重建 Lane B。

同一 synthetic workload：

```text
base-only median: 0.514 ms

0.1% delta:
  raw 0.108 MiB
  median 0.719 ms

1% delta:
  raw 1.082 MiB
  median 0.795 ms

5% delta:
  raw 5.407 MiB
  median 0.878 ms
```

每个档位：
- unchanged Top20 60/60
- updated Top20 60/60
- new Top20 60/60
- deleted old ID absent 60/60

方向：进入 crash-safe immutable generation + delta overlay 设计。
