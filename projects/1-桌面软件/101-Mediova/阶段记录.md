# P101 Mediova 阶段记录

## 当前正式基线

- 当前正式版本：`Mediova v4.1.0`
- 正式标签与 Release：`p101-v4.1.0`
- 正式源提交：`27435598978ec8e16944742ce0de7c56f489dded`
- 正式源文件树：`8021b5621eb0db7a0a054e3e41d07cec35ebc593`
- 正式路径：`projects/1-桌面软件/101-Mediova/`
- 日常分支：`p101-exp`
- 稳定分支：`p101-stable`
- 正式主线：`main`

v4.1.0 已完成产品实现、记录体系收敛、长期 CI 加固、稳定候选、正式主线准入、不可移动标签和完整 Runtime Release。后续版本必须继续遵循 `p101-exp → p101-stable → main → 标签与 Release`，不得把本阶段的一次性发布设施恢复为长期工作流。

## 当前仍成立的架构结论

### 本地媒体结果闭环

- 软件不修改或删除源媒体。
- 每个输入必须形成明确结果。
- 只有真实存在并通过最低完整性检查的输出才能记为成功。
- 硬件编码失败时安全回退 CPU，不能让整个批次静默失效。

### 轻量透明 Runtime / 独立 Data

```text
Mediova/
├── Mediova.exe
├── Components/FFmpeg/bin/
├── runtime-manifest.json
├── THIRD_PARTY_NOTICES.md
└── README.txt
```

- `Mediova.exe` 是唯一直接主程序。
- FFmpeg/FFprobe 和相关 DLL 属于 Runtime，不作为 Data 存入 AppData。
- `%APPDATA%\Mediova` 保存配置、历史、会话和备份。
- `%LOCALAPPDATA%\Mediova` 保存缓存、临时文件、缩略图、本机状态、日志和崩溃信息。
- Runtime 可以公开、复制、覆盖、校验和删除后重建；Release 不包含私人 Data。
- 旧 AppData 组件只在 Runtime 缺失时保守复制，不覆盖新文件、不删除旧目录。

### v4.1.0 界面规则

- 主界面统一底色、轻边线和控件五态，顶部工具栏保持单行。
- 输出区域统一为“输出母目录”，支持编辑、最近目录、浏览和打开当前目录。
- 进度条与体积条采用单层绘制，文字在完整条形区域内居中。
- 体积条按输入与输出体积总和绘制真实比例；±10% 和“变大但最终小于 15 MB”为黄色，明显压缩为分级绿色，明显增长为分级红色。

设计原因和历史取舍以 `软件开发总结.md` 为准；长期不可破坏规则以 `开发约束.md` 为准。

## 正式准入与发布证据

- 正式主线 PR：`#83`
- 正式主线 CI：`30727129758`
- 正式 Artifact：`P101-Mediova-v4.1.0-CI`
- Artifact ID：`8826745059`
- Artifact 外层摘要：`sha256:66f99e97b432349c5f6a3d27f0926edfa056fa3c9b65d8c6539bc498deef8e7a`
- Release 补全运行：`30728888940`
- 标签与正式源提交比较：`identical`

准入覆盖：

- P101 自动范围门禁；
- Linux 源码清单、Go 全测、竞态、`go vet` 和 Windows 交叉构建；
- Windows 隔离 Data 全测、竞态和 `go vet`；
- 真实 FFmpeg/FFprobe；
- 完整 Runtime 构建与展开复核；
- Runtime manifest 逐文件大小与 SHA-256；
- 私人 Data 泄漏扫描；
- 正式 EXE 原生自检 57/57。

正式资产：

```text
Mediova.exe
大小：3,761,152 bytes
SHA-256：f4a084b365b50712dbffb45f19b4098782212caf25dcc9d61eb8defe8a894d81

Mediova-v4.1.0-Runtime.zip
大小：116,138,263 bytes
SHA-256：df4ec8c4db0b84b501faefebf81da84b1463fe69f8c53b234ad24d9e882f4f04
```

- Runtime manifest：通过
- 私人 Data 扫描：通过
- 原生自检：57/57
- Authenticode：`NotSigned`

## 已知限制与剩余风险

- GitHub Windows Runner、自动几何测试和原生自检不能完全替代 Windows 10/11 真实桌面视觉体验。
- 拖放、托盘、多显示器、特殊 DPI、显卡驱动、PotPlayer、文件时间和特殊媒体继续标记为真实环境“待验证”。
- v4.1.0 首次部署必须使用完整 Runtime ZIP，不能只复制 `Mediova.exe`。
- 正式资产当前未签名；不得在文档或界面中暗示已完成代码签名。

上述风险不得在后续记录中改写为“全部实机验证完成”。当前工作和后续候选以 `工作记录.md` 为准；用户可见变化以 `Mediova_v4.1.0_版本说明.md` 为准。
