# Mediova 工作记录

本文件只回答当前正在做什么、怎样算完成、证据在哪里和尚缺什么。完整历史由 Git、标签、Release 和 `软件开发总结.md` 保存；正式基线由 `阶段记录.md` 保存。

## 当前状态

Mediova v4.5.0 已完成实现、实验准入、稳定候选、正式主线、标签、轻量 Release、真实 Windows 截图复核、62 项原生自检和 2500 项会话恢复压力测试。

本轮连续完成四个里程碑：

- v4.2.3：统一 footer 几何，修复视频与图片右下角按钮错位和禁用态残影；
- v4.3.0：增强时间与画面裁剪，加入比例、居中适配、键盘微调和批量应用；
- v4.4.0：扩展图片裁剪，并为 HEIC、HEIF、AVIF 增加 FFmpeg 解码预检；
- v4.5.0：完成版本化会话快照、原子保存、备份回退、文件级恢复和长队列压力门禁。

当前没有正式启动新的产品版本。`p101-exp` 已完成第一轮不改变媒体处理功能、配置或会话格式的自适应稳定性增强，并正在收口第二阶段恢复协议审查。上述工作不改变 v4.5.0 标签、Release 或 `main` / `p101-stable` 正式基线，也不替代后续用户实机验证。

竖屏显示器不列为当前像素级正式验收场景。当前自适应优先级为常见横屏窗口、最大化与还原、100%–175% DPI、多显示器切换和窄窗口降级；竖屏只要求程序不崩溃、不出现阻断操作的严重遮挡。

## 第一轮自适应稳定性增强

### 已实施

- `footerGeometryFor` 在逻辑宽度小于 1040 px 时缩短开始、暂停和停止按钮，继续保持同高、同基线、8 px 固定间距和整体右对齐；
- 状态文字区保留最低 120 px 可读宽度；
- 新增 900–1920 px 连续横向 footer 矩阵，分别覆盖紧凑与非紧凑模式；
- 补充 980、1039、1040、1120、1280、1450 和 1650 px 边界样本；
- 修复低高度横屏下右侧详情框被强制保留 90 px、进而压入底部参数区的问题；
- 当详情区可用高度不足 90 px 时，仅自动收起次要详情框，保留任务参数、应用、默认、预览、裁剪、单独输出和重试等主要操作；空间恢复后详情框自动重新显示；
- 新增 620–1200 px 连续高度矩阵，覆盖紧凑与非紧凑底部区域；
- 增加顶部工具栏、搜索、筛选、四个状态控件、右侧栏和完整参数行的 900–1920 px 连续宽度审计；
- 已同步固定源码 SHA-256 清单。

### 验证证据

- 产品与测试提交：`ff4823527e11a78f6e335c186ef3157c76dbde61`；
- 一次性验证运行：`30784556737`；
- 固定源码 SHA-256：通过；
- Linux Go 全测：通过；
- Linux 全量竞态测试：通过；
- `go vet -unsafeptr=false ./...`：通过；
- Windows amd64 测试程序交叉构建：通过；
- Windows amd64 GUI EXE 交叉构建：通过；
- 生成文件范围门禁：通过；
- footer 连续矩阵和右侧详情连续高度矩阵已在 Go 测试中实际执行；
- 顶部、右栏和完整参数行的 Windows 连续宽度审计已成功编译进 Windows 测试程序，仍需后续 Windows 原生运行与截图复核。

一次性实施脚本和一次性工作流在验证完成后已经从 `p101-exp` 删除，正式产品差异中不保留临时设施。触发 PR 按设计不合并。

### 仍待真实桌面确认

- 900×620、980×620、1039×620、1040×620、1120×720、1280×720、1450×820 和 1650×930 等代表性横屏窗口；
- 100%、125%、150% 和 175% DPI；
- 最大化、还原、拖动改变窗口尺寸和多显示器切换；
- 低高度时详情框收起与重新出现的视觉连续性；
- 顶部搜索、筛选和状态控件在真实字体、主题和系统缩放下的可读性；
- 图片模式、视频模式、搁置模式和禁用状态下 footer 的真实绘制结果。

任何尚未通过真实 Windows 截图或用户桌面验证的视觉结论均保持“待验证”，不得进入正式版本说明或 v4.5.0 Release。

## 第二阶段恢复协议审查

### 已发现问题

v4.5.0 的恢复协议要求完成、跳过和失败结果保持原状态，但原实现先检查源文件是否存在，再判断任务是否已经处于终态。因此，用户在处理完成后移动或删除源文件时，历史上的完成、跳过或失败记录会被错误改写为“源文件缺失失败”，同时丢失原输出路径、失败原因或校验信息。

### 修复原则

- `转换完成`、`已跳过` 和 `失败`属于历史结果，恢复时优先计数并原样保留；
- 不因为源文件后来移动或删除而改写终态结果；
- `准备中`、`队列中`、`转换中`、`已暂停`、`搁置` 和 `已停止`仍需要重新执行，因此继续检查源文件是否存在；
- 非终态任务源文件缺失时，仍明确标记为“源文件缺失”，并清除临时输出、进度、引擎和运行时间；
- 不放宽文件级从 0% 重新处理、源文件保护或结果完整性规则。

### 回归覆盖与证据

- 修复 PR：`#289`；
- 修复分支：`p101-recovery-terminal-preservation`；
- 新增三类终态源文件均已移动时的完整保留测试；
- 新增非终态源文件缺失仍明确失败的反向测试；
- 一次性验证运行：`30784887518`；
- 固定源码 SHA-256、Go 全测、全量竞态、`go vet`、Windows 测试程序交叉构建和 Windows GUI 交叉构建均通过；
- 最终长期 P101 CI 以 PR 当前头提交的同步运行结果为合并门槛。

## 正式证据

- 正式版本与标签：`Mediova v4.5.0` / `p101-v4.5.0`
- 正式产品提交：`878e0463fe7329beb0f027401ae6f8821fe09e51`
- 稳定候选提交：`57bb58b97c459b262f28a0a80957d913b24fc299`
- 最终实现提交：`9c95facabeb964924843494164892564f9da0614`
- 稳定完整准入：`30776072589`
- 正式主线准入：`30776363913`
- 轻量发布运行：`30777890479`
- 原生 self-test：62/62
- 恢复压力测试：2500 项
- 正式 `Mediova.exe` SHA-256：`0de7f28ba734c130fd7ded0b7155aa84d7c22268c365e39b3b9b05539e41fd44`
- 内部完整 Runtime ZIP SHA-256：`dd401303d4c96e8db9b390a78a7ad88d4a44ce0290d28dc8c78623b00a493b1e`
- 用户轻量包：`Mediova-v4.5.0-Light.zip`
- 轻量包大小：`1,712,662 bytes`
- 轻量包 SHA-256：`494b1d99c6420ec9295f44c6a925732d11b09c4937dcf1e08b1dc59f27b77abd`
- 正式 Release 资产：轻量 ZIP 与 SHA256 文件两项
- 四套真实 Windows 截图：视频宽屏、视频紧凑、图片模式、搁置模式
- 截图人工复核：右下角三个操作按钮同高、同基线、固定间距，无重叠与禁用态残影

完整用户可见变化见 `Mediova_v4.5.0_版本说明.md`。

## 持续观察事项

| 工作项 | 状态 | 观察标准 | 当前结论 |
|---|---|---|---|
| 多 DPI 与多显示器 | 实验增强 / 持续观察 | 125%、150%、175% DPI、多显示器切换和最大化/还原不产生重叠或跳位 | 第一轮连续宽高矩阵与交叉构建通过；仍需真实 Windows 原生运行和用户桌面覆盖 |
| 终态结果恢复 | 修复候选 | 完成、跳过和失败结果不因源文件后来移动而被改写 | 专项回归与一次性完整门禁通过；等待长期 P101 CI 和合并 |
| 现代图片输入 | 持续观察 | HEIC、HEIF、AVIF 在不同 FFmpeg 构建中成功或给出明确失败原因 | 已有有界预检和源文件保护；实际支持范围取决于 FFmpeg |
| 长队列异常恢复 | 持续观察 | 数百视频、数千图片、异常退出、主快照损坏和缺失源文件均能形成明确恢复结果 | 2500 项压力测试、旧格式兼容和 `.bak` 回退通过；继续积累真实使用证据 |
| 外部程序与特殊媒体 | 持续观察 | 显卡驱动、PotPlayer、托盘、拖放、复杂轨道、VFR 和特殊文件名无静默失败 | 原生自检覆盖主要路径，真实环境问题按复现条件单独登记 |
| 代码签名 | 暂缓 | 形成稳定证书、发布主体和密钥管理方案后再评估 | 当前正式资产为 `NotSigned`，文档和界面不得暗示已签名 |

## 后续更新计划

### 当前预备工作第二阶段

在不启动正式版本的前提下，继续扩充可自动闭环的稳定性证据：

- 建立受控测试媒体样本，覆盖方向元数据、横竖画面、VFR、多音轨、字幕、特殊文件名、损坏文件和现代图片输入；
- 增加异常退出、主快照损坏、`.bak` 回退、缺失源文件和半成品输出组合；
- 检查 FFmpeg/FFprobe 超时、残留进程、句柄释放和临时文件清理；
- 扩大动态追加、暂停、搁置、删除和恢复交叉发生的长队列测试；
- 只登记能够由自动证据证明的问题，不把尚未实机确认的视觉猜测直接写成修复。

### v4.5.1｜仅限稳定性热修

只有出现明确、可复现且不适合等待大版本的问题时才启动，例如：

- 特定 DPI、主题或窗口尺寸下仍出现控件错位；
- 会话快照在真实异常退出场景中出现恢复遗漏；
- 某类 HEIC/HEIF/AVIF 或复杂媒体产生错误分类或静默失败；
- 托盘、拖放、显卡驱动或外部播放器集成出现阻断性问题。

不得把功能扩张混入 v4.5.1。当前实验自适应增强和恢复协议修复不自动等同于启动 v4.5.1；只有形成明确问题集合、版本范围和正式验收标准后再决定版本归属。

### v4.6.0｜队列可观测性与重试闭环

建议作为下一主版本候选，先做设计和样本验证，再决定是否实施：

- 失败任务按原因筛选、批量重新预检和安全重试；
- 任务历史与当前队列更清楚地区分；
- 可导出精简处理报告，记录输入、输出、参数、引擎、耗时、校验和失败原因；
- 恢复摘要、动态追加、剩余任务和队列健康状态更易理解；
- 不增加复杂账户、数据库或云同步系统。

### v4.7.0｜预览效率与批量编辑

在 v4.6.0 稳定后再评估：

- 更低成本的时间轴缩略图或关键帧导航；
- 裁剪预设管理和更清晰的批量差异提示；
- 大图片与 4K 视频预览的缓存预算、取消和回收策略；
- 保持单一预览系统，不引入第二套渲染架构。

### v5.0.0｜只在真正架构变化时启动

只有现有任务、配置或 Runtime 模型无法承载明确需求时才考虑，例如跨机器可迁移任务包、插件边界或全新的处理图模型。不得仅为“升大版本”重写稳定的 Win32、队列或 FFmpeg 管线。

## 启动下一版本的前置条件

新版本开始前必须写清：

1. 可复现问题或明确用户价值；
2. 不做会造成什么实际损失；
3. 最小实现范围和不做范围；
4. 配置、会话、任务和 Runtime 的兼容边界；
5. 自动测试、真实 Windows 截图、原生自检和用户实机验证标准。

若没有满足这些条件的事项，v4.5.0 保持为当前正式稳定基线。
