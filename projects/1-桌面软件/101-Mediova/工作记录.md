# Mediova 工作记录

本文件只回答当前正在做什么、怎样算完成、证据在哪里和尚缺什么。完整历史由 Git、标签、Release 和 `软件开发总结.md` 保存；正式基线由 `阶段记录.md` 保存。

## 当前目标

Mediova v4.2.0 已正式发布。当前阶段不是继续堆叠新功能，而是收集真实桌面使用反馈、确认高 DPI 与特殊媒体边界，并在形成明确问题后进入下一版本。

下一版本开始前必须先比较 `main`、`p101-stable` 和 `p101-exp`，确认长期分支已经回流到最新正式主线；不得在旧候选分支上直接继续开发。

## v4.2.0 已完成范围

### 输出母目录与目录树

- 视频与图片分别保存输出母目录、最近目录和默认参数。
- 文件夹导入按导入根目录计算相对路径，并在输出母目录下复刻原多级目录。
- 已入队任务冻结输出母目录、输出路径和冲突策略，后续默认变化不影响已有队列。
- 活动队列只锁定当前媒体模式的输出母目录，另一模式仍可准备任务。

### 视频与图片双参数体系

- 视频底部使用分辨率、格式、质量、体积模式和旋转。
- 图片底部使用尺寸、格式、质量、大小限制和旋转。
- 切换媒体模式时，底部、右侧、默认值和输出母目录同步切换。
- 同一活动队列只运行一种媒体，另一模式可继续导入和编辑准备中任务。

### 全局默认与个体编辑

- 底部参数按字段立即应用到同模式准备中任务，并作为新任务模板。
- 已入队、转换中、暂停和搁置任务不受底部默认变化影响。
- 底部只保留“全部恢复默认”。
- 右侧个体参数使用草稿与显式应用；多选混合值只提交用户实际修改的字段。

### 入队冻结与动态追加

- 任务进入队列时冻结转换参数和必要运行快照。
- 转换运行期间可继续导入同类型文件并修改准备中任务。
- 主按钮在存在活动队列时提供“加入队列”，新任务独立完成冲突和磁盘预检后动态追加。
- 动态追加后总进度按真实分母重算，并显示“已完成 x/y”。

### 临时搁置与安全移除

- 队列中、转换中和暂停任务默认锁定。
- 单个锁定任务可进入独立搁置会话修改参数；多个锁定任务只允许批量移除。
- 转换中的任务搁置时安全终止 FFmpeg、清理临时输出、保留并发名额，并在应用或取消后从 0% 重启。
- 从任务列表移除不会删除源文件，也不会误删已经完成的正式输出。

### 进度、体积与状态显示

- “压缩后”和“进度”轨道在准备、队列、转换、暂停、完成、失败和选中状态中保持常驻。
- 选中背景绘制在条形下方，不遮挡轨道、填充或文字。
- 总进度按真实任务总数更新，动态追加时允许百分比下降但同步显示完成数量。
- 状态灯使用 14 px 基线、高对比实色和清晰边线。

## 正式证据

- 正式版本与标签：`Mediova v4.2.0` / `p101-v4.2.0`
- 正式源提交：`c8ea2b904a59aa9416763c3397088fd803d24e15`
- 稳定候选提交：`e46a37cc76b30dccfe8349e322d118adf70f46b8`
- 正式 CI：`30740707877`
- Release 运行：`30740967788`
- 原生自检：61/61
- `Mediova.exe` SHA-256：`1261c158d35d5fd3ae97ebfd36a25e72094d2f5d65f8cc091a331c1955d2a72f`
- 用户轻量包：`Mediova-v4.2.0-Light.zip`，大小 `1,692,644 bytes`，SHA-256 `e96447dee95510acf2e7a059f9c6f5f128c47660af0e561b67021181f81d8224`
- 轻量包补充运行：`30744062665`
- 内部完整 Runtime 验证 SHA-256：`daf2f9e9f527530c170281fe1bc64e16a23b0d6fbb0bf8a1686f7c32a696bacc`

完整用户可见变化和正式资产见 `Mediova_v4.2.0_版本说明.md`；正式架构与历史证据见 `阶段记录.md`。

## 活跃事项

| 工作项 | 状态 | 完成标准 | 当前证据或尚缺内容 |
|---|---|---|---|
| v4.2.0 正式发布与仓库收口 | 已完成 | 正式主线、准确标签、Release、固定 CI、61 项原生自检、四套 UI 截图和一次性工作流清理完成 | 标签 `p101-v4.2.0`；CI `30740707877`；Release 运行 `30740967788`；一次性工作流已从主线删除 |
| 长期分支回流 | 已完成 | `p101-stable` 与 `p101-exp` 在无独有提交的前提下快进到最新 `main` | `main`、`p101-stable`、`p101-exp` 已非强制快进并比较为 `identical` |
| 真实桌面观察 | 待验证 | 在实际 Windows 环境观察高 DPI、多显示器、不同主题、拖放、托盘、显卡驱动、PotPlayer 和特殊媒体 | 自动准入已通过；这些项目不能用 CI 代替真实桌面观察 |
| 下一版本范围 | 待决策 | 收集到可复现问题或明确产品目标后，登记完成标准并从最新 `p101-exp` 开始 | 当前不提前建立空版本或无内容分支 |

## 状态说明

- `待决策`：问题已确认，路线未确定。
- `计划`：路线已确定，尚未开始。
- `进行中`：已经实施，尚未满足完成标准。
- `待验证`：自动验证完成，仍缺真实环境或用户确认。
- `已完成`：实现、完成标准和可追溯证据齐全。
- `暂缓`：当前不推进，并写明重新评估条件。
- `取消`：明确不再实施，并保留原因。

事项只有同时具备实现、完成标准和可追溯证据，才能标记为“已完成”。
