# Mediova 工作记录

本文件只回答当前正在做什么、怎样算完成、证据在哪里和尚缺什么。完整历史由 Git、标签、Release 和 `软件开发总结.md` 保存；正式基线由 `阶段记录.md` 保存。

## 当前状态

Mediova v4.2.2 已完成实现、实验准入、稳定候选、正式主线、标签、轻量 Release、一次性设施清理和长期分支回流。

本轮未扩张新功能，只处理 v4.2.1 实机反馈：DPI 状态灯、视频/图片底部进度和按钮独立性、图片参数宽度、恢复默认图标、压缩条浅灰侧、视频时长列，以及蓝底白字/浅底深字的进度文字。

当前不自动启动下一版本。后续只登记可复现的新问题和真实桌面观察结果；必须先明确复现条件、影响范围和完成标准，才能进入新的开发版本。

## v4.2.2 已完成范围

### 高清状态灯与紧凑按钮

- FFmpeg、GPU、PotPlayer 和并发状态灯改为 DPI 感知字体矢量圆点。
- “全部恢复默认”使用更小的自绘图标并保留边框安全间隙。

### 视频与图片底部控制独立

- 底部进度统计按当前视频或图片工作区独立计算。
- 图片页使用压缩文案，视频页使用转换文案。
- 暂停、继续和停止只能控制当前工作区所属队列；另一个媒体队列运行时，当前页不能误控对方。

### 图片参数宽度

- 图片模式加宽尺寸下拉框，保证“最大边 1000px”等长文本完整显示。
- 格式、质量、大小和旋转按内容重新分配宽度，降低无效占用。

### 压缩与进度条

- 压缩后条形的原始体积侧使用可辨识浅灰底。
- 100% 蓝色进度条使用白字。
- 部分填充条按每个字符所在背景分别使用白色或深色；不再依赖 ListView/`PrintWindow` 中不稳定的裁剪重绘。
- 底部总进度采用同一文字可读性规则。

### 视频时长列

- 视频列表新增可排序时长列。
- 图片行显示 `—`，未知视频时长显示“检测中”。
- 自动迁移 v4.2.1 的 10 列宽设置，避免旧用户列宽错位。

### 轻量覆盖交付

- 正式用户包为 `Mediova-v4.2.2-Light.zip`。
- 包内严格只有 `Mediova.exe`、`README.txt`、`runtime-manifest.json` 和 `THIRD_PARTY_NOTICES.md`。
- 不包含 FFmpeg、FFprobe、`Components/` 或用户 Data。
- 覆盖更新时保留原 `Components\FFmpeg\bin`。

## 正式证据

- 正式版本与标签：`Mediova v4.2.2` / `p101-v4.2.2`
- 正式产品提交：`73a9936ab799f19640f847160f493940e052fb11`
- 稳定分支提交：`05188bf49051ba0626611cc3331484beb7d16a0d`
- 产品候选头：`35e828278c0f306a47be45fd3823d25485927fc0`
- 正式主线准入：`30757622819`
- 轻量发布运行：`30757937363`
- 原生 self-test：62/62
- 正式 `Mediova.exe` SHA-256：`347bfb538848448b84665d72e8d2fd786c62b59f9a2ea20c62142bf50432ee83`
- 内部完整 Runtime ZIP SHA-256：`65bea3b7de07e2bf61928f5d71f5382170113fc4b92cee74e3ef4d1a80f5d3bd`
- 用户轻量包：`Mediova-v4.2.2-Light.zip`
- 轻量包大小：`1,698,207 bytes`
- 轻量包 SHA-256：`34cd31913aa7f1bc61223d973c2806ac4ce87bf8fbd49d2614d5aaa4695c3208`
- 正式 Release 资产：轻量 ZIP 与 SHA256 文件两项
- 四套真实 Windows 截图：视频宽屏 1650×930、视频紧凑 1120×720、图片模式 1450×820、搁置模式 1450×820
- 截图人工复核：100% 蓝底白字、部分填充分区配色以及其余六项 UI 修复均通过
- 一次性设施清理：PR `#242`
- 长期分支：`p101-exp`、`p101-stable` 已非强制回流到清理后的 `main`

完整用户可见变化见 `Mediova_v4.2.2_版本说明.md`。

## 活跃事项

| 工作项 | 状态 | 完成标准 | 当前证据或尚缺内容 |
|---|---|---|---|
| v4.2.2 正式发布与收口 | 已完成 | 正式主线、准确标签、轻量 Release、62 项自检、四套截图、设施清理和分支回流完成 | 正式提交 `73a9936a…`；CI `30757622819`；发布 `30757937363`；清理 PR `#242` |
| v4.2.2 双工作区底部控制 | 已完成 | 当前页只显示和控制所属媒体队列；图片/视频文案及进度独立 | 源码契约、普通/竞态测试、Windows 原生测试和截图通过 |
| v4.2.2 进度文字可读性 | 已完成 | 100% 蓝底白字；部分填充按字符背景分别配色 | 正式四套截图人工复核通过 |
| 多 DPI 与多显示器观察 | 持续观察 | 在 125%、150%、175% DPI 和多显示器切换中记录可复现问题 | 当前字体矢量状态灯与 DPI 契约通过；仍需真实设备持续观察 |
| 外部程序与特殊媒体观察 | 持续观察 | 记录显卡驱动、PotPlayer、拖放、托盘、HEIC 和特殊媒体问题 | 发现问题后单独登记复现条件 |
| 下一版本范围 | 待决策 | 只有出现可复现问题或明确价值目标后才确定版本 | 当前无已批准的新版本范围 |

## 状态说明

- `待决策`：问题已确认，路线未确定。
- `计划`：路线已确定，尚未开始。
- `进行中`：已经实施，尚未满足完成标准。
- `待验证`：自动验证完成，仍缺真实环境或用户确认。
- `已完成`：实现、完成标准和可追溯证据齐全。
- `持续观察`：不阻断当前正式版本，但需要在真实环境中长期记录。
- `暂缓`：当前不推进，并写明重新评估条件。
- `取消`：明确不再实施，并保留原因。
