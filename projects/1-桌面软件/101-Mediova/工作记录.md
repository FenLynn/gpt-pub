# Mediova 工作记录

本文件只回答当前正在做什么、怎样算完成、证据在哪里和尚缺什么。完整功能历史由 Git、标签、Release 和 `软件开发总结.md` 保存；正式基线由 `阶段记录.md` 保存。

## 当前目标

完成 Mediova v4.1.0 的记录体系收敛、长期 CI 加固、稳定候选刷新、正式主线准入和完整 Runtime Release。

## 活跃事项

| 工作项 | 状态 | 完成标准 | 证据或尚缺内容 |
|---|---|---|---|
| 记录体系按 A＞B＞C 收敛 | 完成 | README、工作记录、阶段记录和项目约束职责分离；不新增形式化空文件 | 本轮 `p101-exp` 文档提交；`软件开发总结.md` 继续兼任设计与演进 |
| P101 长期 CI 加固 | 完成 | PR 自动范围门禁；检出当前 PR 准确 head SHA；普通运行并发取消；原有 Linux/Windows/Runtime 验证保留 | `.github/workflows/p101-mediova-ci.yml` |
| v4.1.0 稳定候选刷新 | 进行中 | 最新 `p101-exp` 已包含本轮文档与 CI 调整，并通过 `p101-exp → p101-stable` 的范围、Linux 和 Windows 原生准入 | 尚缺新的稳定候选 PR 与当前准确 head SHA 上的绿色 CI |
| v4.1.0 正式主线准入 | 计划 | 最新 `p101-stable` 已包含最新 `main`，通过 `p101-stable → main` 的完整准入后合并 | 尚缺正式主线 PR 与准确 head SHA 上的绿色 CI |
| v4.1.0 标签与 Release | 计划 | 从 `main` 准确标签提交生成或复核完整 Runtime；固定 ZIP/EXE 大小、SHA-256、自检、清单和签名状态 | 尚缺 `p101-v4.1.0` 标签、Release 与正式资产 |
| 发布后回流与收尾 | 计划 | 最新 `main` 正常回流 `p101-stable` 和 `p101-exp`；关闭失效临时 PR；一次性工作流不留在活动主线 | 不强推长期分支；临时分支删除需另行确认无独有提交 |
| Windows 10/11 实机界面与硬件观察 | 待验证 | 拖放、托盘、多显示器、100%–175% DPI、显卡编码、PotPlayer、文件时间和特殊媒体无阻断问题 | 自动几何、Windows Runner 和原生自检已通过；真实用户环境继续观察，不以构建成功替代实机结论 |

## 近期候选

| 工作项 | 状态 | 完成标准 | 证据或尚缺内容 |
|---|---|---|---|
| 右键菜单 | 计划 | 任务、源文件、输出文件和批量操作具有一致上下文菜单 | 尚未实施 |
| 裁剪时间轴与预览优化 | 计划 | 定位、时长裁剪、画面裁剪和预览交互准确、稳定 | 尚未实施 |
| 文件级暂停与安全恢复 | 计划 | 暂停边界明确，中断后按文件安全恢复，残缺输出不记为成功 | 尚未实施 |
| HEIC/HEIF 与手机媒体兼容 | 计划 | 在不破坏时间、元数据和颜色策略的前提下支持 HEIC/HEIF、手机 MOV 与 HDR | 尚未实施 |
| 轨道、字幕、分段与拼接 | 暂缓 | 轨道选择、字幕封装、视频分段和兼容流无损拼接形成完整工作流 | v4.1.x 稳定性与基础交互完成后重新评估 |

## 最近完成

| 工作项 | 结果 | 证据 |
|---|---|---|
| v4.1.0 界面统一与 Runtime/Data 实现 | 已实现统一底色、按钮五态、输出母目录、真实体积条、透明 Runtime、独立 Data、组件迁移和完整 Runtime ZIP | 实现阶段 Windows 运行 `30724806539`；候选 PR `#68` |
| v4.1.0 实现阶段验证 | Go 全测、竞态、`go vet`、真实 FFmpeg、Runtime 清单、私人 Data 扫描和原生自检通过 | 候选 EXE SHA-256 `9d6cc313182e7804d4117677a3bae033b84ab163690b80acf50302884178bb8b`；候选 ZIP SHA-256 `26ee217c1eeabd66ba0adf4078459ebb73b3093101d696b95eeceac36ae4c74f` |
| v4.0.0 公开迁移与正式发布 | Mediova 成为 GPT-Pub P101 唯一活动维护主线 | 标签与 Release `p101-v4.0.0`；迁移与发布细节见 `迁移校验清单.md` 和 `阶段记录.md` |

## 状态说明

- `待决策`：问题已确认，路线未确定。
- `计划`：路线已确定，尚未开始。
- `进行中`：已经实施，尚未满足完成标准。
- `待验证`：自动验证完成，仍缺真实环境或用户确认。
- `暂缓`：当前不推进，并写明重新评估条件。
- `取消`：明确不再实施，并保留原因。

事项只有同时具备实现、完成标准和可追溯证据，才能移入“最近完成”。
