# Mediova 工作记录

本文件用于快速判断 Mediova 已完成、正在处理和计划中的工作。后续每完成一项，应将复选框改为 `[x]`，并把状态改为“完成”；不得只在对话中声明完成而不更新本表。

## 状态规则

| 复选框 | 状态 | 含义 |
|---|---|---|
| `[x]` | 完成 | 已实现并达到相应验证要求 |
| `[ ]` | 进行中 | 已开始处理，但尚未完成闭环验证 |
| `[ ]` | 计划 | 已确认需要做，但尚未开始 |
| `[ ]` | 暂缓 | 当前不进入近期版本，保留后续评估 |

## 当前基线

| 项目 | 当前值 |
|---|---|
| 产品 | Mediova |
| 正式版本 | v4.0.0 |
| 正式路径 | `projects/1-桌面软件/101-Mediova/` |
| 日常开发分支 | `p101-exp` |
| 稳定候选分支 | `p101-stable` |
| 正式主线 | `main` |
| 正式标签与 Release | `p101-v4.0.0` |
| 当前开发状态 | v4.0.0 已正式发布，等待下一轮明确的软件需求 |

## 工作清单

| 完成 | 状态 | 工作项 | 已有结果或完成标准 |
|---|---|---|---|
| [x] | 完成 | 视频与图片统一批量导入 | 支持文件、文件夹、递归扫描、中文路径和混合媒体导入 |
| [x] | 完成 | 自动方向修正 | 读取媒体方向信息并支持自动、0°、90°、270°及翻转设置 |
| [x] | 完成 | 视频统一转码压缩 | 支持 H.264/H.265、分辨率、质量、码率和目标体积模式 |
| [x] | 完成 | 图片批量处理 | 支持 JPG/PNG、尺寸限制、方向修正和 JPG 输出 |
| [x] | 完成 | 复杂媒体兼容 | 覆盖多音轨、文本字幕、位图字幕安全处理和 VFR |
| [x] | 完成 | GPU 编码与自动回退 | 支持 NVENC、QSV、AMF 检测，失败时自动回退 CPU |
| [x] | 完成 | 动态并发与大批量保护 | 根据逻辑处理器调度，并限制探测、缩略图和转换工作池 |
| [x] | 完成 | 任务状态闭环 | 每个输入具有准备、队列、转换、暂停、完成或失败等明确结果 |
| [x] | 完成 | 输出完整性保护 | 同名输出原子占用，只有真实存在并通过最低完整性检查的文件才记为成功 |
| [x] | 完成 | 配置与历史恢复 | 主配置损坏时可从备份恢复，并保留任务、会话和历史信息 |
| [x] | 完成 | 顶部工具栏与底部状态区重构 | 顶部保持单行，底部状态、总进度和操作区完成自适应整理 |
| [x] | 完成 | 行内进度与体积结果表达 | 进度文字位于完整进度条内部，体积变化按比例显示并区分增减 |
| [x] | 完成 | DPI 与窗口尺寸适配 | 已覆盖 100%–175% DPI、多种窗口尺寸和布局防遮挡回归 |
| [x] | 完成 | 时长裁剪与画面裁剪初版 | 已建立裁剪入口、预览与基础参数链路 |
| [x] | 完成 | 本地播放器对比入口 | 支持 PotPlayer 状态检测和源文件、输出文件对比操作 |
| [x] | 完成 | Windows 原生自检体系 | 正式 v4.0.0 自检 57/57 通过，并使用真实 FFmpeg/FFprobe |
| [x] | 完成 | 自动化测试与构建门槛 | 包含源码哈希、Go 全测、竞态检测、`go vet`、Windows 交叉构建和正式资源构建 |
| [x] | 完成 | Mediova 品牌切换 | 窗口、托盘、关于、诊断、资源、构建产物和数据目录统一为 Mediova |
| [x] | 完成 | 旧数据保守兼容迁移 | 首次运行只复制缺失的旧 VideoUpright 数据，不删除旧目录，不覆盖新文件 |
| [x] | 完成 | 公开迁移与正式发布 | v4.0.0 已合入 `main`，建立 `p101-v4.0.0` 标签和 Release |
| [x] | 完成 | 私有开发主线退出 | 私有旧项目停止 CI、开发和发布，只保留只读源码与 Git 历史 |
| [x] | 完成 | GPT-Pub P101 收尾 | 删除一次性导入与发布设施，校正文档，回流长期分支并清理临时分支 |
| [ ] | 计划 | v4.0.x 真实环境回归修复 | 根据真实 Windows 10/11、显卡、播放器和媒体样本发现的问题发布补丁版本 |
| [ ] | 计划 | 右键菜单 | 为任务、源文件、输出文件和批量操作提供一致的上下文菜单 |
| [ ] | 计划 | 裁剪时间轴与预览优化 | 提升时长裁剪、画面裁剪、定位和预览交互的准确性与易用性 |
| [ ] | 计划 | 文件级暂停与安全恢复 | 明确暂停边界，并支持中断后按文件安全恢复，不把残缺输出记为成功 |
| [ ] | 计划 | HEIC/HEIF 图片支持 | 在不破坏时间与元数据策略的前提下增加读取、预览和转换 |
| [ ] | 计划 | 手机 MOV 与 HDR 扩展 | 增强方向、色彩、时间信息、硬件编码和兼容性处理 |
| [ ] | 计划 | 音视频轨道选择 | 支持用户选择保留、移除或映射音轨与字幕轨道 |
| [ ] | 计划 | 字幕编辑与批量封装 | 支持字幕选择、基础编辑和不重编码封装工作流 |
| [ ] | 计划 | 视频分段 | 支持按时间、数量或标记分段，并保持结果闭合与可追溯 |
| [ ] | 计划 | 无损拼接与合并 | 对兼容流优先无损拼接，不兼容时给出明确原因或安全转码方案 |
| [ ] | 计划 | 多显示器与长期运行回归 | 持续验证 DPI、窗口移动、托盘、大批量任务和长时间运行稳定性 |

## 更新要求

1. 新需求确认进入开发计划后，先在本表增加一行，再开始实现。
2. 处于“进行中”的项目必须写明尚缺的验证或闭环条件。
3. 完成项目时同步更新相关版本说明、开发总结和自动化测试。
4. 不得删除尚未完成的计划以制造“全部完成”的假象；不再需要的项目应改为“暂缓”并说明原因。
