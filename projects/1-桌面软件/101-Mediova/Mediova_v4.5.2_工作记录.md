# Mediova v4.5.2 工作记录

## 当前状态

- 正式版本仍为 Mediova v4.5.0，标签与 Release 为 `p101-v4.5.0`；
- v4.5.2 仍是用户验证候选，不是正式 Release；
- 当前活动分支：`p101-v452-round12-list-structure`；
- 当前 Draft PR：#366，基于 `p101-stable`；
- 第六轮已被实机否决；第七轮从稳定分支回退重做；第八至第十一轮完成 UI 控制权、真实交互与防闪收口；第十二轮完成任务列表结构、真实主页缩略图与最终动态门禁；
- 第十二轮最终产品候选 Head：`ef68dc288b2bc4d66f68d0b0d111987e26180975`；
- 最终产品 CI：P101 Mediova CI #646 / Run `31192085647`，全部通过；
- 用户确认前不得合并到 `p101-stable` 或 `main`，不得建立标签或正式 Release。

## 历史阶段摘要

1. 第一轮：顶部工具栏、输出目录焦点、主按钮图形、有效运行时钟、进度防闪和状态灯基础修复。
2. 第二轮：12 列模型、编号列、旧列宽迁移、缩略图生命周期、多选退出队列与列表视觉收口。
3. 第三轮：直接文件共同顶层、跨卷与 UNC 分组、安全输出前缀、统一导入摘要和轻量通知。
4. 第四轮：双端时间轴、播放头、裁剪框创建/移动/缩放、旋转坐标映射及处理顺序。
5. 第五轮：Windows 原生鼠标交互、真实 FFmpeg 四方向输出矩阵及编辑框覆盖裁剪模型修复。
6. 第六轮：持续重绘与界面叠加方案被用户实机否决，未合并。
7. 第七轮：从 `p101-stable` 重新建立独立编辑窗口、五标记时间轴、状态灯与主界面基础视觉，并完成首批七项反馈修复。
8. 第八轮：不再叠加新补丁层，执行“UI 控制权收口”，解决多个实现同时控制同一控件造成的反复错乱。
9. 第九至第十一轮：真实鼠标交互、缩略图生命周期、滚动覆盖层、剪裁编辑器及主界面 idle/hover 防闪继续收口。
10. 第十二轮：15 列任务结构、预览/文件名拆分、浅蓝选中、剪裁摘要列、列管理、视频/图片独立列配置，以及真实媒体主页缩略图端到端修复与验证。

## 第八轮根因

用户实机反馈中的列宽串扰、滚动条闪烁、底部按钮偶发错位、输出路径蓝色选区和剪裁窗口闪烁，并不是彼此孤立的小问题。根因是：

- 同一主窗口同时存在旧布局、第七轮布局和反馈层布局；
- 同一 ListView 同时存在列表绘制、滚动条开关和叠加绘制等多条子类链；
- 视频和图片虽建立独立文件，但仍回写旧全局列宽数组；
- 输出路径只清理外层 ComboBox，后续异步刷新又会恢复内部 Edit 选区；
- 剪裁窗口安装依赖控件创建事件的时序，不能保证最后一个控件创建后仍有事件触发；
- 预览画面和绿色框直接绘制到屏幕 DC，拖动时同时更新多个控件与摘要。

本轮约束为：

> 同一控件只有一个布局入口；同一状态只有一个权威数据源；同一画面只有一条绘制链。

## 用户 11 项反馈处理

### 1. 预览图不可见

- 左侧画布在无位图时显示“正在生成预览”或明确的 FFmpeg 状态，不再留下无法解释的黑框；
- 新预览成功前保留上一张成功帧；
- “高清预览”改为在左侧画布内显示处理后帧，不再调用外部图片查看器；
- 新请求通过序列号淘汰旧请求，旧请求不能覆盖更新结果。

### 2. 横纵滚动条不停闪烁

- 删除 `ShowScrollBar()` 显示/隐藏原生非客户区滚动条的循环；
- 改为在 ListView 正常绘制后叠加轻量悬浮滑块；
- 只有鼠标进入右侧或底部约 18 DPI 像素的边缘区，停留 500 ms 后才出现；
- 鼠标位于中央时没有计时器、轮询或持续刷新；
- 增加 `WM_STYLECHANGING` 样式守卫，阻止 ListView 在内容变化后重新加入原生横纵滚动条和立体边框。

### 3. 视频与图片列宽仍互相影响

- 视频和图片使用两套独立的 `Video` / `Image` 列宽数组；
- 旧 `TaskColumnWidths` 只作为第一次升级迁移输入，运行期间不再写回；
- 切换媒体、刷新列表、排序、导入和调整窗口尺寸均不得改变列宽；
- 只有列头鼠标按下前后宽度确实发生变化时，才认定用户完成了手动拖动并保存；
- `#` 编号列与其他列遵守同一规则。

### 4. 统一命名为“剪裁”

- 主页面入口改为“剪裁”；
- 右键菜单改为“剪裁…”；
- 独立窗口标题改为“剪裁 · 文件名”；
- 状态提示统一使用“剪裁设置”；
- 窗口内部保留“剪辑”和“画面”两个功能分区。

### 5. 时间轴重做

- 源起始时间固定显示在左侧，源结束时间固定显示在右侧；
- 不再在轨道下方写“源起点/源终点”；
- 剪辑起点使用向右蓝色旗标；
- 剪辑终点使用向左蓝色旗标；
- 当前时间使用独立红色下向游标和底部时间标签；
- 源时间、剪辑边界和当前时间分成三个垂直层；
- 起止标签接近时自动错层，不相互覆盖。

### 6. 剪裁页面整体美化

- 右侧标题简化为“剪辑”和“画面”；
- “设定起始时间/设定结束时间”简化为“起始时间/结束时间”；
- 按钮简化为“当前、源起点、源终点”；
- 源时间范围移到左侧；
- X、Y、宽、高四个输入框放在同一行；
- “高清预览、应用到已选、应用当前、取消”均按文字宽度紧凑排列；
- 右侧说明框改为浅灰只读检查卡，不再呈现为可编辑多行文本框；
- 检查卡固定显示转正尺寸、保留区域、保留面积、输出时长和处理顺序；
- 时间轴交互后强制恢复检查卡格式，旧摘要不能覆盖新布局。

### 7. 左侧绿色选区拖动防闪

- 视频帧、绿色选区和八个控制点先画入内存位图；
- 完整绘制后只执行一次 `BitBlt`；
- 拖动中裁剪数值最多约每 40 ms 同步一次；
- 鼠标释放后再完整更新输入框和检查卡；
- 只重绘画布，不触发整个剪裁窗口持续刷新。

### 8. 主任务区域现代化

- 移除 ListView 的强 `WS_EX_CLIENTEDGE` 立体边框；
- 阻止控件重新恢复 `WS_BORDER` 和原生滚动条样式；
- 表头使用加粗字体和清晰底部分隔线；
- 行之间使用极浅横向分隔线；
- 选中行使用浅蓝背景；
- 编号和缩略图仍在同一正常绘制链中叠加，不安装第二套绘制子类。

### 9. 右下角三个按钮偶发错乱

- 删除旧第七轮独立 footer 布局入口；
- 状态文字、开始、暂停和停止只由统一主窗口布局函数计算；
- 坐标直接依据主窗口客户区，不再把另一个正在移动的子控件作为锚点；
- 使用 `BeginDeferWindowPos / DeferWindowPos / EndDeferWindowPos` 一次性提交四个控件位置；
- 增加布局重入保护；
- 窗口缩放、右栏变化和刷新均经过同一个入口。

### 10. 悬浮提示重做

- 增加一个统一浅色悬浮提示管理器；
- 延迟 420 ms；
- 浅灰白背景、1 px 浅边框、约 6 px 圆角；
- 最大宽度约 280 px；
- 不激活窗口、鼠标可穿透；
- 点击、切换、缩放或失焦时立即关闭；
- 禁用按钮可附带当前不可用原因；
- 不使用重复计时器和黑色大框。

### 11. 输出路径蓝色选区与主进度条起点刻度

- 通过 `GetComboBoxInfo()` 获取输出路径内部真实 Edit 子窗口；
- 媒体切换和后续排队刷新完成后，多阶段执行 `EM_SETSEL`、`HideCaret` 并把焦点恢复到媒体按钮；
- 用户亲自点击路径框时取消自动清理，保留正常编辑能力；
- 主进度条只有进度大于 0 时才绘制填充；
- 删除 0% 时的假竖线刻度。

## 控制权收口

本轮移除了或降级为兼容函数的重复入口：

- 旧第七轮主窗口 WinEvent/layout hook；
- 旧独立剪裁布局事件 hook；
- 旧第二套 ListView 绘制子类；
- 原生滚动条显示/隐藏循环；
- 运行期间对旧全局列宽数组的写回。

剪裁窗口改为：首次检测到窗口后，仅投递一次 UI 线程安装消息；当 `WM_CREATE` 结束后，同步安装窗口、时间轴和画布的最终子类，然后删除临时安装器。该方案不再依赖“最后一个控件之后还会发生一次创建事件”的不可靠时序。

## 固定清单

第八轮建立：

```text
V452_ROUND8_UI_CONVERGENCE_FILES_SHA256.txt
清单 SHA-256：fd1a7d2b5a9fb8946378185a2d0ea6835dca22a3cc0ed4376e42034d12302bf3
固定文件：19 个
```

`SOURCE_FILES_SHA256.txt` 已恢复为严格入口，校验 `go.mod` 和本轮收口清单；本轮清单测试再逐项校验 19 个文件的实际 SHA-256。

## 第八轮自动验证候选

功能实现 Head：

```text
4f27dd68bc723869f64e2d02f5705f63781a30cf
```

源码与固定清单 Head：

```text
1ce39aa3b33d8845dc476ae482a283b9a1345819
```

验证：

```text
P101 Mediova CI #489
Run ID：30985441903
Artifact ID：8921941024
Artifact digest：a6a1d5848c345745fc797845ad2a57c00c4ea0b4c36cda03ee24c57667cbc278
```

CI #489 全部通过：

- P101 路径范围门禁；
- Linux 固定清单、全部 Go 测试、竞态测试、`go vet`、Windows 交叉测试和构建；
- Windows 隔离 Data 原生测试；
- 固定来源真实 FFmpeg 下载与运行；
- 完整 v4.5.2 Runtime 构建与 Runtime 清单校验；
- 视频宽屏、紧凑窗口、图片模式、搁置模式和剪裁窗口真实截图；
- 时间轴交互截图；
- 原生自检 126/126 通过，失败 0。

当时产物：

```text
Mediova.exe
SHA-256：8738198796fa48c41acbcf085eadd60c06871cdc515123e71ec0ce421718d2d7

Mediova-v4.5.2-Verification-Runtime.zip
SHA-256：ac50a55919ca18a80c658b1f088c64053f2f2a03b3170d4ea56f36cf550a60fe
```

自动截图人工复核确认：

- 主列表默认不显示原生横纵滚动条；
- 主列表不再有强立体边框；
- 表头和浅色行分隔线已经生效；
- 输出路径没有蓝色选区；
- 底部三个按钮排列稳定；
- 主进度条 0% 不再出现竖线；
- 剪裁窗口左侧预览正常显示；
- 右侧使用“剪辑/画面”和浅灰检查卡；
- 时间轴源时间、蓝色起止和红色当前时间分层显示；
- 交互后检查卡保持新格式。

## 第十二轮任务列表与布局最终收口

第十二轮按实机反馈完成以下 9 项：

1. 右下角按钮布局专题收口；
2. 表头稳定与单一绘制权；
3. 全行统一浅蓝选中样式；
4. “预览”与“文件名”拆为独立列；
5. 首页缩略图链路修复；
6. 新增“时间剪裁”和“画面剪裁”列；
7. 齿轮列管理并调整折叠按钮位置；
8. 视频/图片独立列配置、显隐、宽度与 DPI 迁移；
9. 固定清单、Linux/Windows、Runtime、原生自检、动态 UI 与 Artifact 全量收口。

最终显示顺序为：

```text
# | 预览 | 文件名 | 分辨率 | 时长 | 方向 | 输出分辨率 | 质量 | 旋转 | 体积 | 压缩后 | 进度 | 状态 | 时间剪裁 | 画面剪裁
```

### 真实主页缩略图问题与根因

在第十二轮最终人工检查中发现，旧动态样本只证明了占位预览绘制，不能证明真实媒体帧确实进入首页。为此增加了真实 Windows 端到端门禁：

```text
正常启动 Runtime
→ 使用 Runtime 内真实 FFmpeg 生成 testsrc2 MP4
→ 点击 Mediova“添加文件”
→ 操作 Windows 原生“添加媒体文件”对话框
→ 导入真实 MP4
→ 等待媒体 probe
→ 读取 ListView 第 0 行预览子项 iImage
→ 像素检查真实预览单元格
```

诊断曾确认：

- 文件选择对话框和真实导入成功；
- 分辨率、时长已从“检测中”更新为 `640×360 / 00:02`，证明 probe 成功；
- Small ImageList 存在；
- 但预览子项 `iImage` 长时间保持 `-1`，说明不是 Round12 绘制失败，而是缩略图生成/安装链未最终绑定到任务行。

产品默认 `ThumbnailCache=true`，而此前原生 `real_thumbnail=true` 只验证直接 `GenerateThumbnailBMP`，没有覆盖默认缓存路径。最终修复保留缓存优先，同时增加可见任务的延迟直连 BMP 回退：缓存/加载链未及时安装且任务仍为 `ThumbnailIndex=-1` 时，使用直接临时 BMP 路径生成并安装；正常缓存路径成功时不会产生额外回退。generation/所有权检查继续阻止旧任务或旧输入反写。

### 最终真实缩略图证据

最终产品候选：

```text
Head：ef68dc288b2bc4d66f68d0b0d111987e26180975
P101 Mediova CI：#646
Run ID：31192085647
Artifact ID：8999469412
Artifact digest：sha256:0cc7524ec832c9102641e0149b8be54c7e51165620796300e73bd85adda17b76
```

真实媒体门禁结果：

```text
normal_runtime = true
real_file_dialog_used = true
file_imported = true
item_count = 1
preview_subitem_image_indices = [-1, 0]
real_thumbnail_visible = true
unique_colors = 674
quantized_unique = 107
saturated_pixels = 3641
dominant_ratio = 0.145995
luma_span = 240
```

`iImage` 从 `-1` 变为 `0`，证明首张真实缩略图成功安装到首页任务行；`0` 是 ImageList 的合法首个索引。`round12-real-thumbnail-cell.png` 与整表截图人工复核均为真实 testsrc2 视频画面，不是“视频”文字占位块。

### 最终 CI 结论

CI #646 全部通过：

- P101 路径范围门禁；
- Round9 历史 receipt 与 Round12 最终固定清单；
- Linux 全部 Go 测试、全量竞态、`go vet`、Windows 交叉测试/构建；
- Windows 隔离 Data 原生源码测试；
- 固定来源真实 FFmpeg；
- v4.5.2 Verification Runtime 构建与 Runtime 边界清单；
- 四套 Windows 主界面截图；
- 原生 self-test 126/126，失败 0；
- Round11 main/editor idle 40 帧均仅 1 个唯一哈希；
- 横纵悬浮滚动条 300 ms 隐藏、650 ms 显示、移开后隐藏；
- Round12 footer/header 稳定；
- 15 列三视口逐像素覆盖；
- 时间剪裁两行、画面剪裁百分比；
- 全行浅蓝选择；
- 视频 → 图片 → 视频独立列配置恢复；
- 真实文件对话框导入与真实主页缩略图。

最终产品产物：

```text
Mediova.exe
SHA-256：4a96c8e1b9dddb69269e6fbfa7e0e18a2d2aad94511eaeb46f4b64a3c41e1a3c

Mediova-v4.5.2-Verification-Runtime.zip
SHA-256：9b22b3ec96c38e8555469a95d3aa6a89048401aa3b1ff705eeb45ed2bb754f05
```

## 当前待用户实机验证

自动化与截图已经覆盖真实文件导入、真实缩略图、主要布局与稳定性，但仍不能替代真实桌面手感，继续验证：

- 视频和图片分别手动拖成明显不同的列宽，反复切换、排序、导入、缩放窗口和重启后是否逐像素保持；
- 横纵悬浮滑块边缘命中范围、500 ms 延迟和真实拖动手感；
- 输出路径在真实连续切换和手工编辑后是否始终不残留蓝色选区；
- AVI、WMV、MP4、H.264、H.265 和图片的首帧及处理后预览；
- 连续拖动时间轴与绿色画面选区时是否仍有可感知闪烁；
- 搁置、退出锁定、保存、归队和立即重启的完整任务状态链；
- 100%、125%、150%、175% DPI、多显示器和真实长文件名；
- 窗口连续缩放及右侧栏展开/收起时底部按钮是否始终稳定。

当前候选继续保持 Draft。正式版本仍为 v4.5.0；用户确认前不得合并、建立标签或正式 Release。
