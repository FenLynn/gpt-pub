# Mediova v4.5.2 工作记录

## 当前状态

- 正式基线：Mediova v4.5.0，标签与 Release `p101-v4.5.0`；
- v4.5.1：用户验证候选，不转正式版本；
- v4.5.2：五轮实机反馈修复与 Windows 原生收口全部完成；
- 唯一执行规格：`Mediova_v4.5.2_实机反馈修复规格.md`；
- 第四轮已通过 PR #332 合并，合并提交 `8eadd2ff96d5211db19d005a5093036e766e2428`；
- 第五轮分支：`p101-v452-round5-windows-closeout`；
- 第五轮 PR：#341；
- 第五轮冻结产品候选：`3ba4b43bc94ff38ab02aae910ff698e5ecbd7881`；
- 最终产品 CI：`P101 Mediova CI #388` / run `30883792882`，范围、Linux 与 Windows 原生链全部通过；
- 当前结论：第五轮已具备合并条件，等待 PR #341 合入 `p101-stable`；用户实机验证后再决定正式 v4.5.2 标签与 Release。

## 版本边界

- 不修改正式 `p101-v4.5.0` 标签和 Release；
- 本轮不创建正式 v4.5.2 标签；
- CI 产物属于 v4.5.2 验证候选，其 Windows 文件版本资源仍沿用正式基线 `4.5.0`；
- 不修改 AtlasDesk 或其他项目；
- 每轮必须通过 P101 范围、全量源码清单、独立固定清单、Go 全测、全量竞态、`go vet`、Windows 双交叉构建和 Windows 原生门禁；
- 自动截图和消息级鼠标验证是准入证据，但不替代用户真实桌面的最终视觉与业务验收。

## 第一轮：基础 UI 与暂停语义

完成顶部工具栏冷启动显示、输出目录焦点清理、实心主按钮图形、有效运行时钟、进度防闪和真圆状态灯。

- PR #321；
- 最终文档头：`28aee2de8711e6746b5df00893d0c02c834a9ff3`；
- CI：`30823007412`、`30823432844`；
- 合并提交：`159ad9ac1a19e91c5ab95c63b5378780565e6ab4`。

## 第二轮：列表、队列和缩略图

完成 12 列显式模型、`#` 编号列、旧列宽迁移、缩略图所有权与缓存释放、目录回退、多选退出队列及列表视觉收口。

- PR #323；
- 产品候选：`9487cb8e68c5fbe972662057bd3bb84542a8c00d`；
- CI：`30832646333`；
- 合并提交：`702e37fa75040ee69491eabb1a52452e5c07633f`。

## 第三轮：导入、输出结构与轻量通知

完成直接文件共同顶层保留、跨卷与 UNC 分组、安全输出前缀、统一导入摘要和无激活右下角通知。

- PR #328；
- 产品候选：`76af0b8199c0765b0a2327c2bb8cdb55f3877bed`；
- 产品 CI：`30865931850`；
- 最终文档头：`78d6ec2b46da6c63bd067a5387e830c8338d37a4`；
- 文档 CI：`30866099807`；
- 合并提交：`1ee487006fa5b07f91c6d4eb3f290d1f80ed7900`。

## 第四轮：裁剪编辑器

### 双端时间区间

- 新增与 Win32 无关的 `TrimRangeState`，统一开始、结束和播放头；
- 最小区间按一帧计算；
- 提供时间与像素双向映射；
- 支持拖动开始端、结束端、播放头和整体平移区间；
- 区间触边时按实际位移同步播放头；
- 数值输入、I/O 快捷键和跳转操作会重绘时间轴；
- 图片任务显示禁用时间轴，不伪造视频时长。

### 裁剪框创建、移动和缩放

- 支持创建、移动、上、右上、右、右下、下、左下、左、左上十种交互状态；
- 已有裁剪框可整体移动并限制在有效帧内；
- 支持八方向缩放；
- 自由比例和锁定比例复用统一归一化与偶数化规则；
- 预览画布显示八个绿色拖拽手柄并切换对应光标；
- 鼠标操作与 X、Y、宽、高数值控件共用 `model.Crop`。

### 旋转坐标与 FFmpeg 顺序

- 完成 0°、90°、180°、270° 帧尺寸和裁剪矩形映射；
- 完成反向映射与四方向往返测试；
- 固定处理顺序为“旋转 → 裁剪 → 缩放 → 编码”；
- 裁剪数值定义在转正后的画面坐标系中。

### 第四轮证据

- PR #332；
- 设计冻结提交：`5b89bda2b2b231d5fbc7e1e18a14f4a95172a46e`；
- Windows 接线首次全绿候选：`5d79d97130dee8d9772fae4c292022d411dcd8b5`；
- Windows 接线 CI：`30867482413`；
- 第四轮冻结产品候选：`650b7ad6d5e4c0e498dae3ada9d9834adfc6e9d1`；
- 正式全链 CI：`30868312362`；
- 最终文档头：`d43ee435d43277393c0d9a32c14530c3efc7d954`；
- 文档 CI：`30868540067`；
- 合并提交：`8eadd2ff96d5211db19d005a5093036e766e2428`；
- 第四轮固定清单与 133 项全量源码清单通过。

## 第五轮：真实 Windows 收口

### 验证范围

第五轮不再只验证逻辑与编译，而是在 GitHub Windows 2025 原生 Runtime 中完成：

- 视频与图片裁剪窗口真实创建；
- 正常、最大化、还原和交互后截图；
- 时间轴开始端、结束端、播放头和整体区间消息级拖动；
- 裁剪框整体移动与八方向手柄拖动；
- 图片任务时间轴禁用；
- 导入通知显示、截图、手动关闭和窗口销毁；
- `0° / 90° / 180° / 270°` 四方向真实 FFmpeg 输出矩阵；
- Runtime Manifest、私有数据边界和完整 Artifact 上传。

### 暴露的真实问题

首轮原生探针发现视频和图片共 18 项裁剪鼠标交互失败。表面现象为：

- 初始裁剪区域 `160 / 90 / 640 / 360` 在窗口创建或首次拖动后退化；
- 早期表现为 Y、宽和高变成 0；
- 后续表现为仅 X 方向能保留，纵向与右侧手柄变化被覆盖。

消息、坐标和手柄诊断最终证明：

1. 鼠标消息坐标正确；
2. 画布坐标到帧坐标的映射正确；
3. `HitCropHandle` 命中方向正确；
4. `MoveCrop` 与 `ResizeCrop` 的纯逻辑测试正确；
5. 真正根因位于 Win32 编辑框程序回填链。

`cropToControls()` 会依次对 X、Y、宽、高编辑框调用 `SetWindowText`。Windows 编辑框在 `WM_SETTEXT` 同步过程中会向父窗口发送 `EN_UPDATE` 和 `EN_CHANGE`。旧父窗口过程不区分通知类型，每收到一次通知就立即从四个编辑框反读完整裁剪状态。因此写入第一个 X 后，尚未写完的 Y、宽、高旧值会立刻覆盖 `model.Crop`；后续写入继续基于已被破坏的模型，最终造成维度丢失。

### 最终修复

第五轮采用独立 Windows 接线，不改写第四轮已冻结文件：

- 通过 UI 线程 `WH_CBT` 在裁剪顶层窗口进入 `WM_CREATE` 前冻结原始 `model.Crop`；
- 在顶层窗口创建前安装父窗口子类，初始化期间屏蔽裁剪编辑框同步通知；
- 四个编辑框建立后，在 UI 线程确定安装时间轴和预览画布子类；
- 对每个编辑框的 `WM_SETTEXT` 建立按控件 ID 的写入位掩码；
- `WM_SETTEXT` 调用期间，父窗口屏蔽该控件产生的全部 `WM_COMMAND`，同时覆盖 `EN_UPDATE` 与 `EN_CHANGE`；
- 用户键盘输入不会设置写入位，因此原有实时编辑行为保持不变；
- 完整控件建立后一次性恢复初始模型与四个数值框；
- 窗口销毁时解除子类并清理状态；
- 临时高容量鼠标诊断在定位完成后删除，不进入冻结候选。

### 最终原生验证

最终冻结候选 `3ba4b43bc94ff38ab02aae910ff698e5ecbd7881` 在 CI #388 中通过：

- 范围门禁：通过；
- 140 项全量源码清单：通过；
- 第五轮独立固定清单：通过；
- Linux Go 全测：通过；
- Linux 全量竞态：通过；
- Linux `go vet`：通过；
- Windows 交叉测试与交叉构建：通过；
- Windows Go 全测：通过；
- Windows 全量竞态：通过；
- Windows `go vet`：通过；
- 真实 FFmpeg 下载与运行：通过；
- 完整 Runtime 构建：通过；
- Runtime Manifest 与私有数据边界：通过；
- 既有主窗口四类截图：通过；
- 第五轮裁剪窗口与通知截图：通过；
- 117 项原生自检：117 通过，失败 0；
- Artifact 上传：通过。

原生交互检查中，视频与图片的以下项目全部通过：

- 裁剪框整体移动；
- 北、东北、东、东南、南、西南、西、西北八方向缩放；
- 正常窗口截图；
- 最大化截图；
- 还原后继续存活；
- 交互后截图。

四方向 FFmpeg 输出：

```text
0°：crop=320:180:40:30 → 320×180
90°：transpose=1,crop=180:320:30:40 → 180×320
180°：hflip,vflip,crop=320:180:40:30 → 320×180
270°：transpose=2,crop=180:320:30:40 → 180×320
```

### 清单与产物

- 第五轮固定清单：`代码/V452_ROUND5_WINDOWS_CLOSEOUT_FILES_SHA256.txt`；
- 固定清单 SHA-256：`a750521c929068443482e853a3709c9c2498a3efe8dde2e79913e7826a133627`；
- 全量源码清单：140 项；
- 产品候选：`3ba4b43bc94ff38ab02aae910ff698e5ecbd7881`；
- 产品 CI：`30883792882`；
- `Mediova.exe` SHA-256：`33ac9c35991cf1a09c21d6af7198bcce805750a70031b3a3654229575f234e29`；
- Runtime ZIP SHA-256：`05dad60cbb88cb6ce94d6152c143250b08c416142fa1c295ac1812d6d4864dbe`；
- CI Artifact SHA-256：`2d8405ece8b1e2811538519fbc28ae717e55e7abc6ffee4573a772ed99dbcde5`；
- Artifact ID：`8882325680`。

## 后续边界

- PR #341 合入 `p101-stable` 后，本次五轮开发阶段结束；
- 当前只交付 v4.5.2 验证候选，不建立正式标签和 Release；
- 用户需要在真实桌面重点复核裁剪框拖动手感、视觉布局、实际视频输出与预览一致性；
- 用户验证通过后，再单独决定是否从稳定线进入 `main` 并建立正式 v4.5.2 标签与 Release。
