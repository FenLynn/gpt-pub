# FigureStudio 工作记录

## 2026-09-19

### 立项与 v0.1

- 建立 P108 与 `p108-exp`。
- 完成 WYSIWYG Plotly 出图、项目保存和首批科研图型。
- 参考 `FenLynn/laser-lab-analyzer` 的中文科研工具 UI 与 Plotly 工作流。

### v0.2

- UI Scale 与科研 Figure 物理尺寸解耦。
- 完成图 / 曲线 / 轴 / 图例 / 检查 Inspector。
- 完成 Project Explorer、Publication Checker、Matplotlib Python script export。
- 建立 Tauri 2 与 Matplotlib publication renderer 基础层。

### v0.3｜Origin-style Data Workspace

这一阶段根据实际使用反馈，重新把“数据”放回 FigureStudio 的核心。

完成：

- 从以 Graph 为中心的 Dataset 模型改为 Project / DataBook / Sheet / Graph；
- 左侧 Project Explorer 不再混放 Graph 内部曲线控制；
- Folder、DataBook、Sheet、Graph 使用不同的单色矢量图标；
- 中央加入 Data / Graph 文档 Tab；
- Sheet 在中央提供 Spreadsheet 编辑；
- 单元格支持 number / text / null；
- 列支持 name / unit / role；
- 支持 X / Y / Z / XErr / YErr / Label / None；
- 列头采用 A(X)、B(Y) 等科研习惯；
- Embedded Data 直接编辑；
- Linked Data 默认只读，避免外部源与项目内编辑冲突；
- Web Linked Data 支持重新选择并 Reload，或解除链接后编辑；
- Graph 改为稳定的 sheetId + columnId 引用；
- 列角色只影响新建 Graph 的默认映射，不会让已有 Graph 静默换数据；
- `.sfig` 升级 schema 0.3；
- 自动迁移 v0.1 / v0.2 项目。

当前仍不创建 stable、tag、Release 或额外桌面 CI。


### v0.4｜Workbook / Worksheet 收敛

继续对照 Origin 官方 Workbook、Worksheet、Column Designation、Plot Setup 与 Project Explorer 的高价值交互，完成：

- 顶层文档从 Sheet 改为 DataBook / Graph；
- Sheet 变为 DataBook 内部页签；
- Sheet 页签支持重命名、拖拽排序；
- Sheet 可以跨 DataBook 移动；
- 增加工作表备注与可展开列备注；
- 增加复制工作表 / 复制结构；
- 增加 Graph 数据映射页；
- 增加 Sheet → Graph Dependents；
- 新建图采用多 X 分段默认关联；
- 已有 Graph 禁止数据映射静默 fallback；
- 删除和 Replace / Reload 都加入引用完整性保护；
- Linked / Embedded source 从 DataBook 下沉到 Sheet；
- 同一 DataBook 可混合链接原始数据与项目内处理数据；
- 浏览器多项目通过多 Tab / Window 实现，sessionStorage 隔离自动恢复；
- UI 数据工作区残留开发术语改为中文；
- `.sfig` 升级 schema 0.4，并兼容迁移 0.1 / 0.2 / 0.3。

本轮没有加入公式引擎、XLSX、虚拟表格或跨 Sheet 多源 Series；这些是下一层能力，不再需要修改核心项目模型。


### Matplotlib Gallery 视觉收敛与常用图 QA

继续按 Matplotlib 官方 Gallery 对常用科研图做第二轮收敛，未修改 .sfig schema（仍为 0.5）。

完成：

- Plotly 升级到 4.1.1；
- MathJax 3.2.2 改为前端本地打包，去除 CDN 依赖；
- 修复混合中文 + LaTeX 轴标题只剩公式、以及 MathJax 与 Plotly.react 重绘竞态；
- 科研默认改用 Matplotlib tab10 色循环，调整线宽、marker、字体层级和向外 Tick；
- 单系列默认不显示无意义 legend，多系列自动显示；
- 双 Y 左右轴文字和 Tick 默认随对应系列颜色区分；
- 新增“图外右侧” legend，并为 Plotly 预留出版边距；Matplotlib export 使用对应 bbox_to_anchor；
- Heatmap / Contour / 3D 的中文轴标题、Colorbar 与 3D 哑光表面进一步收敛；
- 新增“常用图验收”数据与 Scatter / Errorbar / Bar / Grouped / Stacked 五张 QA 图；
- Grouped / Stacked 输入至少两列主数据；
- Errorbar 缺 YErr 时不再静默退化，直接进入“待完成”并由 Checker 报告；
- Bar 增加数值标签开关、小数位和柱内 / 柱外位置；
- Plotly 与 Matplotlib Python 导出同步上述默认和新能力。

验证：

- 多轮 P108 scope gate / TypeScript / Vite / Pages 全绿；
- exact Pages artifact 在 Chromium 中逐图读取 Plotly fullData / fullLayout；
- Scatter：2 个 marker trace、无额外 marker edge；
- Errorbar：真实 error_y，15 个误差值，单系列默认无 legend；
- Bar：单系列无 legend、默认无柱边框；
- Grouped：3 个 bar trace，barmode=group，独立 gap / group gap；
- Stacked：3 个 bar trace，barmode=stack；
- 图外 legend 的 x=1.02 与右侧预留 margin 已运行时确认；
- 柱外 / 柱内数值标签的 Plotly texttemplate / textposition 已确认；
- 从 UI 直接导出 Bar / Grouped / Stacked Python 脚本，三份均通过 py_compile，并在 MPLBACKEND=Agg 下实际运行成功；
- Chromium 验收过程没有 console error / page error。
