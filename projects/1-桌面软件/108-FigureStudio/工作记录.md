# FigureStudio 工作记录

## 2026-09-19

### 立项与设计

- 核对全部现有分支，确认 P106 MediaIndex 与 P107 LaserBench 已占用，P108 为下一个可用桌面软件编号。
- 建立 `p108-exp`。
- 建立 Project / Dataset / Figure / PlotSpec / Template / Preset / Export 设计基线。
- 参考 `FenLynn/laser-lab-analyzer` 的 React / Plotly 工作台、中文 UI 密度与 OSA 导出实现。

### v0.1 Web

完成四轮迭代：

1. 建立 React + TypeScript + Vite + Plotly.js Pages 原型；
2. 修复预览与导出二次布局问题，改为同一出版画布 WYSIWYG；
3. 锁定单视口工作台，最大化中央画布，重构紧凑属性面板；
4. 完成 v0.1 项目工作流与图型体系。

当前能力包括：

- 多 Dataset / 多 Figure；
- 真正的 `.sfig` ZIP 项目；
- Replace Data stable ID；
- Undo / Redo；
- autosave recovery；
- user / project defaults；
- CSV / TSV / TXT / Clipboard；
- XY / Spectrum / Errorbar / Offset；
- Bar / Heatmap / Surface 3D；
- 轴、图例和 Series 高频属性；
- SVG / PNG 600 dpi；
- GitHub Pages 自动部署。

当前仍未创建 stable、正式 tag 或 GitHub Release。
