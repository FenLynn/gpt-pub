# AtlasDesk v0.7.2 迁移校验清单

## 1. 固定来源

| 项目 | 值 |
|---|---|
| 原仓库 | `FenLynn/agent-foundry` |
| 原固定提交 | `1f9f043dcbc31bf39397757f0eb07a137b586cf1` |
| 原产品名 | Personal Workbench |
| 原版本 | `v0.7.2` |
| 目标仓库 | `FenLynn/gpt-hub` |
| 目标项目 | `P202 AtlasDesk` |
| 目标路径 | `projects/2-桌面软件/202-AtlasDesk/` |

原仓库提交、PR、分支、发布记录和历史名称不改写。

## 2. 过滤范围

仅迁移：

```text
personal-workbench-native/**
personal-workbench-launcher/**
personal-workbench-terminal-host/**
personal-workbench-smoke/**
.github/workflows/build-personal-workbench.yml
```

四个工程重写到 `代码/`；旧构建工作流重写到：

```text
迁移资料/历史构建工作流/build-personal-workbench.yml
```

这样既保留历史内容和提交关系，又不会把源仓库工作流直接注册为 GPT-Hub 活动 Actions。

## 3. 固定导出结果

| 校验项 | 固定值 |
|---|---|
| 过滤历史头 | `07fb6b79e9d476d132ca8abb405b885ff369c7c6` |
| 有效提交数 | `68` |
| 当前文件数 | `101` |
| 导出 ZIP SHA-256 | `21e915086bbe3dc0b95e268fee1fd53f1d4844cc6659e2b022b53994ce84396f` |
| Git bundle SHA-256 | `09386d6c175e41ea567763e55f032800a1950e02a3a33799193d6c599db4c9b3` |
| 源文件清单 | [`SOURCE_FILES_SHA256.json`](SOURCE_FILES_SHA256.json) |
| 历史映射 | [`迁移历史映射.tsv`](迁移历史映射.tsv) |

## 4. 已执行的迁移门槛

- [x] 导出 ZIP SHA-256 与固定值一致；
- [x] bundle SHA-256 与固定值一致；
- [x] `git bundle verify` 通过；
- [x] 过滤头与固定提交一致；
- [x] 有效提交数为 68；
- [x] 当前文件数为 101；
- [x] 101 个文件逐项核对字节数和 SHA-256；
- [x] 迁移提交为双父提交；
- [x] GPT-Hub 主线历史与 AtlasDesk 过滤历史均可达；
- [x] 迁移载荷目录在导入提交中删除；
- [x] 活动 `.github/workflows/` 未由 Actions 令牌越权写入；
- [x] VideoUpright、专利、文献调研、`shared/` 和 `GPT_RULES.md` 零变更。

## 5. 活动 CI 接续

目标仓库长期 CI：

```text
.github/workflows/p202-atlasdesk-ci.yml
```

它从 P202 新路径执行：

- C++20 原生 Windows 终端宿主构建；
- .NET 8 恢复、WPF/XAML 编译和 smoke tests；
- 项目中心、任务中心、工具中心、备份恢复、Zotero、Markdown 等回归；
- 生产终端路由与真实 CMD/ConPTY；
- framework-dependent 单文件 payload；
- Go 启动器嵌入；
- 禁止捆绑重型运行时；
- 最终体积、ZIP、EXE、manifest 和 SHA-256。

## 6. 名称与兼容边界

迁移基线的工程名、Assembly、AppData 和配置兼容标识暂时保持原值。新交付文件可以使用 AtlasDesk 名称，但以下内容只有在 v0.7.3 中经过旧配置启动验证后才能修改：

- 应用数据目录；
- 设置文件位置与 JSON schema；
- WebView2 profile；
- 备份和恢复格式；
- 终端与任务历史路径；
- 组件发现名称。

## 7. 合并前待完成

- [ ] P202 Windows CI 成功；
- [ ] AtlasDesk v0.7.2 验证包生成；
- [ ] 记录新 EXE 与 Artifact SHA-256；
- [ ] 更新 PR 正文为最终固定值；
- [ ] 更新根目录和桌面软件分类索引；
- [ ] 合并后删除 `p202-import-v072`；
- [ ] 建立 `p202-v072` 固定标签；
- [ ] 从最新 main 建立 `p202-v073-command-center`。

## 8. 结论规则

迁移成功只说明代码、历史、构建和运行基线无损接入，不自动证明 v0.7.3 新功能正确。v0.7.3 必须使用独立分支、独立 PR 和同一长期 CI 再次验证。
