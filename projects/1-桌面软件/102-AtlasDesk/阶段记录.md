# AtlasDesk 阶段记录

本文件保存 AtlasDesk 当前正式发布基线和最新稳定候选。实时开发事项见 [工作记录](工作记录.md)，长期规则见 [开发约束](开发约束.md)。

## 当前正式基线

- 项目：`P102 AtlasDesk`；
- 正式版本：`v0.8.0`；
- 正式标签与 Release：`p102-v0.8.0`；
- 正式产品提交：`65c433239419ad57d381da65c88f6d57cf01a334`；
- 正式主线：`main`；
- 目标平台：Windows 10/11 x64。

`main` 本轮未修改。v0.8.5—v0.9.0 均按任务分支 → `p102-stable` → `p102-exp` 流程推进，只有明确正式发布时才进入 main。

## 最新稳定候选：v0.9.0

状态：**首轮准确产品 head 已通过，文档证据写入后等待最终准确 head 复验。**

- 候选分支：`p102-v090-project-workflow`；
- 首轮准确产品提交：`a2de95ea1a0b591a343ee153a74c9dd51fdc117b`；
- 首轮 Windows CI：`30759426051`，success；
- Runtime Artifact：`8836999041`；
- Artifact 名称：`AtlasDesk-v0.9.0-Runtime`；
- Artifact digest：`sha256:b72b3b082da41a10254247ad175688d292b1518188da9013d812373911f2daeb`。

该轮已通过：

- P102 变更范围门禁；
- 全部历史与新增 smoke；
- 临时 Python 标志项目和最近文件的真实项目上下文测试；
- Git 有界执行、超时、取消和降级源码边界；
- 项目快速切换 generation 门禁；
- 独立 Windows STA 真实窗口启动驻留；
- “开发”默认项目页与环境懒加载；
- 5 次真实 CMD/ConPTY 连续会话；
- Dashboard 恢复与跨域策略边界；
- Runtime/Data 与安全边界；
- WPF publish、manifest、ZIP 一致性与 Artifact 上传。

本文件和工作记录写入后候选 head 会变化，因此必须在新的最终准确 head 上再次通过同等级完整 CI，之后才可合入 `p102-stable`。

## v0.9.0 项目驱动工作流

v0.9.0 将原有项目、工作区、终端、Git、环境标志和最近文件连接为一条显式工作链：

```text
选择项目
→ 后台读取项目上下文
→ 在工作区打开项目
→ 在项目目录建立终端
→ 显示 Git、环境与最近文件摘要
```

主要结果：

- 新增 `ProjectContextService`；
- 新增长期命名的 `ProjectWorkflowCoordinator`；
- 删除 `V070ProjectCenterEnhancer.cs` 和 `V070RuntimeVerifier.cs`；
- 保留现有“项目 / 环境 / 终端”界面，不增加第二套项目中心；
- 项目扫描只在用户明确进入“开发 → 项目”或点击刷新时启动；
- 项目控件不再通过 Loaded 或可见性变化自动扫描；
- 项目选择后才读取当前项目上下文；
- Git 只读取当前选择，使用 `--untracked-files=no`，三秒超时，取消时终止进程树；
- Git 缺失、非 Git、路径失效和超时均安全降级为状态文字；
- 环境摘要只读取 Python、Node、.NET、LaTeX、Rust、Go 等项目标志，不启动语言运行时；
- 最近文件只过滤已保存的最近文件列表，不递归扫描项目内容；
- 快速切换项目会取消前一请求，过期结果不能覆盖当前项目；
- 后台结果显式回到 WPF Dispatcher 后才更新界面；
- 工作区动作复用 `WorkspaceControl`；
- 项目终端动作复用 `WorkspaceTerminalCoordinator`。

## v0.8.5—v0.8.7 演进证据

### v0.8.5 主壳与 Dashboard 韧性

- 准确 head：`3db46ac1f1f86d7e0163acd5cd02aae11b67d627`；
- Windows CI：`30756405835`，success；
- Artifact：`8836087085`；
- digest：`sha256:661c01a901c8bb2c21826e0073069c395a4ab62a8365e659f68630f955471c84`；
- 产品 PR：`#225`；回流 PR：`#226`。

结果：当前显示器 WorkArea、DPI/显示器变化、快速导航最终请求门禁，以及串行、限流、有上限的 Dashboard 恢复。

### v0.8.6 Enhancer 第一阶段收敛

- 准确 head：`ca84d93c46985ede2bdea7b27e762e0d3b2a9b09`；
- Windows CI：`30756907537`，success；
- Artifact：`8836234005`；
- digest：`sha256:027712c72b4300414558848d005d9a579f4aa3272b7f75551c767449a2d5c008`；
- 产品 PR：`#229`；回流 PR：`#231`。

结果：删除 `V068HotfixEnhancer`，建立职责命名的功能宿主和旧所有权收敛组件，WorkArea/DPI 只由 `ShellResilienceCoordinator` 管理。

### v0.8.7 工作区—终端连续链

- 准确 head：`784a5bcd27bfafd516ce4ec6d2883d12d67faedd`；
- Windows CI：`30757476643`，success；
- Artifact：`8836410389`；
- digest：`sha256:8ec6d2e63c8c1270fa43c35cddfdafe0b976dbf6b9d320ebb614d08e50f5227e`；
- 产品 PR：`#236`；回流 PR：`#237`。

结果：工作区、开发环境和项目中心使用统一 `WorkspaceTerminalCoordinator`；`Ctrl+Shift+T` 新建当前上下文终端，`Ctrl+Shift+R` 用户主动重开最近终端；不自动恢复，不保存命令或输出。

## v0.8.0 正式发布基础

v0.8.0 建立透明文件夹 Runtime、Roaming/Local Data 分离、本地安全中心和原生 ConPTY 边界：

```text
AtlasDesk/
├── AtlasDesk.exe
├── AtlasDesk.TerminalHost.exe
├── WebView2Loader.dll
├── e_sqlite3.dll
├── Assets/Terminal/
├── runtimes/
├── README.txt
└── runtime-manifest.json
```

正式验证：

- 稳定候选 CI：`30719382922`；
- main 准入 CI：`30719585177`；
- Artifact：`8824436803`；
- Artifact SHA-256：`db89d017df7f401a6673f226ad95930e14a599f03c90bde9e069e19b9ee3735c`。

## 当前已知限制

### Zotero 实时数据库共存

部分安装环境中，AtlasDesk 直接读取实时 `zotero.sqlite` 时不能与 Zotero 客户端稳定并行运行。当前处理方式：

- 使用 AtlasDesk 资料库前关闭 Zotero；
- 该问题不阻塞 v0.9.0 项目工作流；
- 不继续在实时数据库上叠加并行、重试或长超时；
- 后续只有明确需求时才评估隔离只读快照方案。

## 发布边界

- 本轮目标是形成 `v0.9.0` 稳定候选；
- 最终准确 head 完整 CI 通过后合入 `p102-stable`；
- 随后将相同树回流 `p102-exp`；
- `main`、正式标签和 Release 不在本轮自动修改。
