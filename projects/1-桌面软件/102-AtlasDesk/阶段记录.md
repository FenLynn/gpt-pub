# AtlasDesk 阶段记录

## 当前状态

- 项目：`P102 AtlasDesk`；
- 外部历史编号：`P202`；
- 当前版本：`v0.7.3`；
- 状态：GPT-Pub 公开迁移验证中；
- 冻结源提交：`c48180a2ac74b6336220bce484c8051551d4e2fb`；
- 公共过滤头：`f8493a0bcb73ed0e05f039144890dc07b5d885fb`；
- v0.7.2 公共映射：`397e142b1160d8065dc39670864cba38b592fec2`；
- 目标唯一事实源：`FenLynn/gpt-pub`。

在公开 Windows CI、产物复核、PR 合并、标签、Release 和长期分支建立全部完成前，私有仓库仍保留只读基线，不提前删除。

## 已完成产品基线

### v0.7.0｜项目中心

- 项目中心并入“开发”页面；
- 项目 / 环境双标签；
- 识别 Git、Python、Node、.NET、LaTeX、Rust 和 Go；
- 扫描保持有界、只读，不执行项目代码。

### v0.7.1｜任务中心

- 启动与筛选、历史列表、详情三栏；
- 保留 SHA-256 与默认工作区统计两类任务；
- 取消、有界并发、历史持久化和中断恢复保持不变。

### v0.7.2｜工具中心与第一阶段历史接续

- 文件完整性正式纳入工具中心；
- Personal Workbench 的 68 个有效历史提交和 101 个当前文件迁入外部 P202；
- 迁移采用双父提交，逐文件字节数和 SHA-256 全部通过；
- AtlasDesk v0.7.2 完成 Windows 构建、smoke、发布和打包；
- 兼容标识和用户数据路径保持 `PersonalWorkbench`。

### v0.7.3｜AtlasDesk Command Center

- 将 Ctrl+K 升级为真实 `AtlasDesk Command Center`；
- 页面入口覆盖首页、Dashboard、工作区、资料库、开发、工具、任务和设置；
- 按需检索项目、最近文件、任务历史、工具和 Zotero 只读文献；
- 工作区文件搜索有界、可取消；
- 不建立后台索引、数据库、AI 搜索或任意脚本执行能力；
- 主窗口产品标题切换为 AtlasDesk，内部兼容身份保持不变。

## 外部 v0.7.3 已验证结果

- 功能合并提交：`f233947134f193a8353bbbd2a6c3707805f4646f`；
- Windows CI：`30688352958`；
- Artifact：`AtlasDesk-v0.7.3-lightweight-win-x64`；
- Artifact ID：`8814776706`；
- Artifact ZIP SHA-256：`4a44f0cd4bb328b48c2e225dce255babd5f24408e8578df663bce8443b4fe37b`；
- 便携 ZIP SHA-256：`6efd90abb21dfa22490ef143e39987d59be6ba394b5f1f1ac4ed8b34154eec81`；
- AtlasDesk.exe SHA-256：`ce33cdc955760f77cefe70f360977c5c92ed4e7c0e4af935369f359460a5a756`；
- AtlasDesk.exe：`11,488,256 bytes`；
- App payload：`5,073,349 bytes`；
- Terminal host：`176,640 bytes`。

上述结果是公开迁移的对照基线，不替代 GPT-Pub 自己的 Windows 重新验证。

## GPT-Pub 公开过滤与导入

### 已完成

- 只保留 AtlasDesk 项目历史并改写到 `projects/1-桌面软件/102-AtlasDesk/`；
- 从全部公共历史删除一次性 `迁移载荷/**` 及其中的私有短时地址；
- 作者邮箱改写为 GitHub noreply，作者姓名和提交归属保留；
- 当前树、全部历史 blob、提交信息、作者身份和秘密扫描通过；
- 3 项测试夹具完成精确人工审计；
- 保留 86 个公共提交、108 个冻结源文件和 925,277 字节；
- 108 个冻结源文件的模式、字节数和 SHA-256 全部通过；
- 公共过滤头已成为 `p102-import-v073` 的祖先；
- GPT-Pub 主线历史与 AtlasDesk 公共历史通过双父导入关系同时保留；
- PR 正文和评论中的临时下载地址已删除；
- 迁移触发文件已删除。

详细证据见 [`迁移校验清单.md`](迁移校验清单.md)。

### 尚待完成

- 登记并通过 `.github/workflows/p102-atlasdesk-ci.yml`；
- 重新生成 GPT-Pub Windows x64 正式验证包；
- 复核 EXE、便携 ZIP、Artifact、manifest 和 SHA-256；
- 合并 P102 公开迁移 PR；
- 建立 `p102-v0.7.3` 标签与公开 Release；
- 从同一公共 `main` 提交建立 `p102-stable` 与 `p102-exp`；
- 完成私有仓库只读收尾并停止双线开发。

## 当前已知技术债

- `WorkbenchFeaturePipeline.cs` 仍包含多个历史 enhancer/polish 层，暂不做大规模重构；
- 工程目录、namespace、Assembly 和 AppData 路径保留 Personal Workbench 历史名称，这是明确的兼容策略；
- MainWindow 基础层仍保留旧命令说明处理器，但实际 Ctrl+K 入口已由体验层接管到真实 Command Center。

## 后续版本计划

- v0.7.4：首页和跨模块上下文联动；
- v0.7.5：设置页与整体体验收口；
- v0.8.x：在产品闭环后评估 enhancer 管线内部整理。

后续功能开发只能在 GPT-Pub 的 `p102-exp` 推进，不再从私有仓库创建第二条活动主线。
