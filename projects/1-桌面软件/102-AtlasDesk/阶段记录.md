# AtlasDesk 阶段记录

本文件保存 AtlasDesk 当前正式发布基线和最新稳定候选。实时事项见 [工作记录](工作记录.md)，长期规则见 [开发约束](开发约束.md)。

## 当前正式基线

- 项目：`P102 AtlasDesk`；
- 正式版本：`v0.8.0`；
- 正式标签与 Release：`p102-v0.8.0`；
- 正式产品提交：`65c433239419ad57d381da65c88f6d57cf01a334`；
- 正式主线：`main`；
- 目标平台：Windows 10/11 x64。

`main` 在 v1.0.0 正式准入前不修改。日常版本按任务分支 → `p102-stable` → `p102-exp` 推进。

## 最新候选：v0.9.2

状态：**产品实现与首轮完整自动验证完成，等待文档准确 head 复验。**

- 候选分支：`p102-v092-zotero-readonly`；
- 产品 head：`5ef68ad41ea41ef3939d1599a6a96078fd57eff5`；
- 产品 PR：`#268`；
- Windows CI：`30778337167`，success；
- Runtime Artifact：`8842841455`；
- Artifact 名称：`AtlasDesk-v0.9.2-Runtime`；
- Artifact digest：`sha256:5930b62a23f6b79857e0e538a1cc62fcbd96041863b5dd8999e23cb16f736ced`。

### v0.9.2 Zotero 只读研究面板

- 保留 Zotero 式三栏结构，不暴露编辑能力；
- 右侧直接显示标题、作者、来源、年份、Citation Key、DOI 和引用预览；
- Citation Key 兼容 Zotero 原生 `citationKey` 与旧 Better BibTeX `Extra` 写法；
- 支持复制标题、DOI、Citation Key、快速引用、LaTeX `\\cite{}` 和 Pandoc `[@key]`；
- 支持打开 PDF，以及在 Windows 资源管理器中显示 PDF，便于复制原文件；
- 摘要、详情、附件、笔记、标签和分类保留为下层只读信息；
- GB/T 7714 与 APA 明确标记为快速引用，正式投稿仍以 Zotero/CSL 输出为准；
- SQLite 继续使用 `SqliteOpenMode.ReadOnly` 与 `PRAGMA query_only=ON`。

### v0.9.2 验证范围

- P102 变更范围门禁；
- 全部历史与新增 smoke；
- 原生 Citation Key 与旧 Better BibTeX Extra 解析；
- LaTeX、Pandoc、DOI 和快速引用格式；
- PDF 文件定位动作与 XAML 非法控制字符门禁；
- 独立 Windows STA 真实窗口启动驻留；
- 5 次真实 CMD/ConPTY 连续会话；
- Dashboard 恢复与跨域策略；
- Runtime/Data 与安全边界；
- WPF publish、manifest、ZIP 一致性和 Artifact 上传。

## 已完成稳定候选

| 版本 | 关键结果 | 完整 CI |
|---|---|---|
| `v0.8.5` | 主壳 WorkArea/DPI、快速导航、Dashboard 有界恢复 | `30756405835` |
| `v0.8.6` | 退休 V068，建立职责命名宿主与所有权收敛 | `30756907537` |
| `v0.8.7` | 工作区、环境和项目统一终端连续性协调器 | `30757476643` |
| `v0.9.0` | 项目选择驱动上下文、工作区、终端、Git、环境和最近文件 | `30759759594` |
| `v0.9.1` | 原生 WorkArea 最大化边界与工具页纵向自适应 | `30772555331` |

## 当前已知限制

部分安装环境中，AtlasDesk 直接读取实时 `zotero.sqlite` 时不能与 Zotero 客户端稳定并行运行：

- 使用 AtlasDesk 资料库前关闭 Zotero；
- 不在实时数据库上叠加并行、长超时或破坏性重试；
- 后续只有必要时才评估隔离只读快照；
- 该限制不改变“绝不写入 Zotero 数据库”的边界。

## 当前发布边界

- 文档提交后的准确 head 必须再次通过同等级完整 CI；
- 通过后将 PR `#268` 合入 `p102-stable`；
- 再将相同产品树回流 `p102-exp`；
- 随后进入 v0.9.3；
- `main`、正式标签和 Release 只在 v1.0.0 阶段修改。
