# AtlasDesk 阶段记录

## 当前状态

- 项目：`P102 AtlasDesk`；
- 当前版本：`v0.7.4`；
- 唯一事实源：`FenLynn/gpt-pub`；
- 正式路径：`projects/1-桌面软件/102-AtlasDesk/`；
- 日常分支：`p102-exp`；
- 稳定分支：`p102-stable`；
- 正式主线：`main`。

## 版本连续性

- `v0.7.0`：项目中心；
- `v0.7.1`：任务中心；
- `v0.7.2`：工具中心与历史接续；
- `v0.7.3`：AtlasDesk Command Center，并完成公开迁移；
- `v0.7.4`：产品称谓与本地路径收口。

## v0.7.4 更新

- 所有用户可见产品称谓统一为 `AtlasDesk`；
- 配置和日志目录切换到 `%APPDATA%\AtlasDesk`；
- 新日志文件为 `%APPDATA%\AtlasDesk\logs\atlasdesk.log`；
- 启动器释放目录切换到 `%LOCALAPPDATA%\AtlasDesk\App\<版本>`；
- 内部释放文件名改为 `AtlasDesk.App.exe`；
- 首次启动自动迁移旧 `%APPDATA%\PersonalWorkbench`；
- 已存在 AtlasDesk 目录时不覆盖；移动失败时复制并保留旧目录；
- 设置、异常提示、诊断、支持包、备份界面和首页称谓全部统一；
- 保留旧备份 schema、namespace、Assembly 与无空格内部标识作为兼容实现；
- CI 版本门槛改为合法版本且不得低于公开基线，不再硬编码某个补丁号。

## v0.7.4 验证

稳定候选 CI：`30699344908`，全部通过：

- PR 路径范围检查；
- C++20 原生终端宿主；
- .NET 8、WPF/XAML 与完整 smoke；
- 大小写不敏感的旧产品可见称谓扫描；
- 旧配置目录真实迁移与已有新目录保护；
- Command Center、项目、任务、工具、备份、Zotero 与 Markdown 回归；
- 真实 CMD/ConPTY；
- framework-dependent 发布与 Go 启动器；
- 轻量体积边界；
- 外层 EXE、便携 ZIP 内 EXE 和 SHA-256 一致；
- 禁止的重型运行时未被捆绑。

## 维护约束

- 后续只使用 AtlasDesk 产品称谓；
- 功能开发只在 `p102-exp`；
- 正式流程为 `p102-exp → p102-stable → main → 标签 / Release`；
- 用户交付默认只提供 `AtlasDesk.exe`；仅在确有多个必须共同更新的文件时提供额外文件。

## 后续计划

- `v0.7.5`：设置页与整体体验的小步收口；
- `v0.8.x`：产品闭环后再评估内部 enhancer 管线整理。
