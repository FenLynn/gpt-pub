# AtlasDesk 阶段记录

## 当前状态

- 项目：`P102 AtlasDesk`；
- 外部历史编号：`P202`；
- 当前正式版本：`v0.7.3`；
- 状态：已完成 GPT-Pub 公开迁移并正式维护；
- 唯一事实源：`FenLynn/gpt-pub`；
- 正式路径：`projects/1-桌面软件/102-AtlasDesk/`；
- 公开迁移合并提交：`df66c9246706439c35f082027a82eb34e827de69`；
- 正式标签：`p102-v0.7.3`；
- 公开 Release：`https://github.com/FenLynn/gpt-pub/releases/tag/p102-v0.7.3`；
- 日常分支：`p102-exp`；
- 稳定分支：`p102-stable`。

## 历史连续性

### v0.7.0｜项目中心

- 项目中心并入“开发”页面；
- 项目 / 环境双标签；
- 识别 Git、Python、Node、.NET、LaTeX、Rust 和 Go；
- 扫描保持有界、只读，不执行项目代码。

### v0.7.1｜任务中心

- 启动与筛选、历史列表、详情三栏；
- 保留 SHA-256 与默认工作区统计两类任务；
- 取消、有界并发、历史持久化和中断恢复保持不变。

### v0.7.2｜工具中心与第一阶段历史接续

- 文件完整性正式纳入工具中心；
- Personal Workbench 的 68 个有效历史提交和 101 个当前文件迁入外部 P202；
- 迁移采用双父提交，逐文件字节数和 SHA-256 全部通过；
- 兼容标识和用户数据路径保持 `PersonalWorkbench`。

### v0.7.3｜AtlasDesk Command Center

- 将 Ctrl+K 升级为真实 `AtlasDesk Command Center`；
- 覆盖页面、项目、最近文件、任务历史、工具、Zotero 只读文献和常用命令；
- 搜索有界、可取消，不建立后台索引、数据库、AI 搜索或任意脚本执行能力；
- 主窗口产品标题切换为 AtlasDesk，内部 Assembly、AppData 和配置兼容身份保持不变。

## GPT-Pub 公开迁移结果

### 固定来源

- gpt-hub 冻结源提交：`c48180a2ac74b6336220bce484c8051551d4e2fb`；
- 公共过滤头：`f8493a0bcb73ed0e05f039144890dc07b5d885fb`；
- v0.7.2 公共映射：`397e142b1160d8065dc39670864cba38b592fec2`；
- 公共历史：86 个提交；
- 冻结源当前树：108 个文件，925,277 字节。

### 公开安全处理

- 只保留 AtlasDesk 项目历史并改写到 P102；
- 从全部公共历史删除一次性 `迁移载荷/**` 和私有短时 URL；
- 作者邮箱改写为 GitHub noreply，作者姓名和提交归属保持不变；
- 当前树、全部历史 blob、提交信息、作者身份和秘密扫描通过；
- 253 个唯一历史 blob 全部审计，无私钥、Token、私有附件 URL 或短时 Artifact URL；
- 3 项固定测试夹具和 1 项脱敏器虚拟路径完成精确人工审计；
- 108 个冻结源文件的模式、字节数和 SHA-256 全部通过。

详细证据见 [`迁移校验清单.md`](迁移校验清单.md) 和 `迁移资料/公开迁移/`。

## Windows 验证与正式发布

- 完整 Windows CI：`30696517002`；
- CI Artifact ID：`8817462686`；
- 正式 Release 运行：`30697099134`；
- Artifact ZIP SHA-256：`1f2564f6429595a049140b684d5b8e4d301c5e47ca273259cac576063230bda3`；
- 便携 ZIP SHA-256：`cf6fdf10b0806b1d81806b1c036323785f6622bc4f7afd57789ff9a199890720`；
- AtlasDesk.exe SHA-256：`78a034923a6d522fb0d2e0c3e2f701dd9ad0be30e21dd33f4edb0c14605825ff`；
- AtlasDesk.exe：`11,488,256 bytes`；
- App payload：`5,073,349 bytes`；
- Terminal host：`176,640 bytes`；
- C++20 原生终端宿主、.NET 8/WPF/XAML、完整 smoke、Command Center、项目/任务/工具、备份、Zotero、Markdown、真实 CMD/ConPTY、Go 启动器和正式打包全部通过；
- 外层 EXE、便携 ZIP 内 EXE 和 `SHA256.txt` 字节及哈希一致；
- manifest 确认 `P102 / gpt-pub / v0.7.3 / PersonalWorkbench`，且未捆绑禁止的重型运行时。

## 分支与维护状态

- `p102-stable` 与 `p102-exp` 最初从同一公开迁移基线 `df66c9246706439c35f082027a82eb34e827de69` 建立；
- 完成公开收尾后，`main` 回流并快进两个长期分支；
- 后续固定流程为 `p102-exp → p102-stable → main → 标签 / Release`；
- 一次性公开导入器、发布工作流、验证脚本和触发文件已删除；
- 私有 gpt-hub P202 长期 CI 与公开导出工具已删除；
- 私有收尾合并提交：`ef7c34422e4ad5e48a98a469ae247debee22298c`；
- P202 仅保留只读源码、旧 PR、提交、标签和迁移证据，不再形成第二开发主线。

## 当前已知技术债

- `WorkbenchFeaturePipeline.cs` 仍包含多个历史 enhancer/polish 层，暂不做大规模重构；
- 工程目录、namespace、Assembly 和 AppData 路径保留 Personal Workbench 历史名称，这是明确的兼容策略；
- MainWindow 基础层仍保留旧命令说明处理器，后续内部整理时再移除死路径。

## 后续版本计划

- v0.7.4：首页和跨模块上下文联动；
- v0.7.5：设置页与整体体验收口；
- v0.8.x：产品闭环后评估 enhancer 管线整理。

后续所有功能开发、CI、PR、标签和 Release 只在 GPT-Pub P102 推进。
