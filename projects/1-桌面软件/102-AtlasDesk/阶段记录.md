# AtlasDesk 阶段记录

## 当前状态

- 项目：`P202 AtlasDesk`；
- 当前版本：`v0.7.2`；
- 状态：无损迁移验证中；
- 当前分支：`p202-import-v072`；
- 当前 PR：`P202｜迁入 AtlasDesk v0.7.2 无损历史基线`；
- 后续唯一事实源：`FenLynn/gpt-hub`。

## 已完成能力

### v0.6.x 基础工作台

- 首页与自定义标题栏；
- 本地工作区和轻量编辑器；
- Markdown、图片、数学与代码预览；
- Zotero 只读资料库；
- Python、Conda、uv 与环境入口；
- xterm.js 前端和 Windows 原生 ConPTY 终端；
- 备份、恢复和脱敏诊断；
- 轻量单文件启动与 framework-dependent 发布。

### v0.7.0｜项目中心

- 项目中心并入“开发”页面；
- 项目 / 环境双标签；
- 识别 Git、Python、Node、.NET、LaTeX、Rust 和 Go；
- 搜索、类型筛选、工作区、终端、资源管理器和复制路径；
- 扫描保持有界、只读，不执行项目代码。

### v0.7.1｜任务中心

- 启动与筛选、历史列表、详情三栏；
- 保留 SHA-256 与默认工作区统计两类任务；
- 搜索标题、类型与路径；
- 活跃、完成和问题状态筛选；
- 取消、有界并发、历史持久化和中断恢复保持不变。

### v0.7.2｜工具中心

- 文件完整性正式纳入工具中心；
- 左侧工具目录、中央结果、右侧详情和进度；
- 生成、校验、比较和取消动作集中；
- SHA-256、路径安全、原子写入和比较算法不变。

## 迁移事实

- 原仓库：`FenLynn/agent-foundry`；
- 原提交：`1f9f043dcbc31bf39397757f0eb07a137b586cf1`；
- 原版本：Personal Workbench v0.7.2；
- 过滤历史头：`07fb6b79e9d476d132ca8abb405b885ff369c7c6`；
- 有效历史提交：68；
- 当前文件：101；
- 迁移采用双父提交，同时保留 GPT-Hub 主线和过滤后的项目历史；
- 旧构建工作流完整归档在 `迁移资料/历史构建工作流/`；
- 目标仓库活动 CI 由 `.github/workflows/p202-atlasdesk-ci.yml` 独立登记。

详细哈希和边界见 [`迁移校验清单.md`](迁移校验清单.md)。

## 当前已知技术债

- `WorkbenchFeaturePipeline.cs` 通过多个 enhancer/polish 层连续演进，适合小版本推进，但不应无限叠加；
- 首页的 Ctrl+K /“搜索或执行”入口仍主要是快捷键说明，并非完整命令中心；
- 设置页仍是较早期的长表单，已有 AppSettings 能力没有全部形成清晰界面；
- 工程目录和部分内部兼容标识仍保留 Personal Workbench 历史名称。

迁移和第一个续接版本期间不做大规模架构重写。先验证产品连续性，再单独规划内部收口。

## 下一版本

### v0.7.3｜全局命令中心

目标是把现有 Ctrl+K 入口改为真实、轻量的 AtlasDesk Command Center：

- 搜索并跳转主要页面；
- 搜索最近工作区文件；
- 搜索已扫描项目；
- 搜索任务历史和工具入口；
- 执行有限、明确的常用导航动作；
- 保持按需查询，不建立全盘后台索引、数据库或 AI 搜索。

### 后续计划

- v0.7.4：首页和跨模块上下文联动；
- v0.7.5：设置页与整体体验收口；
- v0.8.x：在产品闭环后评估 enhancer 管线的内部整理。

## 本阶段完成条件

迁移 PR 只有在以下条件全部成立后才能合并：

1. 101 个文件与固定源清单逐项一致；
2. 68 个有效提交和双父历史可达；
3. P202 长期 Windows CI 通过；
4. WPF/XAML、项目、任务、工具、备份、Zotero 和 Markdown 回归通过；
5. 真实 CMD/ConPTY 连续验证通过；
6. AtlasDesk v0.7.2 轻量包成功生成并记录 SHA-256；
7. 未授权项目与根规则零变更；
8. 合并后从最新 main 建立 `p202-v073-command-center`。
