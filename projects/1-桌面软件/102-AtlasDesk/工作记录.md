# AtlasDesk 工作记录

本文件是滚动工作看板。正式版本与稳定候选证据见 [阶段记录](阶段记录.md)，长期规则见 [开发约束](开发约束.md)。

## 当前状态

- 正式主线：`main`，正式版本仍为 `v0.8.0`，未改动；
- 当前任务分支：`p102-v093-project-usage`；
- 当前版本：`v0.9.3`；
- 产品 head `34cf738d14a32eebd03ff0049c9f4feff588bcbd` 已通过完整 CI `30778927804`；
- 产品 PR：`#273`；
- 当前动作：文档准确 head 完整复验，通过后合入稳定分支并回流实验分支；
- 总目标：继续 v0.9.4、v0.9.5，最终完成 v1.0.0 正式发布后停止。

## v0.9.3 已完成

- 收藏常用项目并提供收藏筛选；
- 工作区、项目终端和资源管理器动作记录最近项目；
- 最近列表有明确上限并自动去重；
- 失效的收藏或最近项目仍显示，但禁止直接打开；
- 支持用户明确选择目录重新定位；
- 新目录必须通过项目标志检测；
- 不递归搜索磁盘，不自动猜测移动位置；
- 最近文件仍严格归属当前项目；
- Runtime Artifact `8843012725`；
- Artifact digest `sha256:c36dda30385099eb4bfbf949160ddb7e4de15883b5eea050116a11c33646b498`。

## 下一版本：v0.9.4

任务与工具最小闭环：

- 工具执行进入统一任务状态；
- 等待、执行、成功、失败、取消与有限历史清晰可见；
- 退出时明确处理未完成任务；
- 不引入后台常驻服务、插件市场或复杂调度。

## 后续

### v0.9.5

- 收敛历史 Enhancer、重复导航与生命周期控制；
- 核查 WebView2、终端、Zotero 与任务取消后的资源释放；
- 检查启动、内存、Runtime/Data 和隐私边界。

### v1.0.0

- 完整 Windows CI 与必要实机证据通过；
- 核心页面和主工作链无已知阻断问题；
- `p102-stable` 正式准入后合入 `main`；
- 建立 `p102-v1.0.0` 标签与 Release。

## 长期规则

- 不新增版本编号 Enhancer；
- 不把 AtlasDesk 扩张为通用 IDE、浏览器或 Zotero 替代品；
- SQLite、Git、环境探测和文件扫描不得占用 WPF Dispatcher；
- 每个版本准确 head 完整 CI 后才能合入稳定分支；
- `main` 只在 v1.0.0 正式发布时更新。
