# AtlasDesk 工作记录

本文件是滚动工作看板。正式版本与稳定候选证据见 [阶段记录](阶段记录.md)，长期规则见 [开发约束](开发约束.md)。

## 当前状态

- 正式主线：`main`，正式版本仍为 `v0.8.0`，未改动；
- 当前任务分支：`p102-v095-lifecycle-convergence`；
- 当前版本：`v0.9.5`；
- 产品 head `81c9006882364ebf80027f0ac4e5d1f00335097f` 已通过完整 CI `30780195872`；
- 产品 PR：`#277`；
- 当前动作：文档准确 head 复验，通过后合入稳定分支并回流实验分支；
- 下一步：v1.0.0 正式准入、main 合并、标签、Release 与最终交付。

## v0.9.5 已完成

- 任务页与工具页迁移到 `TaskToolCoordinator`；
- 页面安装、显示切换、退出确认、控件释放和任务中心关闭只保留一个所有者；
- 删除 `V064TaskEnhancer.cs`；
- 删除 `V065ToolsEnhancer.cs`；
- 删除独立临时类型别名文件；
- Pipeline 只保留一个 `TaskTools` 属性；
- 新增旧文件不得回归与唯一 Shutdown 所有权门禁；
- Runtime Artifact `8843397970`；
- Artifact digest `sha256:7882c4ab2ca90ed916204f45dd20233a5360cece687cd8408076937934648887`。

## v1.0.0 正式发布任务

- 将版本号更新为 1.0.0；
- 增加正式发布就绪门禁；
- 汇总 v0.9.0—v0.9.5 的稳定成果；
- 在 `p102-stable` 完成最终候选 CI；
- 再由 `p102-stable` 向 `main` 执行正式准入 CI；
- 合入 main 后建立 `p102-v1.0.0` 标签和正式 Release；
- 回流正式 main 到 `p102-stable` 与 `p102-exp`；
- 下载并核验最终 Runtime，向用户提供下载链接；
- 完成后停止本轮开发。

## 已知限制

- 使用 AtlasDesk 资料库前关闭 Zotero；
- Zotero 功能保持严格只读；
- GB/T 7714 与 APA 为快速引用，正式投稿使用 Zotero/CSL 输出。

## 长期规则

- 不新增版本编号 Enhancer；
- 不把 AtlasDesk 扩张为通用 IDE、浏览器或 Zotero 替代品；
- SQLite、Git、环境探测和文件扫描不得占用 WPF Dispatcher；
- 每个版本准确 head 完整 CI 后才能合入稳定分支；
- v1.0.0 正式 main CI 通过后才建立标签与 Release。
