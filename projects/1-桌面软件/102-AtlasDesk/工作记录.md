# AtlasDesk 工作记录

本文件是 AtlasDesk 的滚动工作看板，只保留当前活跃事项、暂缓/取消事项和少量最近完成。正式版本证据见 [阶段记录](阶段记录.md)，长期规则见 [开发约束](开发约束.md)。

## 当前目标

`v0.8.1` 已进入 `p102-stable` 并生成通过完整 CI 的候选 Runtime。当前阶段只等待真实 Windows 视觉、窗口、环境发现和网页外链验证；在实机证据完成前，不把这些事项写成完成，也不更新 `v0.8.0` 正式阶段快照。

当前开发分支：`p102-exp`
当前稳定候选：`p102-stable`，合并提交 `4ac1b75f5218007c2ae792922e80a68765cf756e`

## 活跃事项

| 工作项 | 状态 | 完成标准 | 证据或尚缺内容 |
|---|---|---|---|
| `AD-UI-001` 主壳连续式布局 | 待验证 | 去除左右两个大圆角外框、重复底纹和阴影；左侧导航与右侧内容仅以清晰分割线区分；普通窗口与最大化布局一致 | 已修改现有 `WorkbenchEnhancer` 控制源并加入 smoke 源边界；PR `#63`、合并 `d8e0922`；尚缺真实 Windows 视觉确认 |
| `AD-WIN-001` 最大化与工作区修复 | 待验证 | 最大化遵守当前显示器 WorkArea；顶部标题和窗口按钮垂直居中；底部不进入任务栏后方；覆盖 DPI、还原和多显示器 | 已实现 `WM_GETMINMAXINFO`、`MonitorFromWindow` 和 `GetMonitorInfo`；编译与 smoke 通过；尚缺当前电脑最大化/还原及多显示器实机确认 |
| `AD-DEV-001` 开发环境页面恢复 | 待验证 | “项目”和“环境”页面均可见；环境页主动发现 Conda、uv、venv 和系统 Python；终端不再隐藏或替换环境控件 | 已删除终端覆盖环境页的旧路径，环境 Tab 主动调用 `EnsureLoadedAsync()`；源边界与 smoke 通过；尚缺真实环境列表和模块切换确认 |
| `AD-WEB-001` Dashboard 混合外链 | 待验证 | Dashboard 同域页面与必要登录留在内部；所有跨域外链使用系统默认浏览器；不再为外部网站创建内部 WebView2 窗口 | 新增 `DashboardNavigationPolicy`；同域、Cloudflare、GitHub 登录、Google AI Studio 跨域测试通过；尚缺真实 Dashboard 外链和登录流程确认 |
| `AD-WEB-002` WebView2 故障恢复 | 待验证 | `RenderProcessUnresponsive`、渲染进程退出或导航失败后，主 Dashboard 可以恢复或重建，不永久停留在错误状态 | 已实现 `RecoverDashboardAsync()` 并通过编译、Runtime 发布和源边界检查；尚缺真实渲染故障或长期运行确认 |
| `AD-TECH-001` 收敛冲突 Enhancer 路径 | 待验证 | 本轮不新增版本命名修正层；合并项目/环境/终端和窗口布局的重复控制；移除已失效事件与旧容器路径 | 未新增 Enhancer；已删除 `TerminalHostMode.Development` 和隐藏环境控件的旧路径，长期检查写入 smoke；尚缺真实模块切换回归确认 |

## 自动验证与候选证据

- 实施验证运行 `30724702000`：补丁、TerminalHost、完整 smoke、5 次 CMD/ConPTY、WPF Runtime publish、源边界和范围检查均通过；最终推送仅因 GitHub App 不允许同次修改 workflow 被拒绝。
- 已验证源码物化运行 `30724882998`：重新执行补丁、TerminalHost、完整 smoke、Runtime publish 和源码提交，全部通过。
- 最终长期源边界 smoke 运行 `30725116042`：新增 `V081SourceBoundaryChecks.cs` 后完整 smoke 与真实 ConPTY 再次通过。
- 产品 PR `#63` 只包含 11 个 P102 产品/smoke 文件，已合入 `p102-exp`，合并提交 `d8e0922e730cb31b01e2d27e2288b042bcbb413a`。
- 稳定候选 PR `#72` 完整 CI `30725452196` 通过：作用域、TerminalHost、完整 smoke、Runtime/Data 与安全边界、Runtime publish、manifest、ZIP 一致性和 Artifact 上传全部成功。
- 候选 Artifact：`8826179364`，Artifact ZIP SHA-256 `2e3ef1cd026777aeee69d16d947b176ef1acdc185a7df0fe0dc6cd45fca623cd`。
- 内层 Runtime ZIP：2,368,460 字节，SHA-256 `f88311560dff035b62cce5a732d3f124bde30f0e6cb8d42a50f6418c53c5f2a4`。
- 候选 `AtlasDesk.exe`：2,761,014 字节，SHA-256 `cd8b883a9fee319fe9bf88f668264fd3d9d3ec3a6d14caf91f6a6998141d9cca`。
- manifest 共登记 11 个公开 Runtime 文件，逐文件大小与 SHA-256 复核一致，不含任何 Roaming Data 或 Local Data。

## 暂缓或取消

| 工作项 | 状态 | 原因 | 重新评估条件 |
|---|---|---|---|
| AtlasDesk 通用内置浏览器 | 取消 | 需要维护标签页、下载、权限、OAuth、复杂网站和扩展兼容，成本显著高于收益；外部浏览器还能使用 Bitwarden 等现有能力 | 只有未来出现大量必须共享 AtlasDesk 内部会话、且系统浏览器无法满足的明确业务需求时重新评估 |

## 最近完成

| 工作项 | 结果 | 证据 |
|---|---|---|
| `AD-DOC-001` 对齐软件设计记录建议书 | 新增项目级开发约束、设计与演进和滚动工作记录；README 收缩为入口；阶段记录净化为 v0.8.0 正式快照 | `a2145e86`、`231a53ed`、`72125381`、`636331b3`、`07c68728` |
| `AD-MIG-001` v0.8.0 私人数据迁移 | 完整备份后完成 Dashboard/终端 Profile、日志和启动状态迁移，并删除旧 `PersonalWorkbench\App` Runtime | 用户 2026-08-02 实机运行迁移工具与收尾工具，最终输出 `Migration finish completed` |
| v0.8.0 正式发布 | 完成透明文件夹 Runtime、私人 Data 分离和安全中心正式交付 | `p102-v0.8.0`；CI `30719382922`、`30719585177`；完整证据见阶段记录 |

## 维护规则

- 新的独立需求、跨提交修复和需要实机验收的问题先进入活跃事项。
- 普通拼写、明显路径错误和同一工作项内的小回归不单独建项。
- 自动验证完成但仍缺真实 Windows 视觉或网页验证时，状态保持“待验证”，不能提前移入最近完成。
- 正式发布后，把已完成事项从活跃区移除；最近完成只保留少量摘要，历史由 Git、PR、标签和 Release 保存。
