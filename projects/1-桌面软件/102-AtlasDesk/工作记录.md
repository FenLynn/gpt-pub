# AtlasDesk 工作记录

本文件是滚动工作看板。正式版本与稳定候选证据见 [阶段记录](阶段记录.md)，长期规则见 [开发约束](开发约束.md)。

## 当前状态

- 正式主线：`main`，正式版本仍为 `v0.8.0`，未改动；
- 稳定候选：`p102-stable`，当前为 `v0.9.1`；
- 当前任务分支：`p102-v092-zotero-readonly`；
- 当前版本：`v0.9.2`；
- 产品 head `5ef68ad41ea41ef3939d1599a6a96078fd57eff5` 已通过完整 CI `30778337167`；
- 产品 PR：`#268`；
- 当前动作：文档准确 head 完整复验，通过后合入稳定分支并回流实验分支；
- 总目标：逐步固化至 `v1.0.0`，完成正式发布后停止本轮开发。

## v0.9.2 已完成

- Zotero 右侧改为紧凑只读条目详情，不提供数据库编辑；
- 高频信息固定显示标题、作者、来源、年份、Citation Key、DOI 与引用预览；
- Citation Key 支持原生字段与旧 Better BibTeX `Extra` 写法；
- 可复制标题、DOI、Citation Key、快速引用、LaTeX `\\cite{}` 与 Pandoc `[@key]`；
- 可打开主 PDF；
- 可在资源管理器中显示 PDF，方便单独复制原文件；
- 摘要、详情、附件、笔记、标签和分类保留为辅助只读信息；
- GB/T 7714 与 APA 只作为快速引用，界面明确提示正式投稿以 Zotero/CSL 为准；
- 新增引用格式、只读边界、文件定位和 XAML 非法字符回归测试；
- Runtime Artifact `8842841455`；
- Artifact digest `sha256:5930b62a23f6b79857e0e538a1cc62fcbd96041863b5dd8999e23cb16f736ced`。

## 后续路线

### v0.9.3｜项目与工作区使用链固化

- 收藏与最近项目；
- 失效路径重新定位；
- 项目、工作区、资源管理器和终端动作一致；
- 最近文件严格归属当前项目；
- Git 仅提供轻量状态，不发展为 Git 客户端。

### v0.9.4｜任务与工具最小闭环

- 工具执行进入统一任务状态；
- 等待、执行、成功、失败、取消与有限历史清晰可见；
- 退出时明确处理未完成任务；
- 不引入后台常驻服务、插件市场或复杂调度。

### v0.9.5｜稳定性与历史架构收敛

- 继续迁移并删除无必要的版本编号 Enhancer；
- 收敛重复导航、可见性和生命周期控制；
- 核查 WebView2、终端、Zotero 和任务取消后的资源释放；
- 检查启动、内存、Runtime/Data 与隐私边界；
- 原则上不增加大功能。

### v1.0.0｜正式发布

- 完整 Windows CI 与必要实机证据通过；
- 常见窗口尺寸、最大化、还原、100%—175% DPI 与多显示器无已知阻断问题；
- Dashboard、项目、工作区、终端、资料库、工具和任务主链稳定；
- `p102-stable` 完成正式准入后才合入 `main`；
- 建立 `p102-v1.0.0` 标签与正式 Release。

## 已完成基线

| 版本 | 关键结果 | 完整 CI |
|---|---|---|
| `v0.8.5` | 主壳 WorkArea/DPI、快速导航、Dashboard 有界恢复 | `30756405835` |
| `v0.8.6` | 退休 V068，建立职责命名宿主与所有权收敛 | `30756907537` |
| `v0.8.7` | 工作区、环境和项目统一终端连续性协调器 | `30757476643` |
| `v0.9.0` | 项目选择驱动上下文、工作区、终端、Git、环境和最近文件 | `30759759594` |
| `v0.9.1` | 原生 WorkArea 最大化边界与工具页纵向自适应 | `30772555331` |

## 当前已知限制

- 部分环境中 AtlasDesk 读取实时 `zotero.sqlite` 时不能与 Zotero 客户端稳定并行运行；
- 使用 AtlasDesk 资料库前关闭 Zotero；
- 不通过并行、长超时或破坏性重试掩盖该限制；
- 所有 Zotero 功能继续严格只读。

## 长期规则

- 不再新增版本编号 Enhancer；
- 历史层只能迁入职责命名组件后删除；
- 页面可见性不得触发数据库、Git 或外部工具；
- SQLite、Git、环境探测和文件扫描不得占用 WPF Dispatcher；
- 项目上下文只由选择或刷新触发，并必须可取消、有超时；
- 每个版本都先完成准确 head CI，再合入 `p102-stable` 并回流 `p102-exp`；
- `main` 只在 v1.0.0 正式发布时更新。
