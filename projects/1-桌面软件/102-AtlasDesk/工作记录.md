# AtlasDesk 工作记录

本文件是 AtlasDesk 的滚动工作看板，只保留当前活跃事项、暂缓/取消事项和少量最近完成。正式版本证据见 [阶段记录](阶段记录.md)，长期规则见 [开发约束](开发约束.md)。

## 当前目标

`v0.8.1` 已重新进入 `p102-stable` 并生成通过完整 Windows CI 的 Dashboard 修复候选。当前阶段等待真实 Windows 视觉、窗口、环境发现、混合外链及 Dashboard 长时间运行验证；实机确认前，所有相关事项保持“待验证”，`阶段记录.md` 继续冻结在正式版 `v0.8.0`。

- 当前开发分支：`p102-exp`
- 当前稳定候选：`p102-stable`
- 当前稳定合并：`3c0fc6cbccc3592ce37bdb77f6f7f06cfb71fae7`

## 活跃事项

| 工作项 | 状态 | 完成标准 | 证据或尚缺内容 |
|---|---|---|---|
| `AD-UI-001` 主壳连续式布局 | 待验证 | 去除左右大圆角外框、重复底纹和阴影；导航与内容仅以分割线区分；普通与最大化布局一致 | 产品与 smoke 已完成；尚缺真实 Windows 视觉确认 |
| `AD-WIN-001` 最大化与工作区修复 | 待验证 | 最大化遵守当前显示器 WorkArea；顶部、窗口按钮和底部均不被裁切 | 已实现 `WM_GETMINMAXINFO` 等边界；尚缺当前电脑最大化、还原和多显示器确认 |
| `AD-DEV-001` 开发环境页面恢复 | 待验证 | 项目与环境页均正常；环境页可发现 Conda、uv、venv 和系统 Python；终端不覆盖环境页 | 自动验证通过；尚缺真实环境列表和反复切换确认 |
| `AD-WEB-001` Dashboard 混合外链 | 待验证 | 同域页面和必要身份验证留在内部；跨域外链进入系统默认浏览器 | 路由单元检查通过；尚缺真实 Dashboard 外链和登录流程确认 |
| `AD-WEB-002` WebView2 故障恢复 | 待验证 | 渲染进程无响应、退出或导航失败后可重建主 Dashboard | 自动构建和边界检查通过；尚缺真实故障或长时间运行确认 |
| `AD-WEB-003` Dashboard 重复脚本异常弹窗 | 待验证 | 已确认的 `Cannot use 'in' operator to search for 'type' in undefined` 不再形成连续模态弹窗；正常 `alert`、`confirm`、`prompt` 不受影响；异常来源进入日志 | 仅精确抑制该已知 alert；增加 `error`、`unhandledrejection`、URL 脱敏和 5 秒去重；PR `#129`、`#130`；尚缺用户实机确认 |
| `AD-TEST-001` 主窗口完整启动探针 | 待验证 | CI 在 Windows STA 线程中实际构造 `MainWindow` 并挂载完整 `WorkbenchFeaturePipeline`，启动异常直接阻断候选 | CI `30738505459` 已输出 `PASS AtlasDesk MainWindow and full feature pipeline attach on an STA thread`；等待本候选实机启动确认 |
| `AD-TECH-001` 收敛冲突 Enhancer 路径 | 待验证 | 不新增版本命名修正层；移除失效控制路径；项目、环境、终端和窗口布局不互相覆盖 | 自动检查通过；尚缺真实模块切换回归确认 |

## 自动验证与候选证据

- 首轮 v0.8.1 实机发现项目中心初始化顺序空引用；已改为先赋值 `_tabs`、再绑定事件、最后设置初始索引，并固化顺序检查。
- Dashboard 实机发现网页 JavaScript 重复 `alert`；该问题不是新的 .NET 空引用，现只精确抑制已确认文本，保留正常网页脚本对话框。
- 最终候选 PR `#130` 已合入 `p102-stable`，稳定合并提交 `3c0fc6cbccc3592ce37bdb77f6f7f06cfb71fae7`。
- 完整 Windows CI `30738505459` 通过：P102 作用域、TerminalHost、完整 smoke、真实 STA 主窗口模块挂载、真实 ConPTY、Runtime/Data 与安全边界、WPF publish、manifest、ZIP 一致性及 Artifact 上传全部成功。
- 候选 Artifact：`8830472712`；外层 Artifact SHA-256 `eeb37e2e2d71bd9a10a7bcbf577305250fa11a1a90f41929fdd416153d4b3632`。
- 内层 Runtime ZIP：2,376,858 字节；SHA-256 `f311d39807c829472524c17d20d5206642574c8c7fd0ce8ae8ef6511d2982550`。
- 候选 `AtlasDesk.exe`：2,781,494 字节；SHA-256 `d1f76723040d1e4b92d5005047af9738cc3ff39fa23f7d89a4c5820e772dfefb`。
- manifest 登记 11 个公开 Runtime 文件，逐文件大小与 SHA-256 一致，不含任何 Roaming Data 或 Local Data。

## 暂缓或取消

| 工作项 | 状态 | 原因 | 重新评估条件 |
|---|---|---|---|
| AtlasDesk 通用内置浏览器 | 取消 | 维护标签、下载、权限、OAuth、复杂网站和扩展兼容的成本明显高于收益；系统浏览器还能使用 Bitwarden 等现有能力 | 只有出现大量必须共享 AtlasDesk 内部会话且系统浏览器无法满足的明确需求时重新评估 |

## 最近完成

| 工作项 | 结果 | 证据 |
|---|---|---|
| `AD-DOC-001` 对齐软件设计记录建议书 | 建立项目级开发约束、设计与演进和滚动工作记录；README 收缩为入口；阶段记录保持正式快照 | AtlasDesk 五文件记录体系 |
| `AD-MIG-001` v0.8.0 私人数据迁移 | 完整备份后完成 Dashboard/终端 Profile、日志和启动状态迁移，并删除旧 `PersonalWorkbench\\App` Runtime | 用户实机输出 `Migration finish completed` |
| v0.8.0 正式发布 | 完成透明文件夹 Runtime、私人 Data 分离和安全中心正式交付 | `p102-v0.8.0`；完整证据见阶段记录 |

## 维护规则

- 独立需求、跨提交修复、架构或数据变化以及需要实机验收的问题进入活跃事项。
- 自动验证完成但仍缺真实 Windows 视觉、网页或长期运行确认时，状态保持“待验证”。
- 正式发布后移除已完成活跃项；最近完成仅保留少量摘要，历史由 Git、PR、标签和 Release 保存。
