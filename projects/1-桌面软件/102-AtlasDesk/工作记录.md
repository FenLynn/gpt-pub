# AtlasDesk 工作记录

本文件是滚动工作看板。正式版本与稳定候选证据见 [阶段记录](阶段记录.md)，长期规则见 [开发约束](开发约束.md)。

## 当前状态

- 正式主线：`main`，正式版本仍为 `v0.8.0`，未改动；
- 当前任务分支：`p102-v094-task-tools`；
- 当前版本：`v0.9.4`；
- 产品 head `3b2321b794eea70f7b445746613969531b291961` 已通过完整 CI `30779651108`；
- 产品 PR：`#275`；
- 当前动作：文档准确 head 完整复验，通过后合入稳定分支并回流实验分支；
- 总目标：继续 v0.9.5，最终完成 v1.0.0 正式发布后停止。

## v0.9.4 已完成

- 文件完整性工具复用统一任务集合；
- 工具页和任务页显示同一条任务记录与实时状态；
- 两个页面均可取消同一操作；
- 清单生成、验证和文件比较结果进入有限历史；
- 关闭软件时对活动任务明确确认；
- 确认退出后取消任务并保留中断状态；
- 历史限制为最近 100 条；
- 不增加后台服务或复杂调度系统；
- Runtime Artifact `8843232793`；
- Artifact digest `sha256:0ae52c3a890910c41415e07ea626dd797eacccd760e781d60314d936adbb31a3`。

## 下一版本：v0.9.5

稳定性与历史架构收敛：

- 将任务和工具页面从版本编号 Enhancer 迁移到职责命名协调器；
- 删除重复的页面宿主与生命周期所有权；
- 清理临时类型兼容层；
- 核查共享任务、WebView2、终端和 Zotero 的关闭释放；
- 原则上不增加新功能。

## v1.0.0

- 全面正式准入与文档收口；
- 核心页面和主工作链无已知阻断问题；
- `p102-stable` 正式 CI 后合入 `main`；
- 建立 `p102-v1.0.0` 标签与正式 Release；
- 提供最终 Runtime 下载包后停止本轮开发。

## 长期规则

- 不新增版本编号 Enhancer；
- 不把 AtlasDesk 扩张为通用 IDE、浏览器或 Zotero 替代品；
- SQLite、Git、环境探测和文件扫描不得占用 WPF Dispatcher；
- 每个版本准确 head 完整 CI 后才能合入稳定分支；
- `main` 只在 v1.0.0 正式发布时更新。
