# AtlasDesk 工作记录

本文件是滚动工作看板。正式版本与发布证据见 [阶段记录](阶段记录.md)，长期规则见 [开发约束](开发约束.md)。

## 当前状态

- 正式版本：`v1.0.0`；
- 正式主线：`main`，本轮未修改；
- 当前候选版本：`v1.0.1`；
- 产品准确 head：`cd3e536b03c1e5b8401993d4936faa9a1c4d483f`；
- 候选产品 PR：`#284`，已合入 `p102-stable`；
- stable 合并提交：`558545a39f9e916febec2eded4ce019799e4d901`；
- 回流 PR：`#286`，已合入 `p102-exp`；
- exp 回流提交：`ec129d5a2a2f7b434afba5f7695b7c75f58b514f`；
- 最终完整 Windows CI：`30784290487`，success；
- Runtime Artifact：`8844746574`；
- Artifact digest：`sha256:f2b108199c1357cbd11ed338f231f8dd42919fbc687070d82ca4fcafb02aef2a`；
- 解出的 Runtime ZIP SHA-256：`d7fcf140ebb821df7183a47a703f3b580ce35b714e4b6ca694044986d2466bd9`；
- `p102-stable` 与 `p102-exp` 产品文件一致，差异仅为回流合并提交；
- 用户后续与 v1.0.0 集中实机比较；未经用户确认，不合入 `main`。

## v1.0.1 已完成

### 主页日常入口

- 主页只读取已有设置、保存项目路径、最近文件与有上限任务历史；
- 不因显示主页而扫描工作区、读取 Zotero 数据库、执行 Git、探测环境或启动外部工具；
- 主页刷新可取消，过期结果不能覆盖当前状态；
- 项目卡显示收藏、最近项目和失效路径；
- 任务卡显示活动状态与最近任务；
- Zotero 卡只显示配置与只读模式，真正数据库读取仍只在进入资料库后发生。

### 设置页可理解性

- Dashboard URL 提供即时格式状态；
- 工作区、Zotero、PDF 阅读器、Conda 与 uv 提供即时存在性状态；
- 路径不存在时只提示，不自动创建、搜索或猜测位置；
- 增加 Runtime 和日志目录入口；
- 保留 Roaming Data、Local Data 入口和现有设置分组；
- 不修改设置文件格式或数据目录结构。

### 资料库布局

- 保留 Zotero 三栏结构和严格只读边界；
- 分类栏、间距和右侧详情按窗口宽度分档收缩；
- 右侧详情最低保留 320 px；
- 标题、Citation Key、DOI、引用和 PDF 操作继续位于高优先级区域；
- 不新增数据库读取、写入或编辑能力。

### 回归保护

- 新增主页无数据库、无 Git、无项目扫描门禁；
- 使用真实临时路径验证项目、任务与最近文件聚合；
- 新增设置路径和 Zotero 三栏自适应门禁；
- v1.0.0 正式基线门禁支持补丁版本，版本不得低于 1.0.0；
- 全部 v1.0.0 历史 smoke、真实窗口、ConPTY、Dashboard、Runtime/Data、安全、publish、manifest 与 ZIP 均已通过。

## 候选发布边界

- v1.0.1 已形成 stable/exp 候选和可下载 Runtime；
- 用户后续与 v1.0.0 集中实机比较；
- 不合入 `main`，直至用户明确确认；
- 不创建正式标签或 Release；
- 本轮停止，不自动扩展到 v1.0.2。

## 已知限制继续保留

- 使用 AtlasDesk 资料库前关闭 Zotero；
- Zotero 功能保持严格只读；
- GB/T 7714 与 APA 为快速引用，正式投稿使用 Zotero/CSL 输出；
- v1.0.0 标签与 GitHub Release 仍需在具备对应平台写能力的环境补建。
