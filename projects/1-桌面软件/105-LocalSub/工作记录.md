# P105｜LocalSub 工作记录

> 只记录当前正在推进、待验证和下一步。历史基线见 `阶段记录.md`，长期规则见 `开发约束.md`。

## 当前工作主题

**Phase 1A 正式编号迁移与双进程实机验收**

## 当前状态

| 事项 | 状态 | 说明 |
|---|---|---|
| P105 正式编号建立 | 进行中 | 将旧错误 `P103 LocalSub` 迁为正式 `P105 LocalSub` |
| P105 源码完整迁入 | 进行中 | 来源固定为旧 Draft PR #387 head `88be3779...` 的 LocalSub 项目树 |
| P105 长期分支 | 进行中 | 只保留 `p105-exp`、`p105-stable` |
| P105 CI | 进行中 | 新建 `p105-localsub-ci.yml`，增加来源/目标与路径 scope gate |
| 错误 P103 LocalSub CI 清理 | 进行中 | 删除 main 中遗留的 `.github/workflows/p103-localsub-win-x64.yml`，不碰 DavBridge CI |
| Phase 1A Windows CI | 待验证 | P105 新 head 必须重新跑，旧 P103 run 只作为迁移来源证据 |
| Phase 1A 用户实机响应性 | 待验证 | 重点验证 Core 重负载和 Core 被杀死时 GUI 仍存活 |
| Phase 1B | 计划 | 实时 ASR、WASAPI、PotPlayer Process Loopback、模型重任务迁入 Core |
| WebView2 + Vue 3 + TypeScript 主界面 | 计划 | Core API 稳定后再开始 |

## 本轮完成标准

1. `main` 中出现正式 `projects/1-桌面软件/105-LocalSub/` 完整源码与项目文档。
2. `/目录.md` 与桌面软件 README 登记 P105，不修改 P103/P104 项目内容。
3. main 不再存在错误 `.github/workflows/p103-localsub-win-x64.yml`。
4. 活动 CI 统一为 `.github/workflows/p105-localsub-ci.yml`。
5. `p105-exp`、`p105-stable` 从最新 main 建立并按规则完成 `exp → stable → main` 准入。
6. 旧 LocalSub Draft PR #387 关闭，不再作为开发入口。
7. 旧 `p103-localsub-*` 分支在确认无独有内容后清理；若连接器缺少删除 ref 能力，必须明确记录为工具层阻塞，不得假装已删除。
8. DavBridge 的 `103-DavBridge/**` 与 `p103-davbridge-ci.yml` 保持不变。

## Phase 1A 下一实机断点

覆盖 P105 当前 CI 产出的 `LocalSub.exe` 与 `LocalSub.Core.exe` 后：

- 后台导入一个此前容易造成卡顿的视频。
- 波形生成和 SenseVoice 转写期间持续拖动、缩放、切页、滚动。
- 观察任务管理器中 `LocalSub.Core.exe` 承担主要重负载。
- 转写期间手动结束 `LocalSub.Core.exe`。
- 验证主窗口继续存活、当前任务失败、下一次任务重新启动 Core。
- 关闭 LocalSub 后确认 Core 不残留。

该实机项未完成前只能标记“待验证”，不创建正式 Release 标签。

## 下一开发动作

Phase 1A 实机边界确认后，从 `p105-exp` 进入 Phase 1B。优先迁移实时 ASR 与音频捕获中的重任务，不先做大规模主界面重写。