# P105｜LocalSub 工作记录

> 只记录当前正在推进、待验证和下一步。历史基线见 `阶段记录.md`，长期规则见 `开发约束.md`。

## 当前工作主题

**v0.1.1-dev｜Phase 1A Core 崩溃恢复加强**

P105 正式编号迁移和首次 Phase 1A 准入已经完成。当前从最新 main 的干净基线继续在 `p105-exp` 开发，不进入 Phase 1B，也不做大规模 UI 重写。

## 本轮候选

- Draft PR：#395 `p105-exp → p105-stable`
- 功能代码提交：`0ff1cd4dc308ce684e882a3e0c7724304da1ecb4`
- 首轮功能与交接候选 head：`cf7a1e443ba30bc1269b129cab2a317c9a396001`
- Windows CI：run `32128794091`，run number `11`，`success`
- Artifact：`9321423073`
- Artifact digest：`sha256:57a3d886f8e10363ef1895d2888d0f29b73a41f82fef16f6fe867ead59991a53`
- 完整候选包：`LocalSub-v0.1.1-dev-win-x64-net8.zip`

上述 run 已真实覆盖新增 Core 强杀恢复门禁。由于本记录本身会形成新的 PR head，后续是否可以提升仍只认 GitHub 上 **当前 PR exact head** 的最新成功 CI，不拿 `32128794091` 替代新 head 验证。

本轮主要变化：

- Shell/Core 连接增加 generation 代际，旧 reader loop 不得误伤重连后的新请求。
- Core 进程异常退出后当前 generation 立即标记失效并结束对应 pending request。
- 下一次后台请求重新启动新的 `LocalSub.Core.exe`，不复用已断开的 Pipe。
- 用户取消重任务后先发 `cancel`，若 1.5 秒内 Core 不收敛，则回收 worker，下一任务从新 Core 开始。
- Shell 正常退出时主动向共享 Core 发送 shutdown，并等待短时正常退出，超时才强制结束。
- 新增 `Logs/core-client.log`，记录连接 generation、PID、断开和恢复原因。
- Windows CI 新增真实故障注入：先建立 Core 请求，再强杀 Core，要求当前请求失败，随后同一个 Shell client 自动启动第二个 Core 并再次 ping 成功。
- Shell 与 Core 版本统一提升到 `0.1.1`，候选包名改为 `LocalSub-v0.1.1-dev-win-x64-net8.zip`。

## 当前状态

| 事项 | 状态 | 说明 |
|---|---|---|
| P105 正式编号与 main 基线 | 已完成 | 正式项目身份不再变更 |
| v0.1.1-dev 功能实现 | 已完成 | 当前位于 `p105-exp`，未进入 stable/main |
| Core generation 隔离 | 自动验证通过 | 防止旧连接退出回调影响新连接请求 |
| Core 强杀恢复 | 自动验证通过 | CI 真结束第一 Core，并由同一 Shell client 拉起第二 Core |
| 取消超时回收 | 已实现并编译通过 | 1.5 秒未收敛则回收卡死 worker |
| 正常退出主动 shutdown | 已实现并编译通过 | 不再只依赖 parent watcher |
| 原有 Process Loopback / sherpa / native offline ASR | 自动验证通过 | v0.1.1-dev 未破坏既有关键链路 |
| v0.1.1-dev Windows 候选包 | 首轮 Artifact 已生成 | 最终交付仍以当前 PR exact-head CI 产物为准 |
| Phase 1A 用户实机响应性 | 待验证 | 自动故障注入不能替代真实窗口高负载体验 |
| Phase 1B | 暂不进入 | 实机边界确认后再迁实时链 |

## 用户实机验收重点

候选包生成后只需覆盖 `LocalSub.exe` 与 `LocalSub.Core.exe`，原 `ASR/`、`ASR/_runtime/`、Assets、Components、config 和模型保持不动。

重点验证：

1. 后台导入一个此前容易造成卡顿的视频。
2. 波形生成和 SenseVoice 转写期间持续拖动、缩放、切页和滚动。
3. 任务管理器中主要重负载应进入 `LocalSub.Core.exe`。
4. 转写期间手动结束 `LocalSub.Core.exe`，主窗口必须继续存活，当前任务明确显示失败。
5. 再次发起后台任务，应自动出现新的 `LocalSub.Core.exe` 并继续工作。
6. 关闭 LocalSub 后确认 Core 不长期残留。
7. 异常时同时检查 `Logs/core.log`、`Logs/core-client.log` 和 `Logs/responsiveness.log`。

## 下一开发动作

先让文档收口后的当前 PR exact head 再通过一次完整 P105 Windows CI，然后把 v0.1.1-dev 作为 Draft 实机候选交付。用户确认 Phase 1A 响应性边界前不合入 `p105-stable`，不进入 Phase 1B。若实机边界成立，再从 `p105-exp` 逐项迁移实时 ASR 与音频捕获重任务。
