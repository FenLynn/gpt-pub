# P105｜LocalSub 工作记录

> 只记录当前正在推进、待验证和下一步。历史基线见 `阶段记录.md`，长期规则见 `开发约束.md`。

## 当前工作主题

**Phase 1B.0｜冻结 Application Contract，准备实时链与 Web UI 渐进迁移**

2026-09-11 开始新一轮架构改造。改造前 `main / p105-stable / p105-exp` 均为 `042329ede97b09cd375ebcf7c55d7245fc56b933`。

当前策略：

- `main` 保持正式主线。
- `p105-stable` 保持改造前可运行基线。
- 新架构只在 `p105-exp` 推进。
- 不建立额外长期分支。
- 不用正式 Release 充当测试备份。
- 需要旧版本时从 stable 或准确 SHA 重新构建。
- 新候选只有在自动门禁与必要实机验证通过后才允许提升到 stable。

## 当前事实

- v0.1.1 正式 Release 源码固定在 `225eafadb09f1553980153839f9d7c02bb79439c`，既有正式标签和 Release 不移动。
- 改造前冻结基线为 `042329ede97b09cd375ebcf7c55d7245fc56b933`。
- Phase 1A 已把后台媒体分析和后台离线 ASR 移入 `LocalSub.Core.exe`。
- 当前最大的剩余 Shell 重耦合是 `LiveAsrPipeline`，其中仍包含 WASAPI / PotPlayer Process Loopback、实时模型加载、VAD、音频 queue 和实时解码。
- LocalSub 已经实际依赖 WebView2，并使用透明 WebView2 字幕 Overlay，因此未来主 Web UI 不需要引入全新的浏览器运行时。
- 新增 `docs/APP_CONTRACT.md`，冻结 Shell、Core、Web UI 所有权、snapshot、白名单命令、实时 session 与回退规则。
- 原来的严格 `1A → 1B 全部完成 → Phase 2` 顺序已调整为 contract-first 渐进迁移。

## 当前状态

| 事项 | 状态 | 说明 |
|---|---|---|
| 改造前 stable/main 基线 | 已冻结 | `042329ed...` |
| Phase 1A 后台 Core 隔离 | 已完成自动化基线 | 实机高负载响应性仍待用户确认 |
| Application Contract | 已冻结第一版 | `docs/APP_CONTRACT.md` |
| Phase 1B.1 实时链迁 Core | 待实施 | 下一代码阶段 |
| Phase 2A Web Shell | 已规划 | 在稳定契约下可与 1B 后续交错 |
| 模型重任务迁 Core | 待实施 | Phase 1B.2 |
| 旧 WinForms 业务页退役 | 未开始 | Phase 3 |

## 当前唯一开发断点

下一代码阶段只在 `p105-exp` 处理 Phase 1B.1：

1. 为 Core 增加 realtime session 状态机。
2. 增加 `live.start / live.stop`。
3. 增加 `live.status / live.level / live.partial / live.final / live.discontinuity / live.failed`。
4. 将 `LiveAsrPipeline` 的音频捕获、实时模型加载和解码循环迁入 Core。
5. Shell 保留 PotPlayer 进程发现、PID、窗口 bounds 与 Overlay 跟随。
6. 保持现有 WinForms UI 暂时可用，用它验证新 Core realtime API。
7. 该阶段稳定后再建立 Vue 主 Shell，不直接翻译 `MainForm`。

## 最终自动门禁证据

v0.1.1 发布源码在 exact-main `225eafadb09f1553980153839f9d7c02bb79439c` 完成正式 Release。

发布后的 branch hygiene 收口进入 main：

- PR #414：`p105-exp → p105-stable`。
- PR #415：`p105-stable → main`。
- PR #415 P105 Windows CI：run `33979181832`，`success`。
- PR #415 repository hygiene：run `33979181809`，`success`。
- branch hygiene 收口 main push P105 Windows CI：run `33979276043`，build、release no-op 与 `branch_hygiene` 均 `success`。
- 同次 main push repository hygiene：run `33979276028`，`success`。

该 main push 中，`branch_hygiene` 已执行以下动作：

1. 非强制 fast-forward `p105-exp` 与 `p105-stable` 到当时最新 main。
2. 确认两个旧 `p103-localsub-*` ref 已不存在。
3. 安全删除 `_should_not_create`。
4. 再次校验 P105 活跃长期分支仅为 `p105-exp` 与 `p105-stable`，且二者与 main 同 SHA。

后续纯文档收口继续沿同一分支治理规则推进，并在完成后再次同步两条长期分支。具体当前 SHA 应直接读取 Git refs，不在状态文档中写死。

## 当前状态

| 事项 | 状态 | 说明 |
|---|---|---|
| P105 正式编号 | 已完成 | 永久为 P105 |
| v0.1.1 源码正式固化 | 已完成 | Release 源码固定在 `225eafad...` |
| p105-v0.1.1 Release | 已完成 | 正式资产已发布 |
| p103-localsub 分支清理 | 已完成 | 当前不存在 |
| `_should_not_create` 清理 | 已完成 | 已由 branch hygiene 物理删除 |
| P105 长期分支收敛 | 已完成 | 仅 `p105-exp`、`p105-stable` |
| main / stable / exp 同步机制 | 已完成 | main 收口后以非强制 fast-forward 归一，两条长期分支不得保留独有提交 |
| 发布与 branch hygiene 解耦 | 已完成 | Release 只处理不可变发布，main push 独立执行幂等分支治理 |
| 用户特定高负载 GUI 响应性 | 待实机验证 | 自动 CI 不替代真实媒体、模型和机器负载验证 |

## 后续开发断点

下一轮功能开发从最新 `p105-exp` 开始。Phase 1A 的用户特定高负载 GUI 响应性仍是唯一明确保留的实机验证项；在用户完成该验证前，不得把它记录为已完成。

完成实机验证后，再进入 Phase 1B：逐步把 realtime ASR、WASAPI、PotPlayer Process Loopback 和模型重任务继续迁入 Core。WebView2 + Vue 3 + TypeScript UI 迁移仍应等待 Core API 稳定后再推进。