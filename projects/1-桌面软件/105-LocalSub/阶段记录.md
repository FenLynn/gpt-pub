# P105｜LocalSub 阶段记录

## 当前阶段

**Phase 1A：后台重任务进程隔离已正式准入 P105，用户实机响应性待验证**

## 阶段目标

先把媒体解析、波形、VAD 与后台离线 ASR 从 WinForms GUI 进程中隔离到 `LocalSub.Core.exe`，验证重任务异常不会把 `LocalSub.exe` 一起拖成未响应。Core API 稳定后再继续实时链和 Web 主界面迁移。

## 迁移来源基线

2026-08-18 P105 正式化前，LocalSub 曾因编号判断错误暂存为 `103-LocalSub`。用于 P105 迁移的固定来源为：

- 历史 Draft PR：#387
- 历史分支：`p103-localsub-exp`
- 固定来源 head：`88be3779de4777705d4f3ebb552231d381d31a75`
- 历史 Windows CI：run `32085583218`，run number `168`，`success`
- 历史 Artifact ID：`9306612400`
- 历史 Artifact digest：`sha256:95ce1b120bb0d677ea63c181a22f0f05ddfb4d13fde3c1b28af79e6bfa11f42f`

该来源只用于证明 Phase 1A 代码与双进程 CI 已存在，不赋予 P103 项目身份。P103 始终属于 DavBridge。

## P105 正式准入证据

### `p105-exp → p105-stable`

- PR：#388 `P105 LocalSub｜正式编号迁移与 Phase 1A 准入`
- exact head：`15b6c336184c900d480be87204ea31adc034cbf8`
- Windows CI：run `32108500372`，run number `6`，`success`
- Artifact：`9314040768`
- Artifact digest：`sha256:f751b6d561ca14a8fc25285694be2b79819e4b6c71453e855c3eaf60f2006b6f`
- stable 准入提交：`d46eb92cb516377d4ae7c4cbf1a6c1b8f9f9ea4f`

### `p105-stable → main`

- PR：#389 `P105 LocalSub｜Phase 1A 正式准入 main`
- exact head：`d46eb92cb516377d4ae7c4cbf1a6c1b8f9f9ea4f`
- Windows CI：run `32108734999`，run number `7`，`success`
- Artifact：`9314113175`
- Artifact digest：`sha256:45b6c5f20e0c5f0526e11e3f364d56cbe8cff5dfe750c9c078414553192415c3`
- 首次正式 main 准入提交：`f63e5cfa18c345cb4b28d2640ae920c8e3aed856`

P105 首次正式准入后已将 `p105-exp` 与 `p105-stable` 安全 fast-forward 回流到 main。后续当前 head 仍以 GitHub 实时分支状态为准，不用本文件中的历史 SHA 代替 current-head 核验。

旧 Draft PR #387 已写入 P105 迁移说明并关闭，未合并。旧 `p103-localsub-*` branch refs 仅剩历史引用层清理，当前连接器无 delete-ref 能力，不再作为活动维护入口。

## Phase 1A 已实现能力

- `LocalSub.Core.exe` 独立发布。
- Windows Named Pipe + UTF-8 newline-delimited JSON IPC。
- v1 方法：`ping / analyze / transcribe / cancel / shutdown`。
- 媒体解析、波形数据生成、后台离线 ASR 从 Shell 编译边界移出。
- `LocalSub.exe` 对上述功能只保留 Proxy，不允许 Core 失败后静默回进程内执行。
- Core 跟踪父进程，Shell 正常退出时 Core 应退出。
- Core 异常退出时当前后台任务失败，GUI 应保持存活，下一次重任务重新启动 Core。

## Windows 自动验证基线

P105 stable 与 main 两次 current exact-head CI 均完整覆盖：

- Shell/Core 独立 publish。
- 双 EXE 绿色目录组装。
- Core 真启动、Named Pipe 真连接、`ping` 与 `shutdown`。
- Shell 真启动。
- 后台工作区真进入。
- Process Loopback 真激活。
- sherpa native runtime 真加载。
- native offline ASR 真解码。
- 最终包不携带模型、FFmpeg、ONNX Runtime、sherpa native runtime。

## 实机状态

**待验证。**

尚需用户在真实 Windows 环境确认：后台重任务期间 GUI 持续响应，主要负载进入 Core；手动结束 Core 不导致 Shell 退出；下一次任务能重新启动 Core；关闭 Shell 后无长期孤儿 Core。

CI 无法证明 WinForms 消息循环在用户真实模型、媒体和机器负载下绝不阻塞，因此该项在用户确认前不得改写为“已验证”。

## 下一阶段

Phase 1A 实机边界确认后进入 Phase 1B：

- Streaming Zipformer / Paraformer / SenseVoice 实时识别迁入 Core。
- WASAPI 与 PotPlayer Process Loopback 迁入 Core。
- 模型下载、解压、删除与状态计算等重任务逐步迁入 Core。
- Shell 逐步收敛为轻量 Windows host。

之后才进入 WebView2 + Vue 3 + TypeScript 主界面迁移。