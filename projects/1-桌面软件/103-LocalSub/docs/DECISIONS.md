# LocalSub 决策基线

- Windows 绿色运行包，解压即用。
- 模型不随程序分发。
- 默认模型根目录为 `<EXE>\ASR`，设置中可改为任意目录。
- 模型独立下载、删除、升级、完整性检测。
- 模型下载支持直连、系统代理和显式 SOCKS5 代理。
- 实时音源仅保留 PotPlayer 与所有音频两种模式。
- 实时字幕使用 HTML/CSS 渲染。
- 后台支持拖入视频高速转写、波形、VAD 语音轨、关键词高亮和标记。
- 导出至少支持 TXT，内部使用结构化项目数据。
- 第一阶段不把云端 LLM 或 API 作为依赖。
